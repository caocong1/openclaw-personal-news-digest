---
phase: 04-integration-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/prompts/summarize.md
  - config/preferences.json
  - references/scoring-formula.md
  - config/sources.json
  - references/data-models.md
autonomous: true
requirements:
  - PREF-07
  - ANTI-05
  - SRC-09
  - OUT-02

must_haves:
  truths:
    - "Daily summarize.md prompt includes {depth_preference} and {judgment_angles} placeholders, producing variable-depth output matching user preference (brief/moderate/detailed/technical)"
    - "preferences.json contains style.last_exploration_increase field (null default), gating exploration_appetite increment to once per 7 days instead of every pipeline run"
    - "scoring-formula.md documents the 0.5x degraded source penalty under Source Trust, and sources.json entries include degraded_since/recovery_streak_start stats fields"
    - "data-models.md DailyMetrics schema includes alerts_sent_today (int, default 0) and alerted_urls (array, default []) for breaking news alert cap enforcement"
    - "Re-audit passes with 0 integration gaps (MISSING-01 through MISSING-05 closed) and 0 broken E2E flows (BROKEN-01, BROKEN-02 fixed)"
  artifacts:
    - references/prompts/summarize.md
    - config/preferences.json
    - references/scoring-formula.md
    - config/sources.json
    - references/data-models.md
  key_links:
    - "summarize.md reads {depth_preference} and {judgment_angles} from preferences.json via processing pipeline"
    - "preferences.json style.last_exploration_increase consumed by processing-instructions.md Section 4 Step 7"
    - "scoring-formula.md degraded penalty matches processing-instructions.md Section 6 definition"
    - "sources.json degraded_since/recovery_streak_start consumed by processing-instructions.md Section 6 state machine"
    - "data-models.md alerts_sent_today/alerted_urls consumed by SKILL.md Quick-Check Flow step 2"
---

<objective>
Close all 5 cross-phase integration gaps (MISSING-01 through MISSING-05) and fix 2 broken E2E flows (BROKEN-01, BROKEN-02) identified by the v1.0 milestone audit. Targeted field/placeholder additions only -- no new features, no logic changes.

Purpose: Make daily depth control and exploration auto-correction actually function at runtime, and ensure scoring-formula.md and data-models.md are authoritative references matching their consumers.
Output: 5 files edited with additive-only changes; backward compatibility preserved.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-integration-wiring-fixes/04-RESEARCH.md
@references/prompts/weekly-report.md
@references/processing-instructions.md
@references/output-templates.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire depth/judgment placeholders into summarize.md and add last_exploration_increase to preferences.json</name>
  <files>references/prompts/summarize.md, config/preferences.json</files>
  <action>
**Fix 1 (MISSING-01, BROKEN-01 — PREF-07):** Edit `references/prompts/summarize.md` to add depth-aware summarization:

1. Add a "User Preferences Context" section (modeled after `references/prompts/weekly-report.md`) with two placeholders:
   ```
   ## User Preferences Context
   Depth: {depth_preference}
   Angles: {judgment_angles or "none specified"}
   ```

2. Add a "Depth-Adjusted Requirements" section that replaces the fixed length rule with depth-dependent instructions:
   - "brief": 1 sentence per item, core fact only
   - "moderate": 2-3 sentences (current default — this preserves backward compatibility)
   - "detailed": 3-5 sentences, include background context and significance
   - "technical": same as detailed, plus implementation/technical specifics where relevant

3. Add judgment_angles instruction: "If judgment_angles is not empty: for each item where an angle applies, briefly note that perspective."

**CRITICAL constraint (from STATE.md decision):** depth_preference and judgment_angles go into the summarize prompt ONLY, NOT the scoring formula.

**Backward compatibility:** The "moderate" depth must produce identical 2-3 sentence output to the current fixed prompt. When depth_preference is missing or empty, default to "moderate" behavior.

**Fix 2 (MISSING-04, BROKEN-02 — ANTI-05):** Edit `config/preferences.json` to add the missing field to the `style` object:
- Add `"last_exploration_increase": null` after `"rumor_tolerance": "low"`
- This is the exact field that `processing-instructions.md` Section 4 Step 7 reads to gate the 7-day auto-increase
- With null default, the first pipeline run will correctly treat it as "never increased" and do one initial increment, then record the date — subsequent runs will respect the 7-day gate
  </action>
  <verify>
    <automated>grep -q "depth_preference" references/prompts/summarize.md && grep -q "judgment_angles" references/prompts/summarize.md && grep -q "last_exploration_increase" config/preferences.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- summarize.md contains {depth_preference} and {judgment_angles} placeholders with depth-conditional length instructions
- "moderate" depth produces same 2-3 sentence output as before (backward compatible)
- preferences.json style object has last_exploration_increase: null field
- MISSING-01, MISSING-04, BROKEN-01, BROKEN-02 all closed
  </done>
</task>

<task type="auto">
  <name>Task 2: Add degraded penalty to scoring-formula.md, stats fields to sources.json, and alert fields to data-models.md</name>
  <files>references/scoring-formula.md, config/sources.json, references/data-models.md</files>
  <action>
**Fix 3 (MISSING-02 — SRC-09):** Edit `references/scoring-formula.md`:

1. Under the "Source Trust" section (Section 3), add a "Degraded source penalty" note:
   ```
   **Degraded source penalty:** If `source.status == "degraded"` (see `config/sources.json`),
   multiply the `source_trust` value by **0.5** before including it in the weighted sum.
   This deprioritizes items from degraded sources without completely excluding them.
   See `references/processing-instructions.md` Section 6 for the full demotion/recovery state machine.
   ```

2. Update the Phase Activation Status section at the bottom of the file to reflect Phase 4 additions (the degraded penalty documentation). Do NOT change any scoring weights or formula logic.

**Fix 4 (MISSING-05 — SRC-09):** Edit `config/sources.json`:

1. Add `"degraded_since": null` and `"recovery_streak_start": null` to the `stats` object of ALL 6 source entries
2. Place them after the existing `selection_rate` field for consistency
3. Verify all 6 entries are updated (count must match — do not miss any)
4. Use the same null defaults documented in `references/data-models.md` (lines 132-133, 146-149)

**Fix 5 (MISSING-03 — OUT-02):** Edit `references/data-models.md`:

1. Add two fields to the DailyMetrics schema (as TOP-LEVEL fields alongside `sources`, `items`, `llm`, `output` — NOT nested inside `output`):
   - `alerts_sent_today`: integer, default 0
   - `alerted_urls`: array of strings, default []

2. Add field notes explaining their purpose:
   - `alerts_sent_today`: Integer count of breaking news alerts sent during quick-check runs today. Default 0. Read by quick-check flow to enforce 3-alert daily cap.
   - `alerted_urls`: Array of URL strings already alerted today. Default []. Read by quick-check flow for same-URL dedup.

3. Place them in the JSON schema example alongside the existing top-level metric groups, following the pattern from `processing-instructions.md` Section 5 (lines 649-650).
  </action>
  <verify>
    <automated>grep -q "degraded" references/scoring-formula.md && grep -q "degraded_since" config/sources.json && grep -q "recovery_streak_start" config/sources.json && grep -q "alerts_sent_today" references/data-models.md && grep -q "alerted_urls" references/data-models.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- scoring-formula.md documents the 0.5x degraded source penalty under Source Trust section
- scoring-formula.md Phase Activation Status updated to include Phase 4 degraded penalty
- All 6 source entries in sources.json include degraded_since: null and recovery_streak_start: null
- data-models.md DailyMetrics schema includes alerts_sent_today (int, 0) and alerted_urls (array, []) as top-level fields
- MISSING-02, MISSING-03, MISSING-05 all closed
  </done>
</task>

</tasks>

<verification>
All 5 integration gaps (MISSING-01 through MISSING-05) closed:
1. `grep "depth_preference" references/prompts/summarize.md` returns matches
2. `grep "judgment_angles" references/prompts/summarize.md` returns matches
3. `grep "last_exploration_increase" config/preferences.json` returns match
4. `grep "degraded" references/scoring-formula.md` returns match for 0.5x penalty
5. `grep "degraded_since" config/sources.json` returns 6 matches (one per source)
6. `grep "recovery_streak_start" config/sources.json` returns 6 matches (one per source)
7. `grep "alerts_sent_today" references/data-models.md` returns match
8. `grep "alerted_urls" references/data-models.md` returns match

Both broken E2E flows fixed:
- BROKEN-01: summarize.md now has {depth_preference} placeholder for depth-variable output
- BROKEN-02: preferences.json now has last_exploration_increase: null for 7-day gating
</verification>

<success_criteria>
1. Daily summarize.md prompt includes {depth_preference} and {judgment_angles} placeholders with depth-conditional length rules (brief/moderate/detailed/technical)
2. preferences.json style object contains last_exploration_increase: null
3. scoring-formula.md Source Trust section documents 0.5x degraded source penalty
4. All 6 sources.json entries have degraded_since: null and recovery_streak_start: null in stats
5. data-models.md DailyMetrics has alerts_sent_today (int) and alerted_urls (array) as top-level fields
6. No existing behavior changed — all fixes are additive with backward-compatible defaults
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-wiring-fixes/04-01-SUMMARY.md`
</output>

---
phase: 06-per-source-metrics-continuity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/data-models.md
  - references/processing-instructions.md
  - references/collection-instructions.md
  - SKILL.md
autonomous: true
requirements: [SRC-08, SRC-09, MON-02, MON-03]

must_haves:
  truths:
    - "DailyMetrics schema documents a per_source field with fetched, deduped, title_deduped, selected, status, error sub-fields"
    - "Processing instructions describe how per-source counters are accumulated during the pipeline and written to daily metrics"
    - "Collection instructions source health formulas explicitly reference per_source from DailyMetrics as their data source"
    - "health-check.sh field names (fetched, status, error) match the documented per_source schema without modification"
    - "SKILL.md mentions per-source counter accumulation in Collection Phase and per_source in metrics write"
  artifacts:
    - path: "references/data-models.md"
      provides: "per_source field in DailyMetrics schema"
      contains: "per_source"
    - path: "references/processing-instructions.md"
      provides: "Per-source metrics producer steps"
      contains: "per_source"
    - path: "references/collection-instructions.md"
      provides: "Source health formulas referencing per_source"
      contains: "per_source"
    - path: "SKILL.md"
      provides: "Per-source counter mention in pipeline steps"
      contains: "per_source"
  key_links:
    - from: "references/processing-instructions.md"
      to: "references/data-models.md"
      via: "Section 5 references DailyMetrics per_source schema"
      pattern: "per_source"
    - from: "references/collection-instructions.md"
      to: "references/data-models.md"
      via: "Source health formulas read per_source from daily metrics"
      pattern: "per_source.*fetched|per_source.*selected|per_source.*status"
    - from: "scripts/health-check.sh"
      to: "references/data-models.md"
      via: "Checks #10 and #15 read per_source fields"
      pattern: "per_source"
---

<objective>
Add the per_source DailyMetrics contract and producer wiring so source health computation, monitoring alerts, and auto-demotion/recovery operate end-to-end.

Purpose: Close MISSING-06 and BROKEN-03 from the v1.0 milestone audit. The per_source field is the missing link between the daily pipeline (which has the data) and the source health/monitoring consumers (which need the data).

Output: Updated data-models.md, processing-instructions.md, collection-instructions.md, and SKILL.md with consistent per_source contract.
</objective>

<execution_context>
@C:/Users/sorawatcher/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sorawatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-per-source-metrics-continuity/06-RESEARCH.md

<interfaces>
<!-- Key schemas and consumer patterns the executor needs -->

From scripts/health-check.sh check #10 (source concentration, line ~252-268):
```python
per_source = m.get('per_source', {})
for source_id, stats in per_source.items():
    source_fetched = stats.get('fetched', 0)
```

From scripts/health-check.sh check #15 (weekly source success, line ~438-472):
```python
per_source = m.get('per_source', {})
for sid, stats in per_source.items():
    if stats.get('status') == 'success':
        source_stats[sid]['success'] += 1
    source_stats[sid]['total'] += 1
```

From references/collection-instructions.md "Source Health Metrics Computation":
```
selection_rate = items_selected_for_output / total_fetched
dedup_rate = items_deduped / total_fetched
fetch_success_rate = successful_fetches / total_fetch_attempts
quality_score = selection_rate * 0.4 + (1 - dedup_rate) * 0.3 + fetch_success_rate * 0.3
```

From references/data-models.md DailyMetrics (current, no per_source field):
```json
{
  "date": "YYYY-MM-DD",
  "run_id": "run-YYYYMMDD-HHmmss-XXXX",
  "sources": { "total": 1, "success": 0, "failed": 0, "degraded": 0 },
  "items": { "fetched": 0, "url_deduped": 0, "title_deduped": 0, ... },
  ...
}
```

From SKILL.md Output Phase step 7:
```
7. **Write metrics**: Write `{baseDir}/data/metrics/daily-YYYY-MM-DD.json` with run statistics.
   Include `quota_distribution`, `category_proportions`, `source_proportions` in daily metrics.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document per_source schema in DailyMetrics and add producer steps to processing-instructions.md</name>
  <files>references/data-models.md, references/processing-instructions.md</files>
  <read_first>
    - references/data-models.md (current DailyMetrics schema, line ~312-363)
    - references/processing-instructions.md (Section 5 metrics collection, line ~638-658; Section 4 output generation metrics write, line ~620-633)
    - scripts/health-check.sh (consumer field names in checks #10 and #15, lines ~252-268 and ~438-472)
    - .planning/phases/06-per-source-metrics-continuity/06-RESEARCH.md (required schema and producer wiring points)
  </read_first>
  <action>
**In references/data-models.md**, add a `per_source` field to the DailyMetrics JSON schema block (after `source_proportions` and before `alerts`). Add exactly this JSON fragment inside the DailyMetrics schema:

```json
"per_source": {
  "src-36kr": {
    "fetched": 12,
    "deduped": 3,
    "title_deduped": 1,
    "selected": 4,
    "status": "success",
    "error": null
  }
}
```

Add a field notes block for per_source after the existing "Field notes (alerts)" section:

```
**Field notes (per_source):**
- `per_source`: Per-source-id breakdown of pipeline counters for this run. Keyed by `source_id` string. Each value is an object with:
  - `fetched` (integer): Items fetched from this source in this run
  - `deduped` (integer): Items from this source that were URL-deduped (skipped because hash already in dedup-index)
  - `title_deduped` (integer): Items from this source marked as title duplicates during Stage B/C title dedup
  - `selected` (integer): Items from this source that received a `quota_group` tag (selected for digest output)
  - `status` (string): `"success"` if source fetched >= 1 item, `"failed"` if fetch returned 0 items or encountered an error
  - `error` (string or null): Error message if fetch failed, `null` on success
- Sources not attempted in this run (e.g., disabled sources) are omitted from `per_source`
- Historical metrics files from before Phase 6 will lack `per_source`; consumers MUST use `.get('per_source', {})` for backward compatibility
```

**In references/processing-instructions.md Section 5**, add a new subsection titled "### Per-Source Metrics Accumulation" after the existing metrics table in Section 5 (after line ~658). Add this content:

```markdown
### Per-Source Metrics Accumulation

During each pipeline run, accumulate per-source counters alongside aggregate metrics. These feed into the `per_source` field of DailyMetrics (see `references/data-models.md`).

**Accumulation points (by pipeline step):**

| Counter | When to Increment | Pipeline Step |
|---------|-------------------|---------------|
| `fetched` | Each item successfully fetched from a source | Collection Phase step 4 (fetch by type) |
| `deduped` | Each item from a source skipped by URL dedup (hash exists in dedup-index) | Collection Phase step 6 (dedup) |
| `status` | Set to `"success"` if fetched >= 1; `"failed"` if fetched == 0 or error | After Collection Phase step 4 completes for each source |
| `error` | Set to error message string on fetch failure; `null` on success | After Collection Phase step 4 completes for each source |
| `title_deduped` | Each item from a source marked `dedup_status: "title_dup"` | Processing Phase step 8 (title dedup) |
| `selected` | Each item from a source that receives a `quota_group` tag | Output Phase step 3 (quota allocation, Section 4 Step 8) |

**Persistence:** Write the accumulated `per_source` map as part of the daily metrics file during Output Phase step 7. The `per_source` field is additive to existing aggregate counters (`sources.*`, `items.*`); do not remove or modify existing fields.

**Backward compatibility:** Metrics files from before this change lack `per_source`. All consumers (health-check.sh, source health computation) already use `.get('per_source', {})` which returns an empty dict gracefully. No backfill of historical metrics files is needed.
```

IMPORTANT: Do NOT modify any existing content in either file. These are purely additive changes.
  </action>
  <verify>
    <automated>grep -c "per_source" references/data-models.md && grep -c "Per-Source Metrics Accumulation" references/processing-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - references/data-models.md contains the string `"per_source"` inside the DailyMetrics JSON block
    - references/data-models.md contains `fetched` `deduped` `title_deduped` `selected` `status` `error` as documented sub-fields of per_source
    - references/data-models.md contains the text "Field notes (per_source)"
    - references/processing-instructions.md contains the heading "### Per-Source Metrics Accumulation"
    - references/processing-instructions.md contains the text "Collection Phase step 4" and "Output Phase step 7" in the accumulation table
    - references/processing-instructions.md contains the text "Backward compatibility"
    - All existing DailyMetrics fields (sources, items, llm, output, quota_distribution, category_proportions, source_proportions, alerts, alerts_sent_today, alerted_urls) remain present and unchanged
  </acceptance_criteria>
  <done>DailyMetrics per_source schema is documented with all 6 sub-fields matching health-check.sh consumer expectations. Producer accumulation steps are documented with specific pipeline step references.</done>
</task>

<task type="auto">
  <name>Task 2: Wire per_source references into collection-instructions.md source health formulas and SKILL.md pipeline steps</name>
  <files>references/collection-instructions.md, SKILL.md</files>
  <read_first>
    - references/collection-instructions.md (Source Health Metrics Computation section, starting around line ~486)
    - SKILL.md (Collection Phase steps 4-8, Output Phase step 7)
    - references/data-models.md (verify per_source schema from Task 1 is present)
    - scripts/health-check.sh (checks #10 and #15 to confirm field name alignment)
  </read_first>
  <action>
**In references/collection-instructions.md "Source Health Metrics Computation" section**, update the formula descriptions to explicitly reference `per_source` from DailyMetrics as the data source. Currently the formulas say generic things like "from daily metrics files" and "counted by source_id". Make these changes:

1. After the "### Metrics Formula" heading and the line "For each source that contributed items in the current run, compute updated stats using the last 7 days of data:", add a new paragraph:

```markdown
**Data source:** Read `per_source` from `data/metrics/daily-YYYY-MM-DD.json` for each of the last 7 days. For each source_id, the `per_source[source_id]` object provides `fetched`, `deduped`, `title_deduped`, `selected`, `status`, and `error` fields. See `references/data-models.md` DailyMetrics schema for field definitions. If a metrics file lacks `per_source` (pre-Phase-6 files), treat as empty dict -- that source has no data for that day.
```

2. Update the three formula definitions to reference specific per_source fields. Replace the current bullet explanations:

For **selection_rate**:
- Change `items_selected_for_output`: from "count of items from this source that appear in the final digest output (last 7 days of daily metrics files)" to "sum of `per_source[source_id].selected` across last 7 days of daily metrics"
- Change `total_fetched`: from "sum of items fetched from this source (last 7 days of JSONL data, counted by source_id)" to "sum of `per_source[source_id].fetched` across last 7 days of daily metrics"

For **dedup_rate**:
- Change `items_deduped`: from "count of items from this source that were deduplicated (appeared in dedup-index already at fetch time, last 7 days)" to "sum of `per_source[source_id].deduped` across last 7 days of daily metrics"
- Change `total_fetched`: same as above

For **fetch_success_rate**:
- Change `successful_fetches`: from "count of runs where this source fetched >= 1 item (from daily metrics files, last 7 days)" to "count of days where `per_source[source_id].status == 'success'` across last 7 days of daily metrics"
- Change `total_fetch_attempts`: from "count of runs where this source was attempted (from daily metrics files, last 7 days)" to "count of days where `source_id` appears in `per_source` across last 7 days of daily metrics"

**In SKILL.md**, make two small additive changes:

1. In Collection Phase, after step 8 ("Update dedup index"), add a new note to step 4 or add a brief inline note. Specifically, update step 4 "Fetch by type" line to append: "Track per-source counters (`fetched`, `status`, `error`) during collection; also count `deduped` items per source during step 6."

The current step 4 line reads:
```
4. **Fetch by type**: For each source, route by `source.type`. ...
```

Append to the end of that step (after the last sentence before step 5):
```
 Track per-source counters (fetched, deduped, status, error) during collection for DailyMetrics `per_source` field.
```

2. In Output Phase step 7, update the "Write metrics" line. The current text is:
```
7. **Write metrics**: Write `{baseDir}/data/metrics/daily-YYYY-MM-DD.json` with run statistics. Include `quota_distribution`, `category_proportions`, `source_proportions` in daily metrics.
```

Change to:
```
7. **Write metrics**: Write `{baseDir}/data/metrics/daily-YYYY-MM-DD.json` with run statistics. Include `quota_distribution`, `category_proportions`, `source_proportions`, and `per_source` (per-source pipeline counters) in daily metrics.
```

IMPORTANT: Do NOT rewrite entire sections. Make surgical edits to existing text. Preserve all existing content.
  </action>
  <verify>
    <automated>grep -c "per_source" references/collection-instructions.md && grep "per_source" SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - references/collection-instructions.md contains the string "per_source[source_id].selected"
    - references/collection-instructions.md contains the string "per_source[source_id].fetched"
    - references/collection-instructions.md contains the string "per_source[source_id].deduped"
    - references/collection-instructions.md contains the string "per_source[source_id].status"
    - references/collection-instructions.md contains the text "Data source:" paragraph referencing per_source from DailyMetrics
    - SKILL.md Collection Phase step 4 contains "per_source" or "per-source counters"
    - SKILL.md Output Phase step 7 contains "per_source"
    - health-check.sh is NOT modified (field names already match: fetched, status, error)
    - All existing content in collection-instructions.md and SKILL.md is preserved (no sections removed or restructured)
  </acceptance_criteria>
  <done>Source health formulas explicitly reference per_source fields from DailyMetrics. SKILL.md pipeline steps mention per-source counter accumulation and per_source in metrics write. The full producer-to-consumer chain is documented end-to-end.</done>
</task>

</tasks>

<verification>
1. `grep "per_source" references/data-models.md` shows the field in DailyMetrics schema
2. `grep "Per-Source Metrics Accumulation" references/processing-instructions.md` confirms producer documentation exists
3. `grep "per_source\[source_id\]" references/collection-instructions.md` confirms formulas reference the contract
4. `grep "per_source" SKILL.md` confirms pipeline steps reference per-source tracking
5. `bash scripts/health-check.sh . --mode daily` runs without errors (health-check.sh unchanged, consumers already use correct field names)
6. Field names in data-models.md per_source schema (fetched, deduped, title_deduped, selected, status, error) match what health-check.sh reads
</verification>

<success_criteria>
- DailyMetrics schema in data-models.md includes per_source with all 6 sub-fields
- Processing-instructions.md documents per-source counter accumulation at each pipeline step
- Collection-instructions.md source health formulas explicitly reference per_source fields as data source
- SKILL.md mentions per-source counter tracking in Collection Phase and per_source in Output Phase metrics write
- health-check.sh field names (fetched, status) match the documented schema -- no changes needed to the script
- Re-audit should no longer report MISSING-06 (no per_source schema) or BROKEN-03 (degrade/recover missing per-source history)
</success_criteria>

<output>
After completion, create `.planning/phases/06-per-source-metrics-continuity/06-01-SUMMARY.md`
</output>

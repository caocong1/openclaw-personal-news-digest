---
phase: 03-closed-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - references/processing-instructions.md
  - references/collection-instructions.md
  - references/data-models.md
  - config/sources.json
  - SKILL.md
autonomous: true
requirements: [SRC-09]

must_haves:
  truths:
    - "Source with quality_score < 0.2 for 14 consecutive days is auto-demoted to degraded status"
    - "Degraded source with quality_score > 0.3 for 7 consecutive days is auto-recovered to active status"
    - "Degraded sources are still collected but deprioritized in scoring"
    - "degraded_since resets when quality recovers before the 14-day trigger"
    - "recovery_streak_start resets when quality dips again during degraded status"
    - "Source status transitions are logged in daily metrics"
  artifacts:
    - path: "references/processing-instructions.md"
      provides: "Source auto-demotion/recovery procedure"
      contains: "auto-demotion"
    - path: "references/collection-instructions.md"
      provides: "Degraded source handling during collection"
      contains: "degraded"
    - path: "references/data-models.md"
      provides: "Source schema with degraded_since and recovery_streak_start fields"
      contains: "degraded_since"
    - path: "SKILL.md"
      provides: "Processing Phase includes source status check step"
      contains: "source.*status"
  key_links:
    - from: "references/processing-instructions.md"
      to: "config/sources.json"
      via: "Auto-demotion reads quality_score and writes status field"
      pattern: "sources\\.json"
    - from: "references/collection-instructions.md"
      to: "config/sources.json"
      via: "Collection skips degraded sources when budget is tight"
      pattern: "degraded"
    - from: "SKILL.md"
      to: "references/processing-instructions.md"
      via: "Processing Phase step triggers source status check after stats computation"
      pattern: "source.*status|demotion"
---

<objective>
Implement source auto-demotion and auto-recovery based on quality score thresholds.

Purpose: Currently source quality_score is computed but has no automatic consequence -- a source that consistently produces low-quality content (high dedup rate, low selection rate) continues to be collected at full priority forever. This plan adds a state machine where sources with quality_score < 0.2 for 14 days are auto-demoted to "degraded" status, and degraded sources with quality_score > 0.3 for 7 days are auto-recovered. Degraded sources are still collected but deprioritized, giving them a path back while reducing their impact on digest quality.

Output: Auto-demotion procedure in processing-instructions.md, degraded source handling in collection-instructions.md, updated Source schema in data-models.md, SKILL.md wiring.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-closed-loop/03-RESEARCH.md

@references/processing-instructions.md
@references/collection-instructions.md
@references/data-models.md
@references/scoring-formula.md
@config/sources.json
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source auto-demotion/recovery procedure and update Source schema</name>
  <files>references/processing-instructions.md, references/data-models.md</files>
  <action>
1. In `references/processing-instructions.md`, add a new section after the existing "Section 5: Metrics Collection" section. Title: `## Section 6: Source Auto-Demotion and Recovery (SRC-09)`.

   Content:

   ### When to Run
   After source health stats computation (SKILL.md Processing Phase step 12 "Compute source stats"), check all sources for status transitions.

   ### Demotion Check (active -> degraded)

   For each source in sources.json where `status == "active"`:

   1. If `stats.quality_score < 0.2`:
      a. If `stats.degraded_since` is null: set `stats.degraded_since = today` (ISO8601 date). This starts the countdown.
      b. If `stats.degraded_since` is NOT null AND `(today - stats.degraded_since) >= 14 days`: set `status = "degraded"`. Log alert: `"Source {name} auto-demoted: quality_score < 0.2 for 14 consecutive days"`.
   2. If `stats.quality_score >= 0.2`: reset `stats.degraded_since = null`. Quality recovered before the 14-day trigger -- reset counter. This provides hysteresis against brief dips.

   ### Recovery Check (degraded -> active)

   For each source in sources.json where `status == "degraded"`:

   1. If `stats.quality_score > 0.3`:
      a. If `stats.recovery_streak_start` is null: set `stats.recovery_streak_start = today` (ISO8601 date). This starts the recovery countdown.
      b. If `stats.recovery_streak_start` is NOT null AND `(today - stats.recovery_streak_start) >= 7 days`: set `status = "active"`, reset `stats.degraded_since = null`, reset `stats.recovery_streak_start = null`. Log: `"Source {name} auto-recovered: quality_score > 0.3 for 7 consecutive days"`.
   2. If `stats.quality_score <= 0.3`: reset `stats.recovery_streak_start = null`. Quality dipped again -- reset recovery counter.

   ### Effect on Scoring

   Degraded sources are NOT removed from collection. They are still fetched each run. However, items from degraded sources receive a scoring penalty:
   - During scoring (Section 4), if `item.source.status == "degraded"`: apply a 0.5x multiplier to the `source_trust` dimension value before the weighted sum. This deprioritizes degraded source items without completely excluding them.
   - If budget is tight (effective_usage >= 0.8 per Section 0A circuit-breaker), skip degraded sources in collection to save budget for healthy sources.

   ### Write-Back
   Atomic write `config/sources.json` after all status checks. Use backup-before-write pattern.

2. In `references/data-models.md`, update the Source schema to add the two new tracking fields to `stats`:

   Add to the Source stats object:
   ```json
   "stats": {
     "total_fetched": 0,
     "last_fetch": "ISO8601 or null",
     "last_hit_count": 0,
     "avg_daily_items": 0,
     "consecutive_failures": 0,
     "last_error": "string or null",
     "quality_score": 0.5,
     "dedup_rate": 0.0,
     "selection_rate": 0.0,
     "degraded_since": null,
     "recovery_streak_start": null
   }
   ```

   Field notes for new fields:
   - `degraded_since`: ISO8601 date or null. Set when quality_score first drops below 0.2 while status is "active". Reset to null when quality recovers (>= 0.2) or when source transitions to degraded. Used for the 14-day demotion countdown.
   - `recovery_streak_start`: ISO8601 date or null. Set when quality_score first rises above 0.3 while status is "degraded". Reset to null when quality dips (<= 0.3) or when source recovers to active. Used for the 7-day recovery countdown.

   Defaults for missing fields (older schema): `degraded_since`: null, `recovery_streak_start`: null.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "auto-demotion" references/processing-instructions.md && grep -q "degraded_since" references/processing-instructions.md && grep -q "recovery_streak_start" references/processing-instructions.md && grep -q "degraded_since" references/data-models.md && grep -q "recovery_streak_start" references/data-models.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Source auto-demotion procedure documents 14-day demotion (quality < 0.2) and 7-day recovery (quality > 0.3) with hysteresis. Scoring penalty (0.5x source_trust) for degraded sources. Source schema includes degraded_since and recovery_streak_start tracking fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add degraded source collection rules and wire auto-demotion into SKILL.md</name>
  <files>references/collection-instructions.md, SKILL.md</files>
  <action>
1. In `references/collection-instructions.md`, add a new section after the existing "Source Health Metrics Computation" section. Title: `## Degraded Source Handling`.

   Content:

   ### Collection Behavior for Degraded Sources

   Sources with `status: "degraded"` are still fetched during collection, with two exceptions:

   1. **Budget-tight skip**: If budget effective_usage >= 0.8 (circuit-breaker warning threshold), skip degraded sources entirely during collection. Prioritize healthy sources for remaining budget. Log: "Budget tight ({pct}%): skipping degraded source {name}".
   2. **All other runs**: Collect degraded sources normally. Their items receive a scoring penalty (0.5x source_trust dimension) but are otherwise processed through the full pipeline.

   ### Source Management Display

   When displaying source lists to the user (source management commands), include degraded status:
   - Active sources: normal display
   - Degraded sources: append " (degraded since {date})" to display
   - Paused sources: append " (paused)" to display

   ### Manual Override

   User can manually re-activate a degraded source via "activate {source_name}" or "restore {source_name}" command. This sets `status: "active"`, resets `degraded_since` and `recovery_streak_start` to null. Log: "Source {name} manually reactivated by user."

2. In `SKILL.md`, make these changes:

   a. **Processing Phase**: After the existing step 12 ("Compute source stats"), add a new step:
      `13. **Source status check**: Check source auto-demotion/recovery per {baseDir}/references/processing-instructions.md Section 6. Transition sources between active and degraded status based on quality_score thresholds.`

   b. **Collection Phase** step 3 ("Load sources"): Update to:
      `3. **Load sources**: Read {baseDir}/config/sources.json, filter enabled: true. If budget effective_usage >= 0.8, additionally skip status: "degraded" sources.`

   Keep within SKILL.md word budget. The source status step adds ~25 words. The collection update adds ~10 words.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "Degraded Source" references/collection-instructions.md && grep -q "budget.*tight\|Budget.*tight\|degraded.*skip\|skip.*degraded" references/collection-instructions.md && grep -qi "source.*status.*check\|demotion\|degraded" SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Collection instructions document degraded source handling: still collected except when budget tight, manual override supported, display includes degraded status. SKILL.md Processing Phase includes source status check step after stats computation. Collection Phase skips degraded sources when budget >= 80%.</done>
</task>

</tasks>

<verification>
NOTE: All verify commands assume CWD is the project root (skill base directory).

- Auto-demotion: quality_score < 0.2 for 14 days triggers degraded status
- Auto-recovery: quality_score > 0.3 for 7 days restores active status
- Hysteresis: degraded_since resets when quality recovers before 14 days
- Hysteresis: recovery_streak_start resets when quality dips during recovery
- Degraded sources still collected but deprioritized (0.5x source_trust)
- Budget-tight skip: degraded sources skipped when budget >= 80%
- Manual override: user can force-reactivate degraded sources
- Source schema has degraded_since and recovery_streak_start fields
- SKILL.md wired for both collection and processing phases
</verification>

<success_criteria>
- Source auto-demotion fully specified with 14-day threshold and quality_score < 0.2
- Source auto-recovery fully specified with 7-day threshold and quality_score > 0.3
- Degraded sources deprioritized in scoring, skipped when budget tight
- Source schema updated with tracking fields and migration defaults
- SKILL.md includes source status check in processing pipeline
- All files pass grep checks
</success_criteria>

<output>
After completion, create `.planning/phases/03-closed-loop/03-02-SUMMARY.md`
</output>

---
phase: 08-output-quality-foundation-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - references/processing-instructions.md
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "Pre-write quality contract validates UTF-8 sanitization before any JSONL write"
    - "Pre-write quality contract validates title correctness before any JSONL write"
    - "Pre-write quality contract validates URL format and ID consistency"
    - "Invalid items are skipped with a warning, never written as partial/corrupt data"
  artifacts:
    - path: "references/processing-instructions.md"
      provides: "Pre-write quality contract section"
      contains: "Pre-Write Quality Contract"
  key_links:
    - from: "references/processing-instructions.md Pre-Write Quality Contract"
      to: "SKILL.md Collection Phase step 7"
      via: "validation before JSONL write"
      pattern: "Pre-Write Quality Contract"
    - from: "references/processing-instructions.md Pre-Write Quality Contract"
      to: "SKILL.md Processing Phase step 7"
      via: "validation before results write"
      pattern: "Pre-Write Quality Contract"
---

<objective>
Add a pre-write quality contract to processing-instructions.md that validates data integrity before any JSONL write operation.

Purpose: Prevent corrupt, incomplete, or malformed data from entering the pipeline. UTF-8 issues, empty titles, and ID mismatches caught at write time rather than causing downstream failures during scoring/output.
Output: New section in processing-instructions.md defining the quality contract, with cross-references from Collection Phase step 7 and Processing Phase step 7.
</objective>

<execution_context>
@C:/Users/sorawatcher/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sorawatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-output-quality-foundation-infrastructure/08-RESEARCH.md

<interfaces>
<!-- processing-instructions.md Section structure: 0 (decay), 0A (circuit-breaker), 0B (cache), 0C (tiered model), 1 (batch LLM), 1A-1D (dedup/event), 2 (errors), 3 (resume), 4 (output), 5 (metrics), 6 (demotion), 7 (weekly), 8 (history) -->
<!-- SKILL.md Collection Phase step 7: "Write items: Append new items to data/news/YYYY-MM-DD.jsonl atomically" -->
<!-- SKILL.md Processing Phase step 7: "Write results: Update items in JSONL atomically, set processing_status: complete" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-write quality contract to processing-instructions.md</name>
  <files>references/processing-instructions.md</files>
  <read_first>
    - references/processing-instructions.md (full file - need to find right insertion point and understand existing section numbering)
    - .planning/phases/08-output-quality-foundation-infrastructure/08-RESEARCH.md (Pattern 4: Pre-Write Quality Contract with exact validation rules)
    - SKILL.md (Collection Phase step 7 and Processing Phase step 7 - the write points that reference this contract)
  </read_first>
  <action>
Add a new section `## Section 0D: Pre-Write Quality Contract (QUAL-01)` to processing-instructions.md, inserted between Section 0C (Tiered Model Strategy) and Section 1 (Batch LLM Processing).

The section content:

```markdown
## Section 0D: Pre-Write Quality Contract (QUAL-01)

Validation rules applied before ANY write to JSONL files. This contract applies at two points:
- **Collection Phase step 7**: Before appending new items to `data/news/YYYY-MM-DD.jsonl`
- **Processing Phase step 7**: Before writing updated items back to JSONL

### Validation Rules

For each item about to be written, apply ALL checks in order. If ANY check fails, skip the item entirely (do not write). Log a warning with the item URL and failure reason.

#### Rule 1: UTF-8 Sanitization

Strip the following characters from ALL string fields (title, content_summary, url, source_id):
- Null bytes: U+0000
- Control characters: U+0001 through U+001F, EXCEPT U+000A (newline) and U+0009 (tab)
- Lone surrogates: U+D800 through U+DFFF

Replace stripped characters with empty string (remove, do not substitute).

After stripping, if the resulting string is unchanged, proceed. If characters were stripped, use the cleaned version and log: `"UTF-8 sanitized: removed {N} invalid characters from item {url}"`

#### Rule 2: Title Validation

- `title` must be non-empty after trimming whitespace
- `title` must not exceed 500 characters
- If title is empty after trimming: **skip item entirely** (do not write). Log: `"Skipped item: empty title after trim ({url})"`
- If title exceeds 500 characters: **skip item entirely**. Log: `"Skipped item: title exceeds 500 chars ({length} chars) ({url})"`

#### Rule 3: URL Validation

- `normalized_url` must be non-empty
- `normalized_url` must start with `https://`
- If validation fails: **skip item entirely**. Log: `"Skipped item: invalid URL ({url})"`

#### Rule 4: ID Consistency

- Verify: `id == SHA256(normalized_url)[:16]`
- If mismatch: **skip item entirely**. Log: `"Skipped item: ID mismatch (expected {expected}, got {actual}) ({url})"`

### Failure Behavior

- Failed items are NOT written to JSONL
- Failed items do NOT count toward metrics (fetched, processed, etc.)
- Warnings are logged but do NOT stop the pipeline
- Other valid items in the same batch proceed normally

### Interaction with Existing Steps

- This contract runs AFTER URL normalization (Collection step 5) and AFTER dedup check (Collection step 6)
- This contract runs BEFORE the atomic write operation
- Items that fail quality checks in Collection Phase will never reach Processing Phase
- Items that fail quality checks in Processing Phase after LLM processing represent wasted LLM budget; the pre-collection check should catch most issues, but post-processing validation guards against LLM-introduced corruption
```

Also add a brief cross-reference note at the end of the existing SKILL.md Collection Phase step 7 and Processing Phase step 7 descriptions. However, since SKILL.md is tightly word-budgeted, instead add a single parenthetical to processing-instructions.md at the top of Section 0D: `"Referenced by SKILL.md Collection Phase step 7 and Processing Phase step 7."`

This avoids modifying SKILL.md (which Plan 02 already modifies) while maintaining the contract linkage through processing-instructions.md's own documentation.
  </action>
  <verify>
    <automated>cd C:/Users/sorawatcher/skills/openclaw-personal-news-digest && grep -c "Pre-Write Quality Contract" references/processing-instructions.md && grep -c "Section 0D" references/processing-instructions.md && grep -c "UTF-8 Sanitization" references/processing-instructions.md && grep -c "Title Validation" references/processing-instructions.md && grep -c "URL Validation" references/processing-instructions.md && grep -c "ID Consistency" references/processing-instructions.md && grep -c "QUAL-01" references/processing-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - references/processing-instructions.md contains `## Section 0D: Pre-Write Quality Contract (QUAL-01)` header
    - references/processing-instructions.md contains `#### Rule 1: UTF-8 Sanitization` with U+0000, U+0001-U+001F, U+D800-U+DFFF ranges specified
    - references/processing-instructions.md contains `#### Rule 2: Title Validation` with non-empty and 500-char max rules
    - references/processing-instructions.md contains `#### Rule 3: URL Validation` with https:// requirement
    - references/processing-instructions.md contains `#### Rule 4: ID Consistency` with SHA256 verification
    - Section 0D appears between Section 0C and Section 1 (after "Tiered Model Strategy", before "Batch LLM Processing")
    - Failure behavior section specifies items are NOT written and NOT counted in metrics
    - Cross-reference mentions Collection Phase step 7 and Processing Phase step 7
  </acceptance_criteria>
  <done>Pre-write quality contract is defined with 4 validation rules (UTF-8, title, URL, ID). Contract applies at both JSONL write points. Invalid items are skipped with logged warnings.</done>
</task>

</tasks>

<verification>
1. Section 0D exists between 0C and 1 in processing-instructions.md
2. All 4 validation rules are specified with exact character ranges and thresholds
3. Failure behavior is clearly defined (skip + log, do not stop pipeline)
4. Cross-references to SKILL.md write points are documented
</verification>

<success_criteria>
- Quality contract is a single authoritative section in processing-instructions.md
- All validation rules have specific, unambiguous thresholds (500 chars, https://, SHA256[:16])
- Edge case fixture file (data/fixtures/news-items-edge-cases.jsonl from Plan 02) exercises all 4 rules
</success_criteria>

<output>
After completion, create `.planning/phases/08-output-quality-foundation-infrastructure/08-03-SUMMARY.md`
</output>

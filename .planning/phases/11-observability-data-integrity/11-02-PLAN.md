---
phase: 11-observability-data-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - references/data-models.md
  - references/processing-instructions.md
  - SKILL.md
  - data/fixtures/metrics-sample.json
autonomous: true
requirements: [OBS-02]

must_haves:
  truths:
    - "DailyMetrics schema includes a run_log array field"
    - "Pipeline instructions specify which steps emit run_log entries"
    - "SKILL.md Collection/Processing/Output phases have log-emit points"
    - "Fixture file includes a sample run_log array"
  artifacts:
    - path: "references/data-models.md"
      provides: "run_log field definition in DailyMetrics schema"
      contains: "run_log"
    - path: "references/processing-instructions.md"
      provides: "Run log accumulation instructions at pipeline phases"
      contains: "run_log"
    - path: "SKILL.md"
      provides: "Log-emit instructions at phase boundaries"
      contains: "run_log"
    - path: "data/fixtures/metrics-sample.json"
      provides: "Sample run_log array in fixture"
      contains: "run_log"
  key_links:
    - from: "SKILL.md"
      to: "references/processing-instructions.md"
      via: "SKILL.md phase steps reference run_log accumulation from processing-instructions"
      pattern: "run_log"
    - from: "references/processing-instructions.md"
      to: "references/data-models.md"
      via: "run_log entries follow schema defined in data-models"
      pattern: "run_log"
---

<objective>
Add structured run logging to the pipeline so each daily metrics file captures timestamped milestones (pipeline_start through pipeline_end) with relevant counters at each step.

Purpose: Operators can diagnose slow runs, identify which pipeline phase is taking time, and verify all phases completed.
Output: Updated DailyMetrics schema, pipeline instrumentation instructions, SKILL.md log-emit points, updated fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@references/data-models.md
@references/processing-instructions.md
@SKILL.md
@data/fixtures/metrics-sample.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run_log schema to DailyMetrics and update fixture</name>
  <files>references/data-models.md, data/fixtures/metrics-sample.json</files>
  <read_first>references/data-models.md, data/fixtures/metrics-sample.json</read_first>
  <action>
1. In `references/data-models.md`, locate the DailyMetrics schema (the JSON block showing the full DailyMetrics structure). Add `run_log` as a new top-level field in the JSON schema, after `alerted_urls` and before the closing `}`:

```json
  "run_log": [
    {
      "step": "string (pipeline milestone identifier)",
      "timestamp": "ISO8601 (UTC)",
      "details": {}
    }
  ]
```

2. After the DailyMetrics JSON schema block, in the field notes section, add a new subsection:

```markdown
**Field notes (run_log):**
- `run_log`: Array of timestamped pipeline milestone entries accumulated during the run. Written as part of DailyMetrics at Output Phase step 7.
- Each entry has three fields:
  - `step` (string): Milestone identifier. One of: `pipeline_start`, `collection_complete`, `noise_filter_complete`, `classification_complete`, `summarization_complete`, `dedup_complete`, `output_complete`, `pipeline_end`.
  - `timestamp` (string): ISO8601 UTC timestamp when this milestone was reached.
  - `details` (object): Step-specific counters. Contents vary by step (see processing-instructions.md Section 5C for per-step detail schemas).
- Target 8 entries per run. Log phase transitions and aggregate counts, NOT individual item processing.
- All timestamps MUST be ISO8601 UTC (e.g., `2026-04-02T08:00:00Z`). Do NOT use local time.
- If pipeline aborts mid-run, `run_log` will contain entries up to the last completed step (no `pipeline_end` entry).

**Defaults for missing fields (backward compatibility):**
- `run_log`: `[]` (metrics files from before Phase 11 lack this field; consumers MUST use `.get('run_log', [])`)
```

3. In the "New Fields Registry" table at the bottom of data-models.md, add a new row:

```
| `run_log` | DailyMetrics | Phase 11 | - | `[]` | Timestamped pipeline milestone entries |
```

4. In `data/fixtures/metrics-sample.json`, add the `run_log` field to the existing fixture object (after `alerted_urls`):

```json
  "run_log": [
    {
      "step": "pipeline_start",
      "timestamp": "2026-01-01T00:00:00Z",
      "details": { "run_id": "run-20260101-080000-fix1" }
    },
    {
      "step": "collection_complete",
      "timestamp": "2026-01-01T00:01:30Z",
      "details": { "sources_attempted": 3, "items_fetched": 25, "failed_sources": [] }
    },
    {
      "step": "noise_filter_complete",
      "timestamp": "2026-01-01T00:01:45Z",
      "details": { "pre_classify_filtered": 3, "batch_remaining": 17 }
    },
    {
      "step": "classification_complete",
      "timestamp": "2026-01-01T00:02:30Z",
      "details": { "classified": 18, "partial": 0, "post_classify_filtered": 0 }
    },
    {
      "step": "summarization_complete",
      "timestamp": "2026-01-01T00:03:15Z",
      "details": { "summarized": 18 }
    },
    {
      "step": "dedup_complete",
      "timestamp": "2026-01-01T00:03:30Z",
      "details": { "title_deduped": 2, "events_merged": 0, "events_created": 0 }
    },
    {
      "step": "output_complete",
      "timestamp": "2026-01-01T00:04:00Z",
      "details": { "selected": 15, "repeat_suppressed": 2 }
    },
    {
      "step": "pipeline_end",
      "timestamp": "2026-01-01T00:04:15Z",
      "details": { "duration_seconds": 255 }
    }
  ]
```

Ensure the fixture remains valid JSON after adding (check trailing commas).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('data/fixtures/metrics-sample.json')); assert 'run_log' in d; assert len(d['run_log']) == 8; assert d['run_log'][0]['step'] == 'pipeline_start'; print('PASS')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - references/data-models.md DailyMetrics JSON schema contains `"run_log"` field
    - references/data-models.md contains `**Field notes (run_log):**` section
    - references/data-models.md contains `pipeline_start`, `collection_complete`, `noise_filter_complete`, `classification_complete`, `summarization_complete`, `dedup_complete`, `output_complete`, `pipeline_end` as step identifiers
    - references/data-models.md contains `ISO8601 UTC` timestamp requirement for run_log
    - references/data-models.md New Fields Registry contains row with `run_log` | `DailyMetrics` | `Phase 11`
    - data/fixtures/metrics-sample.json is valid JSON
    - data/fixtures/metrics-sample.json contains `run_log` array with 8 entries
    - data/fixtures/metrics-sample.json run_log[0].step == "pipeline_start"
    - data/fixtures/metrics-sample.json run_log[7].step == "pipeline_end"
  </acceptance_criteria>
  <done>DailyMetrics schema documents run_log field with 8 milestone steps, fixture includes full sample run_log array</done>
</task>

<task type="auto">
  <name>Task 2: Add run_log accumulation instructions and SKILL.md emit points</name>
  <files>references/processing-instructions.md, SKILL.md</files>
  <read_first>references/processing-instructions.md, SKILL.md, references/data-models.md</read_first>
  <action>
1. In `references/processing-instructions.md`, after Section 5 "Metrics Collection for Transparency" (after the "Per-Source Metrics Accumulation" and "Failed Source Name Tracking" subsections, or wherever the end of Section 5 content is), add a new subsection:

```markdown
### Section 5C: Run Log Accumulation (OBS-02)

Accumulate `run_log` entries in memory during the pipeline run. Each entry is appended when its milestone is reached. Write the full array as part of DailyMetrics at Output Phase step 7.

**Emit points and detail schemas:**

| Step | Emit After | Details Schema |
|------|-----------|----------------|
| `pipeline_start` | Lock acquired (Collection Phase step 1) | `{ "run_id": "{run_id}" }` |
| `collection_complete` | All sources fetched and deduped (Collection Phase step 8) | `{ "sources_attempted": int, "items_fetched": int, "failed_sources": [string] }` |
| `noise_filter_complete` | Pre-classify noise filter done (Processing Phase step 2.5) | `{ "pre_classify_filtered": int, "batch_remaining": int }` |
| `classification_complete` | All items classified (Processing Phase step 3.5) | `{ "classified": int, "partial": int, "post_classify_filtered": int }` |
| `summarization_complete` | All items summarized (Processing Phase step 4) | `{ "summarized": int }` |
| `dedup_complete` | Title dedup + event merge done (Processing Phase step 10) | `{ "title_deduped": int, "events_merged": int, "events_created": int }` |
| `output_complete` | Digest written (Output Phase step 6) | `{ "selected": int, "repeat_suppressed": int }` |
| `pipeline_end` | Metrics written, before lock release (Output Phase step 9) | `{ "duration_seconds": int }` |

**Rules:**
- All timestamps MUST be ISO8601 UTC.
- `duration_seconds` is computed as difference between `pipeline_end` and `pipeline_start` timestamps in seconds.
- `failed_sources` in `collection_complete` is the array of source_ids where `per_source[id].status == "failed"`.
- If pipeline aborts (e.g., lock contention, zero items), write partial `run_log` with whatever entries have been accumulated so far. The absence of `pipeline_end` indicates an incomplete run.
- Do NOT log individual item processing -- only phase-level transitions with aggregate counts.
```

2. In `SKILL.md`, add run_log emit instructions at phase boundaries. Make these minimal additions to existing steps:

a. In Collection Phase, after step 1 (Acquire lock), add inline note:
   Find the line starting with `1. **Acquire lock**:` and append to the end of that step: `Emit run_log entry: step="pipeline_start", details={run_id}.`

b. In Collection Phase, after step 8 (Update dedup index), add inline note:
   Find the line starting with `8. **Update dedup index**:` and append to the end of that step: `Emit run_log entry: step="collection_complete", details={sources_attempted, items_fetched, failed_sources}.`

c. In Processing Phase, after step 2.5 (Pre-classify noise filter), append:
   `Emit run_log entry: step="noise_filter_complete".`

d. In Processing Phase, after step 3.5 (Post-classify importance filter), append:
   `Emit run_log entry: step="classification_complete".`

e. In Processing Phase, after step 4 (Summarize batch), append:
   `Emit run_log entry: step="summarization_complete".`

f. In Processing Phase, after step 10 (Event merge), append:
   `Emit run_log entry: step="dedup_complete".`

g. In Output Phase, after step 6 (Write output), append:
   `Emit run_log entry: step="output_complete".`

h. In Output Phase, step 7 (Write metrics), update to mention run_log:
   After the existing text about writing metrics, append: `Include accumulated `run_log` array in daily metrics.`

i. In Output Phase, after step 9 (Release lock), but before the "Release lock" text itself, or as an addition to step 9:
   Append: `Emit run_log entry: step="pipeline_end", details={duration_seconds}. (This entry is written to the already-persisted metrics file via atomic update before lock release. See `{baseDir}/references/processing-instructions.md` Section 5C.)`

Note: Keep each addition as a single sentence appended to the existing step text. Do NOT restructure existing steps.
  </action>
  <verify>
    <automated>grep -c "run_log" SKILL.md | xargs -I{} test {} -ge 3 && grep -c "Section 5C" references/processing-instructions.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - references/processing-instructions.md contains `### Section 5C: Run Log Accumulation (OBS-02)` heading
    - references/processing-instructions.md contains the 8-row emit points table with step names and detail schemas
    - references/processing-instructions.md contains `ISO8601 UTC` timestamp rule
    - references/processing-instructions.md contains `duration_seconds` computation rule
    - SKILL.md Collection Phase step 1 contains `run_log` or `pipeline_start`
    - SKILL.md Collection Phase step 8 contains `collection_complete`
    - SKILL.md Processing Phase step 2.5 contains `noise_filter_complete`
    - SKILL.md Processing Phase step 3.5 contains `classification_complete`
    - SKILL.md Output Phase step 7 contains `run_log`
    - SKILL.md contains at least 3 references to `run_log`
  </acceptance_criteria>
  <done>Processing instructions document full run_log accumulation procedure; SKILL.md has log-emit instructions at all 8 pipeline milestones</done>
</task>

</tasks>

<verification>
- `grep "run_log" references/data-models.md` returns multiple matches (schema, field notes, registry)
- `grep "Section 5C" references/processing-instructions.md` returns the subsection heading
- `grep -c "run_log" SKILL.md` returns >= 3 (multiple phase boundary emit points)
- `python3 -c "import json; json.load(open('data/fixtures/metrics-sample.json'))"` succeeds (valid JSON)
- `python3 -c "import json; d=json.load(open('data/fixtures/metrics-sample.json')); assert 'run_log' in d"` succeeds
</verification>

<success_criteria>
- DailyMetrics schema documents run_log with 8 defined step types and detail schemas
- Processing instructions specify exact emit points mapped to pipeline steps
- SKILL.md has inline log-emit instructions at Collection/Processing/Output phase boundaries
- Fixture file includes realistic run_log sample with all 8 entries
- New Fields Registry updated with run_log entry
</success_criteria>

<output>
After completion, create `.planning/phases/11-observability-data-integrity/11-02-SUMMARY.md`
</output>

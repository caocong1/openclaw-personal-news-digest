---
phase: 11-observability-data-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/output-templates.md
  - references/processing-instructions.md
  - SKILL.md
autonomous: true
requirements: [OBS-01]

must_haves:
  truths:
    - "Transparency footer shows only enabled source count, not total attempted"
    - "When a source fails, its display name appears in a dedicated footer line"
    - "When no sources fail, the failed source footer line is omitted"
  artifacts:
    - path: "references/output-templates.md"
      provides: "Failed source conditional footer line template"
      contains: "采集失败"
    - path: "references/processing-instructions.md"
      provides: "Failed source name tracking instruction in Section 5"
      contains: "failed_source_names"
    - path: "SKILL.md"
      provides: "Output Phase step 8 references failed source rendering"
      contains: "failed source"
  key_links:
    - from: "references/processing-instructions.md"
      to: "references/output-templates.md"
      via: "failed_source_names variable populated from per_source status"
      pattern: "per_source.*status.*failed"
    - from: "SKILL.md"
      to: "references/output-templates.md"
      via: "Output Phase step 8 references footer rendering"
      pattern: "Transparency Footer"
---

<objective>
Make the transparency footer accurately reflect source health: source_count shows only enabled sources, and a conditional line names any sources that failed during collection.

Purpose: Operators and users can see at a glance whether all sources succeeded or which ones had problems, without digging into metrics JSON.
Output: Updated footer template, updated metrics tracking instruction, updated SKILL.md Output Phase reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@references/output-templates.md
@references/processing-instructions.md
@SKILL.md
@references/data-models.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failed source footer template and tracking instruction</name>
  <files>references/output-templates.md, references/processing-instructions.md</files>
  <read_first>references/output-templates.md, references/processing-instructions.md, references/data-models.md</read_first>
  <action>
1. In `references/output-templates.md`, locate the Transparency Footer section (around line 143-167). After the existing conditional for `repeat_suppressed_count > 0` block and before the `Where:` field definitions, add a new conditional block:

```
If any sources had `status: "failed"` in today's per_source metrics, append on a new line:
采集失败: {failed_source_name_1}, {failed_source_name_2} (共 {failed_count} 个)

Where:
- `failed_source_names`: Read from DailyMetrics `per_source` entries where `status == "failed"`. Look up display name from `config/sources.json` `name` field (not `source_id`).
- `failed_count`: Number of sources with `status: "failed"` in `per_source`.
- If no sources failed, omit this line entirely (do not show "采集失败: (共 0 个)").
```

2. In `references/processing-instructions.md`, locate Section 5 "Metrics Collection for Transparency" (around line 859). Add a new subsection after the "Per-Source Metrics Accumulation" subsection titled "### Failed Source Name Tracking":

```markdown
### Failed Source Name Tracking

During Output Phase step 8 (transparency footer rendering), derive the list of failed source display names:

1. Read today's `data/metrics/daily-YYYY-MM-DD.json` field `per_source`
2. For each entry where `status == "failed"`, collect the source_id
3. For each failed source_id, look up `name` from `config/sources.json` (use source_id to find the matching source object)
4. Pass the list of display names (`failed_source_names`) and count (`failed_count`) to the footer template
5. If `failed_source_names` is empty, skip the failed source footer line

This uses already-tracked data from `per_source` (Phase 6) -- no new collection logic needed.
```
  </action>
  <verify>
    <automated>grep -c "采集失败" references/output-templates.md | grep -q "1" && grep -c "failed_source_names" references/processing-instructions.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - references/output-templates.md contains the line `采集失败: {failed_source_name_1}, {failed_source_name_2} (共 {failed_count} 个)`
    - references/output-templates.md contains `If any sources had` conditional before the failed source line
    - references/output-templates.md contains `If no sources failed, omit this line entirely`
    - references/processing-instructions.md contains `### Failed Source Name Tracking` heading
    - references/processing-instructions.md contains `failed_source_names`
    - references/processing-instructions.md contains `config/sources.json` in the failed source tracking subsection
    - references/processing-instructions.md contains `per_source` reference for reading failed status
  </acceptance_criteria>
  <done>Footer template has conditional failed source line; processing-instructions has derivation procedure for failed source names from existing per_source data</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md Output Phase step 8 to reference failed source footer</name>
  <files>SKILL.md</files>
  <read_first>SKILL.md, references/output-templates.md</read_first>
  <action>
In `SKILL.md`, locate Output Phase step 8 (currently reads: "**Append transparency footer**: Read stats from `data/metrics/daily-YYYY-MM-DD.json`, format per `{baseDir}/references/output-templates.md` "Transparency Footer" section. Include repeat_suppressed_count if > 0 (suppression footer line). Append to digest output.").

Replace with:

```
8. **Append transparency footer**: Read stats from `data/metrics/daily-YYYY-MM-DD.json`, format per `{baseDir}/references/output-templates.md` "Transparency Footer" section. Include repeat_suppressed_count if > 0 (suppression footer line). If any `per_source` entries have `status: "failed"`, derive failed source display names from `config/sources.json` and include failed source footer line. Append to digest output.
```

The key addition is the sentence: `If any per_source entries have status: "failed", derive failed source display names from config/sources.json and include failed source footer line.`
  </action>
  <verify>
    <automated>grep -c "failed source" SKILL.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md Output Phase step 8 contains `per_source` reference
    - SKILL.md Output Phase step 8 contains `failed source display names`
    - SKILL.md Output Phase step 8 contains `config/sources.json`
    - SKILL.md Output Phase step 8 still contains `repeat_suppressed_count` (existing content preserved)
    - SKILL.md Output Phase step 8 still contains `references/output-templates.md` reference
  </acceptance_criteria>
  <done>SKILL.md Output Phase step 8 instructs the pipeline to check per_source for failures and render failed source names in footer</done>
</task>

</tasks>

<verification>
- `grep "采集失败" references/output-templates.md` returns the Chinese failed-source footer template line
- `grep "Failed Source Name Tracking" references/processing-instructions.md` returns the subsection heading
- `grep "failed source" SKILL.md` returns the Output Phase step 8 instruction
- No references to `sources.total` for footer source_count (it should use enabled count from sources.json)
</verification>

<success_criteria>
- Transparency footer template includes conditional failed source line with Chinese label
- Processing instructions document how to derive failed source names from existing per_source data
- SKILL.md Output Phase step 8 references the failed source footer rendering
- source_count definition remains "enabled sources in sources.json" (already correct, verified not changed)
</success_criteria>

<output>
After completion, create `.planning/phases/11-observability-data-integrity/11-01-SUMMARY.md`
</output>

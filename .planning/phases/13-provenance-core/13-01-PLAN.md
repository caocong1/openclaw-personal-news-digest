---
phase: 13-provenance-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/t1-sources.json
  - config/t2-sources.json
  - references/data-models.md
  - SKILL.md
autonomous: true
requirements: [PROV-01, PROV-02, DISC-05, PROV-06]

must_haves:
  truths:
    - "T1 and T2 provenance libraries live in dedicated config files, not inside config/sources.json"
    - "The repo documents a first-class Provenance phase between Collection and Processing"
    - "Authoritative provenance persistence is keyed by NewsItem.id and stored under data/provenance/"
  artifacts:
    - path: "config/t1-sources.json"
      provides: "T1 official/direct-source pattern library"
      contains: "\"ai_models\""
    - path: "config/t2-sources.json"
      provides: "T2 original-report pattern library"
      contains: "\"general_tech\""
    - path: "SKILL.md"
      provides: "Pipeline bootstrap and phase order including provenance"
      contains: "## Provenance Phase"
    - path: "references/data-models.md"
      provides: "Schema contracts for provenance stores"
      contains: "## ProvenanceRecord"
  key_links:
    - from: "SKILL.md"
      to: "config/t1-sources.json"
      via: "Provenance phase reads dedicated T1/T2 rule libraries before LLM provenance classification"
      pattern: "config/t1-sources.json"
    - from: "references/data-models.md"
      to: "SKILL.md"
      via: "Provenance stage writes authoritative data/provenance stores documented by schema"
      pattern: "data/provenance/provenance-db.json"
---

<objective>
Create the provenance foundation for Phase 13 by adding dedicated T1/T2 rule libraries, bootstrapping a documented Provenance phase in the skill pipeline, and defining authoritative provenance persistence contracts under `data/provenance/`.

Purpose: establish repo-owned configuration and schema contracts so later provenance inference and source-discovery work operates on stable inputs and stable stores.
Output: `config/t1-sources.json`, `config/t2-sources.json`, updated `SKILL.md`, and updated `references/data-models.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-provenance-core/13-RESEARCH.md
@.planning/phases/13-provenance-core/13-VALIDATION.md
@SKILL.md
@references/data-models.md
@references/collection-instructions.md
@references/processing-instructions.md
@config/sources.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated T1/T2 provenance rule libraries with concrete starter patterns</name>
  <files>config/t1-sources.json, config/t2-sources.json</files>
  <read_first>config/sources.json, .planning/phases/13-provenance-core/13-RESEARCH.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>
1. Create `config/t1-sources.json` with `_schema_v: 1` and a top-level `categories` object. Add exactly these T1 categories and starter patterns:
   - `ai_models` with `description: "AI model releases and official announcements"`, `applies_to_subdomains: true`, `priority: 1`, and patterns:
     - `openai.com/blog`
     - `anthropic.com/blog`
     - `blog.google`
     - `blogs.microsoft.com`
     - `ai.meta.com/blog`
     - `deepmind.com/blog`
     - `x.ai/blog`
     - `mistral.ai/news`
     - `ai.google.dev`
     - `stability.ai/news`
     - `bytedance.com/news`
     - `baidu.com/technology`
     - `tencent.com/technology`
     - `aliyun.com/ai`
     - `huawei.com/research`
   - `github_releases` with `priority: 1`, `wildcard_depth: 1`, and patterns:
     - `github.com/*/releases`
     - `github.com/*/releases/latest`
   - `arxiv_papers` with `priority: 1` and patterns:
     - `arxiv.org/abs`
     - `arxiv.org/pdf`
     - `arxiv.org/html`
   - `policy_regulation` with `priority: 2` and patterns:
     - `gov.cn`
     - `gov.*`
     - `sec.gov`
     - `ec.europa.eu`
     - `legislation.gov.uk`
     - `congress.gov`
     - `dpa.org.cn`
     - `cac.gov.cn`
     - `most.gov.cn`
     - `nbsc.gov.cn`
   - `pr_wire` with `priority: 2` and patterns:
     - `prnewswire.com`
     - `businesswire.com`
     - `globenewswire.com`
     - `prweb.com`
     - `business.51cto.com`
   - `scientific` with `priority: 2` and patterns:
     - `nature.com`
     - `science.org`
     - `cell.com`
     - `nejm.org`
     - `jamanetwork.com`
     - `lancet.com`
     - `pubmed.ncbi.nlm.nih.gov`
     - `dx.doi.org`
   - `exchange_filings` with `priority: 3` and patterns:
     - `sec.gov/filing`
     - `disclosure.nl`
     - `asx.com.au/regulatory`

2. Create `config/t2-sources.json` with `_schema_v: 1` and a top-level `categories` object. Add exactly these T2 categories and starter patterns:
   - `ai_tech_media` with `applies_to_subdomains: true` and patterns:
     - `machineintro.com`
     - `the-decoder.com`
     - `venturebeat.com/ai`
     - `papers.lab`
     - `mittrchina.com`
     - `jiqizhixin.com`
     - `leiphone.com`
     - `turingtopia.com`
   - `general_tech` with `applies_to_subdomains: true` and patterns:
     - `techcrunch.com`
     - `theverge.com`
     - `wired.com`
     - `arstechnica.com`
     - `engadget.com`
     - `sina.com.cn/tech`
     - `ifeng.com/tech`

3. Keep these files additive and provenance-specific:
   - do not move collection-source definitions out of `config/sources.json`
   - do not add `enabled`, `weight`, or fetch-specific fields to these new rule-library files
   - keep the matching inputs limited to provenance metadata: category, patterns, match-behavior flags, and priority where needed
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('config/t1-sources.json')); json.load(open('config/t2-sources.json')); print('PASS')" 2>&1 && rg -n "openai.com/blog|github.com/.*/releases|gov.cn|techcrunch.com|the-decoder.com" config/t1-sources.json config/t2-sources.json</automated>
  </verify>
  <acceptance_criteria>
    - `config/t1-sources.json` exists and is valid JSON
    - `config/t2-sources.json` exists and is valid JSON
    - `config/t1-sources.json` contains `openai.com/blog`, `github.com/*/releases`, and `gov.cn`
    - `config/t2-sources.json` contains `techcrunch.com`, `the-decoder.com`, and `jiqizhixin.com`
    - neither new file introduces fetch-management fields such as `enabled`, `weight`, or `stats`
    - both new files contain `_schema_v`
  </acceptance_criteria>
  <done>Dedicated provenance rule libraries exist with concrete T1 and T2 starter patterns and are cleanly separated from collection-source config.</done>
</task>

<task type="auto">
  <name>Task 2: Add provenance pipeline bootstrap and schema contracts for authoritative persistence</name>
  <files>references/data-models.md, SKILL.md</files>
  <read_first>SKILL.md, references/data-models.md, .planning/phases/13-provenance-core/13-RESEARCH.md, .planning/phases/13-provenance-core/13-VALIDATION.md</read_first>
  <action>
1. In `SKILL.md` Bootstrap step 0, add `{baseDir}/data/provenance/` to the required directories created during startup.

2. In `SKILL.md`, insert a new top-level section titled `## Provenance Phase` between `## Collection Phase` and `## Processing Phase`. Add these exact stage responsibilities:
   - collect today's items that do not yet have provenance records
   - load `config/t1-sources.json` and `config/t2-sources.json`
   - run URL-rule preclassification and produce `tier_guess`, `tier_confidence`, and `tier_source`
   - extract cited URLs and named upstream sources from `content_snippet`
   - batch unresolved or low-confidence items through `references/prompts/provenance-classify.md`
   - write `data/provenance/provenance-db.json`, `data/provenance/citation-graph.json`, `data/provenance/tier-stats.json`, and `data/provenance/provenance-discrepancies.jsonl`
   - continue into the existing Processing Phase only after provenance artifacts are updated

3. In `references/data-models.md`, add these new sections with concrete JSON contracts:
   - `## ProvenanceRecord`
   - `## CitationGraph`
   - `## TierStats`

4. In `## ProvenanceRecord`, document an authoritative record keyed by `NewsItem.id` with exactly these fields:
   - `id`
   - `tier`
   - `tier_confidence`
   - `tier_source`
   - `rule_result`
   - `llm_result`
   - `original_source_name`
   - `original_source_url`
   - `current_source_name`
   - `current_source_url`
   - `cited_sources`
   - `propagation_hops`
   - `provenance_chain`
   - `classified_at`
   - `_schema_v`

5. In `## CitationGraph`, document a JSON object with:
   - `_schema_v`
   - `nodes` keyed by normalized URL or canonical URL token
   - `edges` array with `from`, `to`, `relation`, and `ts`

6. In `## TierStats`, document this exact shape:
   - `_schema_v`
   - `days` keyed by `YYYY-MM-DD`
   - each day contains `total_items`, `tiers` with `T0` through `T4`, and `resolved_by` with `url_rule`, `llm`, and `resolved_disagreement`
   - `sources` keyed by `source_id`, each containing `total_items` and `tiers`
   - `last_updated`

7. Update `references/data-models.md` Schema Version Registry to add rows for `ProvenanceRecord`, `CitationGraph`, and `TierStats`, all at `v1`.

8. Do not add inline provenance fields to `NewsItem` in this plan. The authoritative join path for this phase is `NewsItem.id -> data/provenance/provenance-db.json`.
  </action>
  <verify>
    <automated>rg -n "## Provenance Phase|data/provenance|## ProvenanceRecord|## CitationGraph|## TierStats|ProvenanceRecord \\| v1|CitationGraph \\| v1|TierStats \\| v1" SKILL.md references/data-models.md</automated>
  </verify>
  <acceptance_criteria>
    - `SKILL.md` contains `## Provenance Phase`
    - `SKILL.md` bootstrap includes `data/provenance/`
    - `references/data-models.md` contains `## ProvenanceRecord`
    - `references/data-models.md` contains `## CitationGraph`
    - `references/data-models.md` contains `## TierStats`
    - `references/data-models.md` Schema Version Registry contains `ProvenanceRecord`, `CitationGraph`, and `TierStats` at `v1`
    - `references/data-models.md` documents `NewsItem.id` as the join key for authoritative provenance records
  </acceptance_criteria>
  <done>Provenance is a documented first-class pipeline stage, and the repo has authoritative schema contracts for provenance persistence keyed by item identity.</done>
</task>

</tasks>

<verification>
- `python3 -c "import json; json.load(open('config/t1-sources.json')); json.load(open('config/t2-sources.json'))"` succeeds
- `rg -n "## Provenance Phase|data/provenance" SKILL.md` returns matches
- `rg -n "## ProvenanceRecord|## CitationGraph|## TierStats" references/data-models.md` returns all three sections
- `rg -n "openai.com/blog|techcrunch.com|gov.cn" config/t1-sources.json config/t2-sources.json` returns the expected starter patterns
</verification>

<success_criteria>
- Dedicated T1/T2 provenance rule libraries exist under `config/`
- The skill pipeline documents provenance as a stage between Collection and Processing
- Authoritative provenance stores are defined under `data/provenance/`
- Phase 13 foundation stays bounded to provenance capture and does not auto-enable sources or change scoring/output behavior
</success_criteria>

---
phase: 13-provenance-core
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - references/processing-instructions.md
  - references/data-models.md
  - data/fixtures/provenance-db-sample.json
  - data/fixtures/citation-graph-sample.json
  - data/fixtures/provenance-discrepancies-sample.jsonl
  - data/fixtures/tier-stats-sample.json
autonomous: true
requirements: [PROV-05, PROV-06]

must_haves:
  truths:
    - "Rule-vs-LLM disagreement resolution is fixed and explicit, not case-by-case prose"
    - "Discrepancy logs preserve both candidate results and the final winner"
    - "The provenance stores and fixtures allow a reader to reconstruct a chain from original source to current item"
  artifacts:
    - path: "references/processing-instructions.md"
      provides: "Cross-validation precedence and provenance persistence rules"
      contains: "T1: URL-rule wins"
    - path: "references/data-models.md"
      provides: "Documented discrepancy and persistence schemas"
      contains: "provenance-discrepancies.jsonl"
    - path: "data/fixtures/provenance-db-sample.json"
      provides: "Authoritative provenance-record examples"
      contains: "\"provenance_chain\""
    - path: "data/fixtures/provenance-discrepancies-sample.jsonl"
      provides: "Rule-vs-LLM disagreement examples with final winner"
      contains: "\"final_winner\""
  key_links:
    - from: "references/processing-instructions.md"
      to: "data/fixtures/provenance-discrepancies-sample.jsonl"
      via: "Fixture proves the same disagreement-resolution fields described in the docs"
      pattern: "final_winner"
    - from: "data/fixtures/provenance-db-sample.json"
      to: "data/fixtures/citation-graph-sample.json"
      via: "Provenance chain nodes and cited-source edges agree on the same upstream URLs"
      pattern: "openai.com/blog/gpt-5-release"
---

<objective>
Finish Phase 13 by defining fixed cross-validation rules, authoritative persistence behavior, and concrete fixtures that prove provenance chains and discrepancy handling are reconstructable after a run.

Purpose: make the provenance core auditable and ready for downstream source-discovery work without ambiguity about which tier won or where the result was stored.
Output: updated persistence docs plus provenance store and discrepancy fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-provenance-core/13-RESEARCH.md
@.planning/phases/13-provenance-core/13-VALIDATION.md
@references/processing-instructions.md
@references/data-models.md
@data/fixtures/news-items-provenance-sample.jsonl
@references/prompts/provenance-classify.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode cross-validation precedence and provenance-store writeback contracts</name>
  <files>references/processing-instructions.md, references/data-models.md</files>
  <read_first>references/processing-instructions.md, references/data-models.md, .planning/phases/13-provenance-core/13-RESEARCH.md, data/fixtures/news-items-provenance-sample.jsonl</read_first>
  <action>
1. In `references/processing-instructions.md`, add a provenance subsection titled `### Cross-Validation` with these exact precedence rules:
   - `T1: URL-rule wins`
   - `T0: LLM wins`
   - `T2/T3/T4: LLM wins`

2. In the same subsection, define discrepancy logging for any case where both a rule result and an LLM result exist and they differ. Use this exact JSONL shape:
```json
{"ts":"ISO8601","item_id":"1234abcd5678ef90","url":"https://36kr.com/p/1234567890","rule_result":"T1","rule_category":"ai_models","llm_result":"T2","final_tier":"T1","final_winner":"url_rule"}
```

3. In `references/processing-instructions.md`, add a provenance persistence subsection that explicitly says the provenance stage must:
   - upsert one record per item into `data/provenance/provenance-db.json`
   - upsert citation nodes and edges into `data/provenance/citation-graph.json`
   - append discrepancy records to `data/provenance/provenance-discrepancies.jsonl`
   - update `data/provenance/tier-stats.json` day and source counters
   - write `provenance_chain` ordered from original source to current source

4. In `references/data-models.md`, document `data/provenance/provenance-discrepancies.jsonl` explicitly as an append-only JSONL store with the fields:
   - `ts`
   - `item_id`
   - `url`
   - `rule_result`
   - `rule_category`
   - `llm_result`
   - `final_tier`
   - `final_winner`

5. In `references/data-models.md`, ensure the `ProvenanceRecord` field notes state:
   - `tier_source` must be one of `url_rule`, `llm`, or `resolved_disagreement`
   - `rule_result` and `llm_result` are preserved when available
   - `provenance_chain` is ordered from upstream origin to current item URL
  </action>
  <verify>
    <automated>rg -n "T1: URL-rule wins|T0: LLM wins|T2/T3/T4: LLM wins|provenance-discrepancies.jsonl|final_winner|resolved_disagreement|provenance_chain" references/processing-instructions.md references/data-models.md</automated>
  </verify>
  <acceptance_criteria>
    - `references/processing-instructions.md` contains `T1: URL-rule wins`
    - `references/processing-instructions.md` contains `T0: LLM wins`
    - `references/processing-instructions.md` contains `T2/T3/T4: LLM wins`
    - `references/processing-instructions.md` explicitly names all four `data/provenance/*` stores written by the provenance stage
    - `references/data-models.md` documents `provenance-discrepancies.jsonl`
    - `references/data-models.md` documents `tier_source` values `url_rule`, `llm`, and `resolved_disagreement`
  </acceptance_criteria>
  <done>The repo now defines fixed disagreement resolution and authoritative writeback rules for all Phase 13 provenance stores.</done>
</task>

<task type="auto">
  <name>Task 2: Add provenance persistence fixtures that prove chain reconstruction and disagreement auditability</name>
  <files>data/fixtures/provenance-db-sample.json, data/fixtures/citation-graph-sample.json, data/fixtures/provenance-discrepancies-sample.jsonl, data/fixtures/tier-stats-sample.json</files>
  <read_first>data/fixtures/news-items-provenance-sample.jsonl, references/data-models.md, references/processing-instructions.md, .planning/phases/13-provenance-core/13-VALIDATION.md</read_first>
  <action>
1. Create `data/fixtures/provenance-db-sample.json` with three records keyed by these IDs:
   - `0f1e2d3c4b5a6978` for `https://openai.com/blog/gpt-5-release`
   - `1234abcd5678ef90` for `https://techcrunch.com/2026/04/02/openai-gpt-5-report`
   - `fedcba9876543210` for `https://36kr.com/p/1234567890`

2. In the OpenAI record:
   - set `tier: "T1"`
   - set `tier_confidence: 0.95`
   - set `tier_source: "url_rule"`
   - set `original_source_name` and `current_source_name` to `OpenAI Blog`
   - set `propagation_hops: 0`
   - set `provenance_chain` to a single-node chain at `https://openai.com/blog/gpt-5-release`

3. In the TechCrunch record:
   - set `tier: "T2"`
   - set `tier_source: "llm"`
   - set `original_source_name: "OpenAI Blog"`
   - set `original_source_url: "https://openai.com/blog/gpt-5-release"`
   - include `cited_sources` with the OpenAI Blog URL marked `T1`
   - set `propagation_hops: 1`
   - make `provenance_chain` show `OpenAI Blog -> TechCrunch`

4. In the 36Kr record:
   - set `tier: "T4"`
   - set `tier_source: "resolved_disagreement"`
   - set `rule_result` to a `T1` `ai_models` guess caused by a cited official domain
   - set `llm_result` to `T4`
   - set `original_source_url` to the OpenAI Blog URL
   - include `cited_sources` for both the TechCrunch report and the OpenAI Blog URL
   - set `propagation_hops: 2`
   - make `provenance_chain` show `OpenAI Blog -> TechCrunch -> 36Kr`

5. Create `data/fixtures/citation-graph-sample.json` with:
   - `_schema_v: 1`
   - nodes for the OpenAI Blog URL, the TechCrunch URL, and the 36Kr URL
   - edges:
     - `36Kr -> TechCrunch` with `relation: "references"`
     - `TechCrunch -> OpenAI Blog` with `relation: "cites"`

6. Create `data/fixtures/provenance-discrepancies-sample.jsonl` with at least one line matching the 36Kr disagreement case:
```json
{"ts":"2026-04-03T08:15:00Z","item_id":"fedcba9876543210","url":"https://36kr.com/p/1234567890","rule_result":"T1","rule_category":"ai_models","llm_result":"T4","final_tier":"T4","final_winner":"llm"}
```

7. Create `data/fixtures/tier-stats-sample.json` with:
   - `_schema_v: 1`
   - `days.2026-04-03.total_items: 3`
   - `days.2026-04-03.tiers` showing `T1: 1`, `T2: 1`, and `T4: 1`
   - `days.2026-04-03.resolved_by.url_rule: 1`
   - `days.2026-04-03.resolved_by.llm: 1`
   - `days.2026-04-03.resolved_by.resolved_disagreement: 1`
   - `sources.src-official-openai-blog`, `sources.src-techcrunch`, and `sources.src-36kr` entries with matching counts
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('data/fixtures/provenance-db-sample.json')); json.load(open('data/fixtures/citation-graph-sample.json')); json.load(open('data/fixtures/tier-stats-sample.json')); print('PASS')" 2>&1 && rg -n "final_winner|fedcba9876543210|openai.com/blog/gpt-5-release|techcrunch.com/2026/04/02/openai-gpt-5-report|36kr.com/p/1234567890" data/fixtures/provenance-db-sample.json data/fixtures/citation-graph-sample.json data/fixtures/provenance-discrepancies-sample.jsonl data/fixtures/tier-stats-sample.json</automated>
  </verify>
  <acceptance_criteria>
    - all four new provenance fixture files exist
    - `data/fixtures/provenance-db-sample.json` contains `provenance_chain`
    - `data/fixtures/citation-graph-sample.json` contains nodes for OpenAI Blog, TechCrunch, and 36Kr
    - `data/fixtures/provenance-discrepancies-sample.jsonl` contains `final_winner`
    - `data/fixtures/tier-stats-sample.json` records one `T1`, one `T2`, and one `T4` item for `2026-04-03`
    - the 36Kr sample can be traced from `news-items-provenance-sample.jsonl` to `provenance-db-sample.json` to `citation-graph-sample.json`
  </acceptance_criteria>
  <done>Concrete fixtures now prove provenance persistence, discrepancy handling, and full-chain reconstruction for direct, report, and aggregator cases.</done>
</task>

</tasks>

<verification>
- `rg -n "T1: URL-rule wins|T0: LLM wins|T2/T3/T4: LLM wins" references/processing-instructions.md` returns all precedence rules
- `rg -n "provenance-discrepancies.jsonl|resolved_disagreement|provenance_chain" references/data-models.md` returns matches
- `python3 -c "import json; json.load(open('data/fixtures/provenance-db-sample.json')); json.load(open('data/fixtures/citation-graph-sample.json')); json.load(open('data/fixtures/tier-stats-sample.json'))"` succeeds
- `rg -n "final_winner|fedcba9876543210" data/fixtures/provenance-discrepancies-sample.jsonl data/fixtures/provenance-db-sample.json` returns the disagreement and chain records
</verification>

<success_criteria>
- Cross-validation precedence is explicit and consistent across docs and fixtures
- All required `data/provenance/*` stores have documented writeback behavior
- Discrepancy logging preserves both candidate results and the final winner
- Fixtures prove the provenance chain for an indirect aggregator item can be reconstructed after the run
</success_criteria>

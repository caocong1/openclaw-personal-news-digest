---
phase: 07-readme-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "Operator can find architecture overview showing Collection -> Processing -> Output pipeline"
    - "Operator can follow deployment instructions to set up skill on OpenClaw platform"
    - "Operator can locate all 4 config files and understand their purpose and key fields"
    - "Operator can run all 3 operational scripts with correct arguments"
  artifacts:
    - path: "README.md"
      provides: "Complete project documentation"
      contains: "## Architecture"
      min_lines: 150
  key_links:
    - from: "README.md"
      to: "SKILL.md"
      via: "reference link"
      pattern: "SKILL\\.md"
    - from: "README.md"
      to: "references/cron-configs.md"
      via: "reference link"
      pattern: "cron-configs\\.md"
    - from: "README.md"
      to: "references/data-models.md"
      via: "reference link"
      pattern: "data-models\\.md"
---

<objective>
Create README.md at project root documenting architecture, deployment, configuration, and operational scripts for the OpenClaw news-digest skill.

Purpose: Any operator should be able to understand, deploy, configure, and operate the skill from this single document within 5 minutes.
Output: README.md at project root (~200-250 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-readme-documentation/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with full documentation</name>
  <files>README.md</files>
  <read_first>
    SKILL.md
    config/sources.json
    config/preferences.json
    config/budget.json
    config/categories.json
    scripts/health-check.sh
    scripts/data-archive.sh
    scripts/dedup-index-rebuild.sh
    references/cron-configs.md
    references/platform-verification.md
    .planning/phases/07-readme-documentation/07-RESEARCH.md
  </read_first>
  <action>
Create README.md at project root with these exact sections in order (per DOC-01):

**1. Title and What This Is**
- Title: `# OpenClaw News Digest Skill`
- Subtitle in Chinese: `OpenClaw 个性化新闻研究与推送 Skill`
- One paragraph in English explaining: runs on OpenClaw platform, multi-source collection (RSS, GitHub, search, official, community, ranking), LLM-driven classification/summarization/dedup, event tracking, 7-layer preference model, anti-echo-chamber quotas, feedback learning, cost control
- One paragraph in Chinese preserving the core value from PROJECT.md

**2. Architecture Overview (`## Architecture`)**
- ASCII diagram from RESEARCH.md showing:
  - config/ box (sources.json, preferences.json, budget.json, categories.json) at top
  - SKILL.md as central hub
  - Three pipeline phases: Collection -> Processing -> Output
  - Data stores: data/news/*.jsonl, data/events/, output/*.md
- Below diagram: bullet list of 6 flows:
  - Daily Digest: Collection -> Processing -> Scoring -> Output -> `output/latest-digest.md`
  - Quick-Check: Collection -> Processing -> breaking filter (importance >= 0.85) -> `output/latest-alert.md`
  - Weekly Report: 7-day aggregation -> `output/latest-weekly.md`
  - Feedback Loop: user feedback -> `data/feedback/log.jsonl` -> preference updates in `config/preferences.json`
  - Health Check: `scripts/health-check.sh` -> data consistency validation
  - Data Lifecycle: `scripts/data-archive.sh` -> TTL-based cleanup

**3. Directory Structure (`## Directory Structure`)**
- Annotated tree matching the exact structure from RESEARCH.md inventory (lines 34-73)
- Each entry has a brief inline comment

**4. Configuration Guide (`## Configuration`)**
- Table with 4 rows (one per config file):

| File | Purpose | Key Fields |
|------|---------|------------|
| `config/sources.json` | News source definitions (6 types) | `id`, `name`, `type`, `enabled`, `weight`, `credibility`, `topics`, `fetch_config` |
| `config/preferences.json` | User preference model (7 layers) | `topic_weights`, `source_trust`, `form_preference`, `style`, `depth_preference`, `judgment_angles` |
| `config/budget.json` | LLM cost control | `daily_llm_call_limit` (500), `daily_token_limit` (1M), `alert_threshold` (0.8) |
| `config/categories.json` | Topic taxonomy (12 categories) | Category names, scoring weights, sub-topics |

- Note: "See individual files for full schema. Data model schemas in [`references/data-models.md`](references/data-models.md)."

**5. Deployment (`## Deployment`)**
- Subsection `### Prerequisites` with bulleted list:
  - OpenClaw platform account with workspace
  - Telegram chat ID for delivery
- Subsection `### Setup Steps` numbered list:
  1. Clone repository
  2. Install skill in OpenClaw workspace (load SKILL.md)
  3. **CRITICAL** callout box: `> **CRITICAL:** Set `lightContext: false` in all cron job payloads. The skill requires full context to load reference documents. Without this, cron jobs will fire but produce no output.`
  4. Set `sessionTarget: "isolated"` for cron jobs
  5. Run platform verification per `references/platform-verification.md`
  6. Register cron jobs per `references/cron-configs.md`
- Subsection `### Platform Verification` with link to `references/platform-verification.md` and brief note about what it covers

**6. Cron Jobs (`## Cron Jobs`)**
- Table with 4 rows:

| Job | Schedule | Purpose |
|-----|----------|---------|
| `news-daily-digest` | 08:00 CST daily | Full pipeline: collect, process, score, generate digest |
| `news-quick-check` | Every 2 hours | Breaking news detection (importance >= 0.85) |
| `weekly-health-inspection` | Monday 03:00 CST | Health check + data archive |
| `news-weekly-report` | Sunday 20:00 CST | Weekly aggregation report |

- Note: "See [`references/cron-configs.md`](references/cron-configs.md) for full registration JSON."

**7. Operational Scripts (`## Operational Scripts`)**
- For each of the 3 scripts, include: purpose, usage example, key behavior
- Use the exact signatures from the script files:

```bash
# Daily health check
bash scripts/health-check.sh /path/to/skill

# Weekly full inspection
bash scripts/health-check.sh /path/to/skill --mode weekly

# Data cleanup (TTL: news 30d, cache 7d, metrics 30d, feedback 90d)
bash scripts/data-archive.sh /path/to/skill

# Rebuild dedup index after corruption
bash scripts/dedup-index-rebuild.sh /path/to/skill
```

**8. User Commands (`## User Commands`)**
- Brief paragraph: "The skill responds to natural language commands via the OpenClaw chat interface. See [`SKILL.md`](SKILL.md) for the full command reference."
- Bullet list of command categories: source management, feedback, history queries, preference adjustment

**9. Data Lifecycle (`## Data Lifecycle`)**
- Table of TTL rules:

| Data | Location | Retention |
|------|----------|-----------|
| News items | `data/news/*.jsonl` | 30 days |
| LLM cache | `data/cache/` | 7 days |
| Metrics | `data/metrics/` | 30 days |
| Feedback | `data/feedback/` | 90 days |
| Events | `data/events/` | Until archived |

**10. References (`## References`)**
- Bulleted list linking to all reference documents in `references/` directory with one-line descriptions

**Anti-patterns to avoid:**
- Do NOT duplicate SKILL.md content -- always link to it
- Do NOT inline data model schemas -- link to `references/data-models.md`
- Do NOT include `.planning/` directory in the directory tree (internal only)
  </action>
  <verify>
    <automated>bash -c "test -f README.md && echo 'EXISTS' || echo 'MISSING'; wc -l README.md 2>/dev/null; grep -c '## ' README.md 2>/dev/null; grep -l 'lightContext' README.md 2>/dev/null"</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at project root
    - README.md contains `# OpenClaw News Digest Skill`
    - README.md contains `## Architecture` with ASCII diagram (contains `+--` characters)
    - README.md contains `## Directory Structure`
    - README.md contains `## Configuration` with table containing `sources.json`, `preferences.json`, `budget.json`, `categories.json`
    - README.md contains `## Deployment` with `lightContext: false` in a CRITICAL callout
    - README.md contains `## Cron Jobs` with table containing `news-daily-digest`, `news-quick-check`
    - README.md contains `## Operational Scripts` with `health-check.sh`, `data-archive.sh`, `dedup-index-rebuild.sh`
    - README.md contains `## User Commands` with link to `SKILL.md`
    - README.md contains `## Data Lifecycle` with TTL table
    - README.md contains `## References` with links to `references/` documents
    - README.md is >= 150 lines
    - README.md does NOT contain `.planning/` in directory tree
    - README.md links to `references/data-models.md` (grep returns match)
    - README.md links to `references/cron-configs.md` (grep returns match)
  </acceptance_criteria>
  <done>README.md exists at project root with all 10 sections, architecture diagram, config table, deployment with CRITICAL callout, cron table, script examples, and reference links. File is >= 150 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Verify README accuracy against source files</name>
  <files>README.md</files>
  <read_first>
    README.md
    config/sources.json
    config/budget.json
    scripts/health-check.sh
    references/cron-configs.md
  </read_first>
  <action>
Cross-check the README.md created in Task 1 against actual project files to catch any drift:

1. **Config fields accuracy**: Read `config/sources.json` and verify the "Key Fields" listed in README's Configuration table match actual top-level keys. Read `config/budget.json` and verify `daily_llm_call_limit`, `daily_token_limit`, `alert_threshold` values match README.

2. **Script signatures**: Read the first 20 lines of each script in `scripts/` and verify the usage examples in README match actual argument patterns (especially `[base_dir]` vs positional args, and `--mode` flag).

3. **Cron schedule accuracy**: Read `references/cron-configs.md` and verify all 4 cron schedules in README match the actual cron expressions.

4. **Directory completeness**: Run `ls` on each top-level directory (config/, references/, scripts/, references/prompts/) and verify README's Directory Structure tree includes all files that actually exist.

5. **Fix any discrepancies** found in steps 1-4 by editing README.md.

6. **Link validation**: Verify all relative links in README resolve to actual files. Check each `](path)` link target exists.
  </action>
  <verify>
    <automated>bash -c "echo '=== Link check ==='; grep -oP '\\]\\(([^)]+)\\)' README.md | sed 's/](//;s/)//' | while read f; do test -e \"$f\" && echo \"OK: $f\" || echo \"BROKEN: $f\"; done; echo '=== Budget values ==='; grep -o 'daily_llm_call_limit.*500' README.md && echo 'budget OK' || echo 'budget MISMATCH'"</automated>
  </verify>
  <acceptance_criteria>
    - All relative links in README.md resolve to existing files (zero BROKEN links from link check)
    - Budget values in README match: `daily_llm_call_limit` is 500, `daily_token_limit` is 1M, `alert_threshold` is 0.8
    - Script usage examples use correct argument patterns matching actual script files
    - All files in config/, references/, scripts/ directories appear in README directory tree
    - Cron schedules match: `0 8 * * *`, `0 */2 * * *`, `0 3 * * 1`, `0 20 * * 0`
  </acceptance_criteria>
  <done>README.md content verified accurate against all source files. All links resolve, all values match, all files listed.</done>
</task>

</tasks>

<verification>
1. README.md exists at project root with >= 150 lines
2. Contains all 10 required sections (Architecture, Directory Structure, Configuration, Deployment, Cron Jobs, Operational Scripts, User Commands, Data Lifecycle, References)
3. Architecture ASCII diagram present
4. CRITICAL callout for lightContext: false in Deployment section
5. All relative links resolve to existing files
6. Config values match actual files
7. No .planning/ directory leaked into directory tree
</verification>

<success_criteria>
- README.md provides complete operator documentation per DOC-01
- Any operator can understand architecture, deploy, configure, and run operational tasks from README alone
- All factual claims (config values, cron schedules, script arguments) verified against source files
</success_criteria>

<output>
After completion, create `.planning/phases/07-readme-documentation/07-01-SUMMARY.md`
</output>

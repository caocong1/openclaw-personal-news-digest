---
phase: 15-provenance-aware-ranking-delivery
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - references/processing-instructions.md
  - references/output-templates.md
  - references/data-models.md
  - SKILL.md
autonomous: true
requirements: [PIPE-02, PIPE-04]

must_haves:
  truths:
    - "T4 items must clear 0.92 importance threshold for alerts (stricter than the standard 0.85)"
    - "An event that has already been alerted today is suppressed from additional alerts"
    - "Digest items display source tier with Chinese label (e.g., 直接来源, 聚合转载)"
    - "Original-source attribution renders only when original_source_name differs from current source"
    - "Provenance chain renders only when chain length > 1"
    - "Internal provenance fields (tier_source, tier_confidence, llm_result) never appear in output"
  artifacts:
    - path: "references/processing-instructions.md"
      provides: "Steps 0 and 0A in Section 5A alert decision tree"
      contains: "Event-level alert suppression"
    - path: "references/output-templates.md"
      provides: "Provenance rendering rules in digest and alert templates, tier display mapping, updated rendering contract"
      contains: "信源层级"
    - path: "references/data-models.md"
      provides: "New user-facing provenance fields documented"
      contains: "provenance_chain"
    - path: "SKILL.md"
      provides: "Output Phase rendering notes mention provenance display"
      contains: "provenance"
  key_links:
    - from: "references/processing-instructions.md Section 5A"
      to: "data/provenance/provenance-db.json"
      via: "ProvenanceRecord.tier lookup for threshold selection"
      pattern: "provenance_db\\[item\\.id\\]"
    - from: "references/processing-instructions.md Section 5A"
      to: "data/alerts/alert-state-{date}.json"
      via: "alert_log event_id count for suppression"
      pattern: "alert_log.*event_id"
    - from: "references/output-templates.md"
      to: "data/provenance/provenance-db.json"
      via: "tier, original_source_name, provenance_chain for rendering"
      pattern: "ProvenanceRecord\\.(tier|original_source_name|provenance_chain)"
---

<objective>
Add tier-aware alert gating and provenance-chain rendering to alerts and digests.

Purpose: T4 aggregated content should face a stricter alert threshold (0.92 vs 0.85) to reduce false alarms from aggregation, and events already alerted today should not trigger duplicate alerts. User-facing output should display source tier, original-source attribution, and provenance chain so readers understand information provenance.

Output: Modified alert decision tree with two new entry steps, updated digest and alert templates with provenance display, tier display mapping table, and updated rendering contract.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-provenance-aware-ranking-delivery/15-RESEARCH.md
@.planning/phases/15-provenance-aware-ranking-delivery/15-01-SUMMARY.md

@references/processing-instructions.md
@references/output-templates.md
@references/data-models.md
@SKILL.md

<interfaces>
<!-- Key contracts from Plan 15-01 that this plan depends on. -->

From references/scoring-formula.md (added by 15-01):
- Provenance modifier table: T0=1.15, T1=1.10, T2=1.05, T3=1.00, T4=0.75/1.00
- Join path: NewsItem.id -> provenance-db.json -> ProvenanceRecord.tier

From references/processing-instructions.md (added by 15-01):
- Section 4 step 4: adjusted_score = final_score * provenance_modifier
- Section 4R: Event representative selection with TIER_RANK map

From references/data-models.md (added by 15-01):
- Event._schema_v: 4 (with representative_item_id)

From references/processing-instructions.md Section 5A (current, to be modified):
```
1. Item has importance_score >= 0.85?
2. Item form_type is "news" or "announcement"?
3. Read alert-state file
4. alerts_sent >= max_alerts (3)?
5. Item URL already in alerted_urls?
6. Item has event_id AND event has last_alerted_at?
   YES -> DELTA ALERT path
   NO  -> STANDARD ALERT path (step 7)
```

From references/output-templates.md (current digest template):
```
来源: {source_name} | {form_type display label} | 重要性: {importance_score}
```

From references/output-templates.md Rendering Contract:
- User-facing fields table (to add provenance fields)
- Internal-only fields table (provenance diagnostic fields go here)

From references/data-models.md ProvenanceRecord fields:
- tier, tier_confidence, tier_source (internal diagnostics)
- original_source_url, original_source_name (attribution)
- provenance_chain (array of {name, tier, url} objects)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add event-level alert suppression and tier-aware threshold to the alert decision tree</name>
  <files>references/processing-instructions.md</files>
  <action>
**Modify Section 5A: Unified Alert Decision Tree** in `references/processing-instructions.md`. Insert two new steps BEFORE the existing Step 1 (importance threshold check). Renumber all subsequent steps.

**New Step 0: Event-Level Alert Suppression**
```
0. Event-level alert suppression (PIPE-02):
   If item has non-null event_id:
     Read data/alerts/alert-state-{today YYYY-MM-DD}.json
     Count entries in alert_log where event_id == item.event_id
     If count >= 1: skip (event already alerted today, even if this is a new item for the same event)
   Continue to Step 0A
```

Rationale note: "Event-level suppression prevents multiple alerts for the same event in one day. This is especially important when both T1 and T4 items exist for the same event -- the first alert (whichever tier arrives first) is sufficient."

**New Step 0A: Tier-Aware Threshold Adjustment**
```
0A. Tier-aware threshold adjustment (PIPE-02):
   Look up ProvenanceRecord for item via NewsItem.id -> data/provenance/provenance-db.json
   If record exists and record.tier == "T4":
     effective_threshold = 0.92
   Else:
     effective_threshold = 0.85 (standard threshold, unchanged for T0-T3 and items without provenance)
   
   Item has importance_score >= effective_threshold?
     NO  -> skip (below tier-adjusted threshold)
     YES -> continue to Step 1 (form_type check, previously Step 2)
```

Rationale note: "T4 aggregated content must clear a higher bar (0.92 vs 0.85) because aggregation is less likely to represent genuinely breaking news the user has not already seen from a direct source."

**Renumber existing steps:**
- Old Step 1 (importance >= 0.85) -> REMOVE (replaced by Step 0A with tier-aware thresholds)
- Old Step 2 (form_type check) -> becomes Step 1
- Old Step 3 (read alert-state) -> becomes Step 2
- Old Step 4 (daily cap) -> becomes Step 3
- Old Step 5 (URL dedup) -> becomes Step 4
- Old Step 6 (event_id delta routing) -> becomes Step 5
- Old Step 7 (standard alert) -> becomes Step 6
- Old Step 8 (update alert-state) -> becomes Step 7
- Old Step 9 (atomic write) -> becomes Step 8

Also update the Breaking News Alert section in `references/output-templates.md` trigger condition description (around line 95-97) to note: "Effective threshold is 0.85 for T0-T3 items and 0.92 for T4 items (see Section 5A Step 0A)."

Do NOT change the conservative threshold policy text -- just add a parenthetical noting the T4 adjustment.
  </action>
  <verify>
    <automated>grep -c "Step 0:" references/processing-instructions.md && grep -c "Step 0A:" references/processing-instructions.md && grep -c "effective_threshold" references/processing-instructions.md && grep -c "0.92" references/processing-instructions.md</automated>
  </verify>
  <done>
- Section 5A has Step 0 (event-level suppression) checking alert_log for event_id count
- Section 5A has Step 0A (tier-aware threshold) using 0.92 for T4 and 0.85 for others
- Old Step 1 (flat 0.85 check) is replaced by the tier-aware Step 0A
- Remaining steps renumbered correctly
- output-templates.md trigger condition updated with T4 threshold note
  </done>
</task>

<task type="auto">
  <name>Task 2: Add provenance rendering rules to output templates, tier display mapping, and updated rendering contract</name>
  <files>references/output-templates.md, references/data-models.md, SKILL.md</files>
  <action>
**In references/output-templates.md**, make the following changes:

1. **Add Tier Display Mapping** to the "## Display Mapping Tables" section, after the existing `relation` display mapping:

```markdown
### tier Display Mapping

| Internal Value | Chinese Display |
|----------------|-----------------|
| `T0` | 一手来源 |
| `T1` | 直接来源 |
| `T2` | 原创报道 |
| `T3` | 评论分析 |
| `T4` | 聚合转载 |
```

2. **Update the Daily Digest Template** Core Focus format. Change the current line:
```
来源: {source_name} | {form_type display label} | 重要性: {importance_score}
```
To:
```
来源: {source_name} | 信源层级: {tier_display} | {form_type display label} | 重要性: {importance_score}
{原始来源: {original_source_name} -- only render this line if original_source_name != source_name}
{溯源链: {chain_display} -- only render this line if provenance_chain.length > 1}
```

Update the compact format for Adjacent, Hotspot, and Explore sections. Change:
```
- **{title}** -- {1 sentence summary} ({source_name})
```
To:
```
- **{title}** -- {1 sentence summary} ({source_name} | {tier_display})
  {原始来源: {original_source_name} -- only if different from source_name}
```

3. **Add provenance chain display format** as a new subsection "### Provenance Chain Rendering" after the Display Mapping Tables section:

```markdown
### Provenance Chain Rendering

Format: `溯源链: {name_1} ({tier_display_1}) -> {name_2} ({tier_display_2}) -> ...`

Example: `溯源链: OpenAI Blog (直接来源) -> TechCrunch (原创报道) -> 36Kr (聚合转载)`

**Render conditions:**
- Only render the provenance chain line when `provenance_chain.length > 1`
- A single-hop chain (item is the original source) does not need chain display -- the tier label already conveys this
- Use the tier Display Mapping for each node's tier in the chain
```

4. **Add English title rendering rule** as part of the Core Focus template:
After the title line `### {title}`, add:
```
{English original title -- only render if item language == "zh" and ProvenanceRecord.original_source_name suggests an English-language original. Use original_source_name as attribution, not a separate LLM translation call.}
```

5. **Update Rendering Contract** user-facing fields table. Add these rows:

| Field | Display Label | Notes |
|-------|---------------|-------|
| `ProvenanceRecord.tier` | 信源层级 | Always rendered for every item using tier Display Mapping |
| `ProvenanceRecord.original_source_name` | 原始来源 | Only when different from current source_name |
| `ProvenanceRecord.provenance_chain` | 溯源链 | Only when chain length > 1 |

6. **Update Rendering Contract** internal-only fields table. Add these rows:

| Field | Purpose |
|-------|---------|
| `ProvenanceRecord.tier_source` | Internal: how tier was determined (rule vs llm) |
| `ProvenanceRecord.tier_confidence` | Internal: classification confidence score |
| `ProvenanceRecord.rule_result` | Internal: URL-rule preclassification result |
| `ProvenanceRecord.llm_result` | Internal: LLM classification result |
| `ProvenanceRecord.original_source_url` | Internal: raw original source URL |

**In references/data-models.md**, add a note under the ProvenanceRecord section clarifying which fields are user-facing vs internal, referencing the rendering contract in output-templates.md.

**In SKILL.md**, update the Output Phase step 4 ("Generate digest") to add: "Include provenance metadata (tier, original-source attribution, provenance chain) per `references/output-templates.md` rendering contract. Join each selected item to its ProvenanceRecord via `NewsItem.id` in `data/provenance/provenance-db.json`."
  </action>
  <verify>
    <automated>grep -c "信源层级" references/output-templates.md && grep -c "tier Display Mapping" references/output-templates.md && grep -c "溯源链" references/output-templates.md && grep -c "tier_source" references/output-templates.md && grep -c "provenance" SKILL.md</automated>
  </verify>
  <done>
- Tier display mapping table exists with T0-T4 Chinese labels
- Daily digest Core Focus template shows tier, optional original-source, optional provenance chain
- Compact format shows tier in parenthetical
- Provenance chain rendering rules documented with length > 1 condition
- Rendering contract has 3 new user-facing provenance fields
- Rendering contract has 5 new internal-only provenance fields
- SKILL.md Output Phase references provenance rendering
- No internal provenance diagnostic fields appear in any template
  </done>
</task>

</tasks>

<verification>
1. `grep "Step 0:" references/processing-instructions.md` returns event-level suppression
2. `grep "0.92" references/processing-instructions.md` returns T4 threshold
3. `grep "信源层级" references/output-templates.md` returns tier display label in templates
4. `grep "tier_source" references/output-templates.md` appears only in the Internal-Only table
5. `grep "溯源链" references/output-templates.md` returns chain rendering rules
6. `grep "provenance" SKILL.md` returns updated Output Phase step
7. The existing 0.85 threshold remains for T0-T3 items (not globally replaced)
</verification>

<success_criteria>
- Alert decision tree has event-level suppression (Step 0) and tier-aware threshold (Step 0A)
- T4 alert threshold is 0.92; T0-T3 remains 0.85
- Digest template renders tier, original-source, and provenance chain with documented conditions
- Rendering contract explicitly classifies provenance fields as user-facing vs internal-only
- SKILL.md references provenance in Output Phase
- No internal provenance fields leak into any user-facing template
</success_criteria>

<output>
After completion, create `.planning/phases/15-provenance-aware-ranking-delivery/15-02-SUMMARY.md`
</output>

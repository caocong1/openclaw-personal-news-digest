---
phase: 03-closed-loop
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - references/prompts/history-query.md
  - references/processing-instructions.md
  - SKILL.md
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-04, HIST-05]

must_haves:
  truths:
    - "User can ask about recent news and get results from the last 24 hours"
    - "User can ask about a specific topic and get filtered results from recent days"
    - "User can ask about event follow-ups and get timeline from active events"
    - "User can ask about missed high-importance items outside their interests"
    - "User can ask about source health and get performance dashboard"
    - "Natural language queries in both Chinese and English are classified into query types"
    - "Query results are capped at 20 items to avoid overwhelming the chat"
  artifacts:
    - path: "references/prompts/history-query.md"
      provides: "LLM prompt for query type classification and response formatting"
      contains: "RECENT_ACTIVITY"
    - path: "references/processing-instructions.md"
      provides: "History query execution procedures for all 5 query types"
      contains: "History Query"
    - path: "SKILL.md"
      provides: "User Commands routes history queries to history-query.md"
      contains: "history"
  key_links:
    - from: "references/processing-instructions.md"
      to: "data/news/*.jsonl"
      via: "History queries read JSONL files by date range"
      pattern: "JSONL|jsonl"
    - from: "references/processing-instructions.md"
      to: "data/events/active.json"
      via: "Event tracking queries read active events"
      pattern: "active\\.json"
    - from: "references/processing-instructions.md"
      to: "config/sources.json"
      via: "Source analysis queries read source stats"
      pattern: "sources\\.json"
---

<objective>
Implement natural language history query system with 5 query types.

Purpose: Users currently have no way to explore their accumulated news data. This plan adds a natural language query system that lets users ask questions like "what happened with AI this week", "show me recent news", "what did I miss", "how is 36Kr doing", and receive relevant results from JSONL data files. The system uses LLM to classify query intent into one of 5 types (HIST-01 through HIST-05), then executes type-specific data lookups. HIST-06 (preference state query) is already handled by Plan 01's preference visualization.

Output: History query LLM prompt, query execution procedures in processing-instructions.md, SKILL.md user command routing.

Note: Depends on 03-01 because SKILL.md User Commands section is restructured there to separate preference queries from history queries.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-closed-loop/03-RESEARCH.md
@.planning/phases/03-closed-loop/03-01-SUMMARY.md

@references/processing-instructions.md
@references/output-templates.md
@references/data-models.md
@config/categories.json
@config/sources.json
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create history query prompt and execution procedures</name>
  <files>references/prompts/history-query.md, references/processing-instructions.md</files>
  <action>
1. Create new file `references/prompts/history-query.md` -- LLM prompt template for history query classification and response:

   ```markdown
   # History Query Classification Prompt

   Given user message: "{user_message}"

   Classify into one of these query types:

   1. **RECENT_ACTIVITY** - asking about recent news (last 24h, today, latest, 最新, 最近, 今天)
   2. **TOPIC_REVIEW** - asking about a specific topic over a time period (AI this week, 这周的科技新闻, show me X news from last N days)
   3. **EVENT_TRACKING** - asking about a specific event's developments (what happened with X, 某某事件后续, follow-up on X)
   4. **HOTSPOT_SCAN** - asking about important news they might have missed (what did I miss, 错过了什么, high importance outside interests)
   5. **SOURCE_ANALYSIS** - asking about a specific source's performance (how is X source, 某来源怎么样, source health)

   Output JSON:
   ```json
   {
     "query_type": "TOPIC_REVIEW",
     "parameters": {
       "topic": "ai-models",
       "days": 7,
       "source": null,
       "event_keywords": null
     }
   }
   ```

   Parameter extraction rules:
   - `topic`: Match against category IDs from {categories_list}. Null if not topic-specific.
   - `days`: Extract time range. Default 1 for RECENT_ACTIVITY, 7 for TOPIC_REVIEW, 30 for EVENT_TRACKING. Cap at 30 days.
   - `source`: Match against source names/IDs. Null if not source-specific.
   - `event_keywords`: Extract event-related keywords for EVENT_TRACKING. Null otherwise.

   If the query does not clearly fit any type, default to RECENT_ACTIVITY.
   ```

2. In `references/processing-instructions.md`, add a new section after Section 7 (Weekly Report): `## Section 8: History Query Execution (HIST-01 through HIST-05)`.

   Content:

   ### Overview
   When the user sends a natural language query (not source management, not feedback, not preference query), classify query type using `references/prompts/history-query.md` then execute the matching procedure below. Cap all results at 20 items.

   ### HIST-01: Recent Activity Query

   **Trigger:** User asks about recent/latest/today's news.
   **Procedure:**
   1. Read today's `data/news/YYYY-MM-DD.jsonl`
   2. Filter items with `processing_status: "complete"`
   3. Sort by `final_score` descending (if available) or `importance_score` descending
   4. Cap at 20 items
   5. Format response: compact list with title, 1-sentence summary, source, importance

   **Response format:**
   ```
   ## Recent News (last 24 hours)
   Found {N} items.

   1. **{title}** -- {1-sentence summary}
      Source: {source_name} | Importance: {score} | {time_ago}
   2. ...
   ```

   ### HIST-02: Topic Review Query

   **Trigger:** User asks about a specific topic over a time period.
   **Procedure:**
   1. Classify the topic using `config/categories.json` category IDs
   2. Read N days of `data/news/YYYY-MM-DD.jsonl` files (default 7, max 30)
   3. Filter items where `categories.primary == matched_topic` AND `processing_status: "complete"`
   4. Sort by `published_at` descending
   5. Cap at 20 items
   6. Use LLM to generate a brief trend summary of the filtered items (2-3 sentences)

   **Response format:**
   ```
   ## {Topic Name} News (last {N} days)
   {LLM trend summary: 2-3 sentences}

   Found {total} items, showing top {shown}:
   1. **{title}** -- {1-sentence summary}
      Source: {source_name} | {date} | Importance: {score}
   2. ...
   ```

   ### HIST-03: Event Tracking Query

   **Trigger:** User asks about a specific event's developments.
   **Procedure:**
   1. Read `data/events/active.json`
   2. Match event by keyword overlap: tokenize user query keywords, compare against `event.keywords` and `event.title`
   3. If multiple matches: present top 3 candidates and ask user to clarify
   4. If single match: present full event timeline

   **Response format:**
   ```
   ## Event: {event_title}
   Status: {status} | Importance: {importance} | First seen: {date}

   {event_summary}

   ### Timeline
   - [{date}] {brief} ({relation}) -- Source: {source_name}
   - [{date}] {brief} ({relation}) -- Source: {source_name}
   ...

   Related items: {item_count} total
   ```

   If no event match found, also scan last 7 days of JSONL for items matching the query keywords and present those instead.

   ### HIST-04: Hotspot Scan Query

   **Trigger:** User asks about missed or high-importance news outside their interests.
   **Procedure:**
   1. Read `config/preferences.json` to get topic_weights
   2. Read last 7 days of JSONL files
   3. Filter items where: `importance_score >= 0.7` AND `topic_weights[categories.primary] < 0.5`
   4. Sort by `importance_score` descending
   5. Cap at 20 items

   **Response format:**
   ```
   ## Things You Might Have Missed
   High-importance news outside your usual interests:

   1. **{title}** -- {1-sentence summary}
      Category: {category} (your weight: {weight}) | Importance: {score}
      Why shown: Important event in a category you don't usually follow
   2. ...
   ```

   ### HIST-05: Source Analysis Query

   **Trigger:** User asks about a specific source's performance.
   **Procedure:**
   1. Read `config/sources.json`
   2. Match source by name (case-insensitive substring match)
   3. If multiple matches: list candidates
   4. If single match: format source health dashboard

   **Response format:**
   ```
   ## Source: {source_name}
   Type: {type} | Status: {status} | Enabled: {enabled}

   ### Performance (7-day rolling)
   - Quality score: {quality_score}
   - Dedup rate: {dedup_rate} ({interpretation})
   - Selection rate: {selection_rate} ({interpretation})
   - Total fetched: {total_fetched}
   - Last fetch: {last_fetch}
   - Consecutive failures: {consecutive_failures}

   {If degraded: "Degraded since: {degraded_since}. Source will auto-recover when quality_score > 0.3 for 7 consecutive days."}
   ```

   ### Query Performance Guidelines
   - Default lookback: 1 day (HIST-01), 7 days (HIST-02, HIST-04), event lifetime (HIST-03)
   - Max lookback: 30 days. If user requests longer range, cap at 30 and inform: "Showing last 30 days (maximum range)."
   - If query takes long (>15 seconds), consider reducing date range
   - JSONL files are date-partitioned so lookback maps directly to file count
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "RECENT_ACTIVITY" references/prompts/history-query.md && grep -q "TOPIC_REVIEW" references/prompts/history-query.md && grep -q "History Query" references/processing-instructions.md && grep -q "HIST-01" references/processing-instructions.md && grep -q "HIST-05" references/processing-instructions.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>History query prompt classifies user messages into 5 query types with parameter extraction. Execution procedures for each type: HIST-01 (recent 24h), HIST-02 (topic by time), HIST-03 (event timeline), HIST-04 (missed hotspots), HIST-05 (source health). All results capped at 20 items with consistent response formats.</done>
</task>

<task type="auto">
  <name>Task 2: Wire history query routing into SKILL.md</name>
  <files>SKILL.md</files>
  <action>
1. In `SKILL.md`, the User Commands section was already restructured by Plan 03-01 to separate preference queries and history queries. This task ensures the history query routing is fully wired.

   Verify that SKILL.md User Commands includes (from 03-01's changes):
   ```
   4. **History query**: If intent is a data query (recent news, topic review, event tracking, hotspot scan, source analysis), follow `{baseDir}/references/prompts/history-query.md`.
   ```

   If the step already exists from 03-01, enhance it to be more specific:
   ```
   4. **History query**: If intent is a data query (recent news, topic review, event tracking, hotspot scan, source analysis), classify query type per `{baseDir}/references/prompts/history-query.md`, then execute per `{baseDir}/references/processing-instructions.md` Section 8.
   ```

   The key addition is the reference to Section 8 execution procedures, not just the classification prompt. This ensures the executor knows where to find the full query handling logic.

2. Verify SKILL.md word count stays within budget. The change is minimal (~10 words net). If over budget, compact surrounding text slightly.

3. No other SKILL.md changes needed -- the User Commands routing is the only integration point. The actual query logic is self-contained in processing-instructions.md Section 8.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "history-query" SKILL.md && grep -q "Section 8" SKILL.md && wc -w < SKILL.md | awk '{if ($1 <= 950) print "PASS: " $1 " words"; else print "FAIL: " $1 " words (over 950)"}'</automated>
  </verify>
  <done>SKILL.md User Commands routes history queries through classification prompt then to Section 8 execution procedures. Word count within budget. History query system fully wired end-to-end.</done>
</task>

</tasks>

<verification>
NOTE: All verify commands assume CWD is the project root.

- History query prompt classifies into 5 types: RECENT_ACTIVITY, TOPIC_REVIEW, EVENT_TRACKING, HOTSPOT_SCAN, SOURCE_ANALYSIS
- Each query type has a complete execution procedure with data lookup and response format
- Results capped at 20 items for all query types
- Date range caps: 1 day default for recent, 7 for topic/hotspot, 30 max
- SKILL.md routes history queries through prompt classification then Section 8 execution
- Chinese and English query phrasings supported in classification prompt
</verification>

<success_criteria>
- All 5 HIST query types (01-05) have execution procedures
- LLM classifies natural language queries into correct types
- Response formats are consistent and informative
- Performance guidelines prevent excessive JSONL scanning
- SKILL.md wiring complete and word count within budget
- All files pass grep checks
</success_criteria>

<output>
After completion, create `.planning/phases/03-closed-loop/03-04-SUMMARY.md`
</output>

---
phase: 03-closed-loop
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - references/output-templates.md
  - references/prompts/weekly-report.md
  - references/processing-instructions.md
  - references/cron-configs.md
  - SKILL.md
autonomous: true
requirements: [OUT-03]

must_haves:
  truths:
    - "Weekly report covers 5+ topic categories with trend analysis"
    - "Weekly report includes event timelines for top events of the week"
    - "Weekly report has cross-domain synthesis narrative written by strong model"
    - "Weekly quota ratios differ from daily: core 40% / adjacent 20% / hotspot 20% / explore 20%"
    - "Weekly report aggregates 7 days of data including daily metrics and news items"
    - "Weekly report includes source health summary with degraded source mentions"
    - "Weekly cron job fires Sunday 20:00 CST"
  artifacts:
    - path: "references/output-templates.md"
      provides: "Weekly report template with 6 sections"
      contains: "Weekly Report"
    - path: "references/prompts/weekly-report.md"
      provides: "LLM prompt for weekly cross-domain synthesis"
      contains: "cross-domain"
    - path: "references/processing-instructions.md"
      provides: "Weekly report generation procedure"
      contains: "weekly"
    - path: "references/cron-configs.md"
      provides: "Weekly report cron job (Sunday 20:00)"
      contains: "weekly-report"
    - path: "SKILL.md"
      provides: "Output Phase handles weekly report type"
      contains: "weekly"
  key_links:
    - from: "references/processing-instructions.md"
      to: "data/news/*.jsonl"
      via: "Weekly report reads 7 days of JSONL files"
      pattern: "JSONL|jsonl"
    - from: "references/processing-instructions.md"
      to: "data/metrics/daily-*.json"
      via: "Weekly report aggregates 7 days of daily metrics"
      pattern: "daily-.*\\.json"
    - from: "references/processing-instructions.md"
      to: "data/events/active.json"
      via: "Weekly report includes event timelines"
      pattern: "events"
---

<objective>
Implement weekly trend report generation with cross-domain synthesis.

Purpose: The system currently only produces daily digests and breaking news alerts. This plan adds a weekly report that aggregates 7 days of data into trend analysis, event timelines, source health summaries, and cross-domain connections. The weekly report uses different quota ratios (40/20/20/20 vs daily 50/20/15/15) to provide broader perspective and uses the strong model tier for synthesis. It must cover at least 5 topic categories per ANTI-05.

Output: Weekly report template in output-templates.md, LLM prompt for synthesis, generation procedure in processing-instructions.md, cron job config, SKILL.md wiring.

Note: This plan depends on 03-01 (decay/7-layer) because the weekly report references depth_preference for summary depth and judgment_angles for perspective emphasis. It depends on 03-02 because the source health summary mentions degraded sources.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-closed-loop/03-RESEARCH.md
@.planning/phases/03-closed-loop/03-01-SUMMARY.md
@.planning/phases/03-closed-loop/03-02-SUMMARY.md

@references/output-templates.md
@references/processing-instructions.md
@references/cron-configs.md
@references/data-models.md
@references/scoring-formula.md
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create weekly report template, LLM prompt, and generation procedure</name>
  <files>references/output-templates.md, references/prompts/weekly-report.md, references/processing-instructions.md</files>
  <action>
1. In `references/output-templates.md`, add a new section after the Transparency Footer section: `## Weekly Report Template (OUT-03)`.

   Template structure:

   ```markdown
   # Weekly News Digest: {start_date} - {end_date}

   ## One Week Overview
   {LLM-generated trend narrative, 2-3 paragraphs. Cross-domain synthesis highlighting connections between categories. Written by strong model.}

   ## Key Events & Timelines
   {Top 5-8 events by importance, each with full timeline:}
   ### {event_title}
   {event_summary (current state)}
   Timeline:
   - [{date}] {brief} ({relation}) -- Source: {source_name}
   - [{date}] {brief} ({relation}) -- Source: {source_name}
   Status: {status} | Items: {item_count} | First seen: {first_seen_date}

   ## Category Trends
   {Per-category highlights with comparison to previous week:}
   ### {category_name}
   - This week: {item_count} items ({proportion}%)
   - Trend: {up/down/stable compared to previous week}
   - Key stories: {top 2-3 stories from this category}

   ## Source Health Summary
   {Source performance overview:}
   - Active sources: {count}
   - Degraded sources: {count and names}
   - Quality changes: {sources with significant quality_score changes}
   - New sources added this week: {count}

   ## Cross-Domain Connections
   {LLM-synthesized insights identifying connections across categories. E.g., "AI regulation developments (macro-policy) may affect developer tools adoption (dev-tools)" -- written by strong model.}

   ---
   Week stats: {total_items_processed} items from {source_count} sources | {event_count} events tracked | {llm_calls} LLM calls total
   ```

   Quality rules for weekly report:
   - Must cover >= 5 different categories (ANTI-05 weekly requirement)
   - Use weekly quota: core 40% / adjacent 20% / hotspot 20% / explore 20% (more exploration than daily)
   - If fewer than 3 days of data available: output shortened version, omit trend comparisons
   - Use strong model tier for "One Week Overview" and "Cross-Domain Connections" sections (COST-04)

2. Create new file `references/prompts/weekly-report.md` -- LLM prompt template for weekly report synthesis:

   ```markdown
   # Weekly Report Synthesis Prompt

   You are generating a weekly news digest report. Your task is to synthesize {days_count} days of news data into trend analysis and cross-domain insights.

   ## Input Data

   ### Category Distribution
   {category_stats: per-category item counts, week-over-week change}

   ### Top Events (by importance)
   {event_list: event title, summary, timeline entries, item count, importance}

   ### Top Stories Per Category
   {per_category_top_items: for each category, top 3-5 items by final_score with title, summary, source}

   ### Source Performance
   {source_stats: source name, quality_score, status, items fetched this week}

   ### User Preferences Context
   Depth: {depth_preference}
   Angles: {judgment_angles or "none specified"}

   ## Output Requirements

   Generate TWO sections:

   ### 1. One Week Overview (2-3 paragraphs)
   - Identify the dominant themes and shifts of the week
   - Note emerging trends or surprising developments
   - Connect developments across categories when meaningful
   - If judgment_angles specified, emphasize those perspectives
   - Match depth to depth_preference setting

   ### 2. Cross-Domain Connections (3-5 bullet points)
   - Identify non-obvious connections between stories in different categories
   - Each connection must reference specific stories/events
   - Focus on actionable insights, not vague generalizations

   Output as markdown. Do not include section headers -- the caller will wrap your output in the report template.
   ```

3. In `references/processing-instructions.md`, add a new section after Section 6 (Source Auto-Demotion): `## Section 7: Weekly Report Generation (OUT-03)`.

   Content:

   ### Trigger
   Weekly cron job (Sunday 20:00 CST) or manual user request.

   ### Data Collection (pre-LLM)
   1. Read last 7 days of `data/news/YYYY-MM-DD.jsonl` files. Collect all items with `processing_status: "complete"`.
   2. Read last 7 days of `data/metrics/daily-*.json` files. Aggregate totals.
   3. Read `data/events/active.json` for event timelines. Include events with status "active" or "stable" that have timeline entries in the last 7 days.
   4. Read `config/sources.json` for source health data.
   5. Read `config/preferences.json` for depth_preference and judgment_angles.

   ### Pre-filtering (to avoid LLM context overflow)
   - From the 7-day item pool, use only items that were selected for daily digests (items with `quota_group` set). Expected: 15-25/day * 7 = ~105-175 items.
   - If still > 200 items: further filter to top 150 by final_score.
   - Group items by `categories.primary` for per-category summaries.
   - Pass event summaries rather than full item lists to the LLM.

   ### Weekly Quota (different from daily)
   Apply weekly quota ratios: core 40% / adjacent 20% / hotspot 20% / explore 20%.
   The weekly report shows more exploration and hotspot content than daily (daily: 50/20/15/15).
   Target 30-50 items for weekly report. Apply the same quota algorithm from Section 4 but with weekly ratios.

   ### Category Minimum
   Ensure >= 5 different categories are represented. If fewer than 5 categories have items, include the best item from each missing category that has ANY items in the 7-day pool (even if not selected for daily digests).

   ### LLM Synthesis
   1. Use `references/prompts/weekly-report.md` template
   2. Fill placeholders with aggregated data
   3. Use **strong model tier** (COST-04) for this call -- cross-domain synthesis requires nuanced reasoning
   4. Parse LLM output: extract "One Week Overview" and "Cross-Domain Connections" sections

   ### Assembly
   1. Fill weekly report template from `references/output-templates.md`
   2. Write to `output/latest-weekly.md` atomically
   3. Write weekly metrics to `data/metrics/weekly-{start_date}.json` (separate from daily metrics)
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "Weekly Report" references/output-templates.md && grep -q "cross-domain" references/prompts/weekly-report.md && grep -q "Section 7" references/processing-instructions.md && grep -q "weekly" references/processing-instructions.md && grep -q "40%" references/processing-instructions.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Weekly report template has 6 sections (Overview, Key Events, Category Trends, Source Health, Cross-Domain, Stats footer). LLM prompt generates cross-domain synthesis using strong model. Generation procedure aggregates 7 days of data with pre-filtering to avoid context overflow. Weekly quota 40/20/20/20 with 5-category minimum.</done>
</task>

<task type="auto">
  <name>Task 2: Add weekly report cron job and wire into SKILL.md</name>
  <files>references/cron-configs.md, SKILL.md</files>
  <action>
1. In `references/cron-configs.md`, add a new cron job section after the "Weekly Health Inspection Job" section. Title: `## Weekly Report Job (Phase 3+)`.

   ```json
   {
     "name": "news-weekly-report",
     "schedule": { "kind": "cron", "expr": "0 20 * * 0", "tz": "Asia/Shanghai" },
     "sessionTarget": "isolated",
     "payload": {
       "kind": "agentTurn",
       "message": "Generate the weekly news digest report: aggregate last 7 days of news data, compute category trends, collect event timelines, synthesize cross-domain connections, generate weekly report, update weekly metrics.",
       "lightContext": false,
       "timeoutSeconds": 600
     },
     "delivery": {
       "mode": "announce",
       "channel": "telegram",
       "to": "{target_chat_id}"
     }
   }
   ```

   Key config notes:
   - Schedule: `0 20 * * 0` = Sunday 20:00 CST (day 0 = Sunday)
   - Timeout: 600 seconds (10 min) -- weekly report needs LLM strong model synthesis which takes longer
   - lightContext: false (required for SKILL.md loading, per Phase 0 decision)
   - sessionTarget: isolated (clean session per cron run)

   Update the Recommended Setup Order section to include: "7. Register the weekly report job (Phase 3+)"

2. In `SKILL.md`, make these changes:

   a. **Output Phase**: After the existing step for digest generation and before "Write output", add handling for weekly report:
      - After step 4 (Generate digest): add `4b. **Weekly report** (if triggered by weekly cron): Read {baseDir}/references/processing-instructions.md Section 7. Aggregate 7 days of data, apply weekly quota (40/20/20/20), use strong model for synthesis, write to {baseDir}/output/latest-weekly.md.`

   b. **Cron trigger detection**: The agent already distinguishes daily digest from quick-check based on the cron message. The weekly report cron message ("Generate the weekly news digest report") is distinct enough for the agent to route correctly. No additional routing logic needed in SKILL.md -- the message itself guides the agent.

   Keep within SKILL.md word budget. The weekly report step adds ~40 words. If needed, compact the existing Output Phase description slightly.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "news-weekly-report" references/cron-configs.md && grep -q "0 20" references/cron-configs.md && grep -qi "weekly.*report\|weekly.*cron\|weekly" SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Weekly report cron job fires Sunday 20:00 CST with 10-min timeout and isolated session. SKILL.md Output Phase routes weekly cron trigger to Section 7 procedure. Recommended setup order updated.</done>
</task>

</tasks>

<verification>
NOTE: All verify commands assume CWD is the project root.

- Weekly report template has all 6 sections (Overview, Events, Trends, Source Health, Cross-Domain, Stats)
- Weekly quota ratios are 40/20/20/20 (different from daily 50/20/15/15)
- LLM prompt uses strong model tier for synthesis
- Pre-filtering caps items at ~150-200 to avoid context overflow
- Category minimum of 5 enforced
- Cron job configured for Sunday 20:00 CST
- Output written to output/latest-weekly.md
- SKILL.md routes weekly cron trigger correctly
</verification>

<success_criteria>
- Weekly report covers trends, events, categories, sources, and cross-domain connections
- Strong model used for narrative synthesis
- Pre-filtering prevents LLM context overflow on 7-day data aggregation
- Weekly quota provides more exploration than daily (20% vs 15%)
- Cron job scheduled Sunday 20:00 CST with correct configuration
- All files pass grep checks
</success_criteria>

<output>
After completion, create `.planning/phases/03-closed-loop/03-03-SUMMARY.md`
</output>

---
phase: 03-closed-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/processing-instructions.md
  - references/feedback-rules.md
  - references/data-models.md
  - config/preferences.json
  - SKILL.md
autonomous: true
requirements: [PREF-04, PREF-06, PREF-07, HIST-06]

must_haves:
  truths:
    - "After 30 days without reinforcement, preference weights visibly drift back toward neutral via decay formula"
    - "Decay applies at most once per 30-day period with no catch-up for missed periods"
    - "topic_weights decay toward 0.5, source_trust toward 0, form_preference toward 0"
    - "Decay runs BEFORE feedback processing in the pipeline to avoid compounding"
    - "preferences.json includes depth_preference and judgment_angles as new 7-layer fields"
    - "depth_preference controls summary length/depth in summarize prompt, not the scoring formula"
    - "judgment_angles influences which aspects the summary emphasizes, not the scoring formula"
    - "User can query their preference state and receive a human-readable text description"
  artifacts:
    - path: "references/processing-instructions.md"
      provides: "Preference decay procedure as pipeline pre-step"
      contains: "decay"
    - path: "references/feedback-rules.md"
      provides: "Preference visualization procedure for HIST-06/PREF-06"
      contains: "Preference Visualization"
    - path: "references/data-models.md"
      provides: "Updated Preferences schema with depth_preference and judgment_angles"
      contains: "depth_preference"
    - path: "config/preferences.json"
      provides: "Updated preferences file with new 7-layer fields"
      contains: "depth_preference"
    - path: "SKILL.md"
      provides: "Processing Phase includes decay check; User Commands routes HIST-06"
      contains: "decay"
  key_links:
    - from: "SKILL.md"
      to: "references/processing-instructions.md"
      via: "Processing Phase step 0 runs decay check before feedback"
      pattern: "decay"
    - from: "SKILL.md"
      to: "references/feedback-rules.md"
      via: "User Commands routes preference query to visualization procedure"
      pattern: "preference.*query|HIST-06"
    - from: "references/processing-instructions.md"
      to: "config/preferences.json"
      via: "Decay formula reads and writes preference fields atomically"
      pattern: "preferences\\.json"
---

<objective>
Implement preference decay, extend to 7-layer model, and add preference state visualization.

Purpose: Preferences currently fixate over time -- once a topic weight is pushed high by feedback, it stays there forever. This plan adds a 30-day decay mechanism that regresses weights toward neutral, preventing interest fixation. It also extends the 5-layer preference model to 7 layers by adding depth_preference (controls summary detail level) and judgment_angles (controls perspective emphasis). Finally, it adds the ability for users to query their preference state as a human-readable text description (PREF-06/HIST-06).

Output: Decay procedure in processing-instructions.md, visualization procedure in feedback-rules.md, updated preferences schema in data-models.md, updated preferences.json with new fields, SKILL.md wiring for decay and preference queries.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-closed-loop/03-RESEARCH.md

@references/processing-instructions.md
@references/feedback-rules.md
@references/data-models.md
@references/scoring-formula.md
@config/preferences.json
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preference decay procedure and 7-layer model schema</name>
  <files>references/processing-instructions.md, references/data-models.md, config/preferences.json</files>
  <action>
1. In `references/processing-instructions.md`, add a new section **before** Section 0A (Circuit-Breaker). Insert as `## Section 0: Preference Decay (PREF-04)` at the very top of the file (after the file header). This ensures decay runs as the first step before any LLM processing or feedback application.

   Content of the new section:

   ### When to Run
   At the START of each daily pipeline run, before feedback processing (Section 11 of SKILL.md) and before any LLM calls.

   ### Decay Check Procedure
   1. Read `config/preferences.json`
   2. Check `last_decay_at` field:
      - If `last_decay_at` is null OR `(now - last_decay_at) >= 30 days`: proceed with decay
      - Otherwise: skip decay, continue to next pipeline step
   3. **No catch-up rule**: If 60+ days have passed, still apply only ONE decay round. Do NOT accumulate missed periods.

   ### Decay Formula
   For each field that decays:

   **topic_weights (decay toward 0.5 -- cold-start neutral):**
   ```
   For each key in topic_weights:
     topic_weights[key] = topic_weights[key] + (0.5 - topic_weights[key]) * 0.05
   ```

   **source_trust (decay toward 0 -- neutral/absent):**
   ```
   For each key in source_trust:
     source_trust[key] = source_trust[key] + (0.0 - source_trust[key]) * 0.05
     If abs(source_trust[key]) < 0.01: DELETE key (clean up near-zero entries)
   ```

   **form_preference (decay toward 0 -- neutral):**
   ```
   For each key in form_preference:
     form_preference[key] = form_preference[key] + (0.0 - form_preference[key]) * 0.05
   ```

   **Fields that do NOT decay:** style.*, feedback_samples, depth_preference, judgment_angles

   ### Write-Back
   1. Set `last_decay_at` to current ISO8601 timestamp
   2. Backup-before-write (existing pattern from feedback-rules.md)
   3. Atomic write `config/preferences.json`
   4. Log to daily metrics: `"preference_decay_applied": true`

   ### Interaction with Feedback
   Decay runs BEFORE feedback processing. This means user feedback in the same run adjusts from the post-decay baseline, which is correct -- recent feedback should override decay drift.

2. In `references/data-models.md`, add a new `## Preferences` section (after the existing FeedbackEntry section, before the DailyMetrics section). Document the full preferences.json schema including the two new 7-layer fields:

   ```json
   {
     "topic_weights": { "ai-models": 0.5, "...": 0.5 },
     "source_trust": {},
     "form_preference": { "news": 0.0, "...": 0.0 },
     "style": {
       "density": "medium",
       "repetition_tolerance": "low",
       "exploration_appetite": 0.3,
       "rumor_tolerance": "low",
       "last_exploration_increase": null
     },
     "feedback_samples": {
       "liked_items": [],
       "disliked_items": [],
       "trusted_sources": [],
       "distrusted_sources": [],
       "blocked_patterns": []
     },
     "depth_preference": "moderate",
     "judgment_angles": [],
     "version": 3,
     "last_updated": null,
     "last_decay_at": null,
     "total_feedback_count": 0,
     "feedback_processing_enabled": true,
     "_schema_v": 2
   }
   ```

   Field notes for new fields:
   - `depth_preference`: One of "brief" | "moderate" | "detailed" | "technical". Default "moderate". Controls summary generation depth -- "brief" produces 1-sentence summaries, "moderate" produces 2-3 sentences (current default), "detailed" produces 3-5 sentences with background context, "technical" adds implementation details where relevant. Wired into summarize prompt, NOT the scoring formula.
   - `judgment_angles`: Array of perspective tags the user prefers. Options: "workflow_impact", "worth_trying", "hype_vs_real", "market_change", "long_term_value", "practical_use". Cold-start is empty array (no angle preference). Wired into weekly report synthesis and summarize prompt to emphasize relevant angles. NOT wired into scoring formula.
   - `last_decay_at`: ISO8601 timestamp of last decay application. Null means never decayed.
   - `_schema_v`: Bump to 2. Readers handle v1 (missing `depth_preference`, `judgment_angles`) with defaults: `depth_preference` = "moderate", `judgment_angles` = [].

3. Update `config/preferences.json` to add the new fields:
   - Add `"depth_preference": "moderate"` after `feedback_samples`
   - Add `"judgment_angles": []` after `depth_preference`
   - Bump `"version": 3`
   - Add `"_schema_v": 2`
   - Keep `"last_decay_at": null` (already exists)
   - Keep all existing fields unchanged
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "Preference Decay" references/processing-instructions.md && grep -q "0.05" references/processing-instructions.md && grep -q "depth_preference" references/data-models.md && grep -q "judgment_angles" references/data-models.md && grep -q "depth_preference" config/preferences.json && grep -q "judgment_angles" config/preferences.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Preference decay procedure documented in processing-instructions.md as Section 0 (runs before circuit-breaker and all LLM calls). Decay formula: 5% regression toward neutral per 30-day period, no catch-up. Preferences schema in data-models.md includes depth_preference and judgment_angles fields. config/preferences.json updated with new fields and bumped to schema v2.</done>
</task>

<task type="auto">
  <name>Task 2: Add preference visualization procedure and wire decay + queries into SKILL.md</name>
  <files>references/feedback-rules.md, SKILL.md</files>
  <action>
1. In `references/feedback-rules.md`, add a new section at the end: `## Preference Visualization (PREF-06 / HIST-06)`. This defines how to generate a human-readable preference state description when the user asks about their preferences.

   ### Trigger
   User asks about preferences: "what have you learned about me", "show my preferences", "我的偏好", "偏好状态", "what are my interests", or similar intent.

   ### Procedure
   1. Read `config/preferences.json`
   2. Generate text description using the following structure:

   ```
   ## Your Preference Profile

   **Top interests:** {topics with weight >= 0.7, sorted descending, format: "TopicName (weight: X.X)"}
   **Lower interest:** {topics with weight <= 0.3, sorted ascending}
   **Neutral topics:** {topics with weight 0.4-0.6, list names only}

   **Trusted sources:** {source_trust entries > 0, format: "SourceName (trust: +X.X)"}
   **Distrusted sources:** {source_trust entries < 0, format: "SourceName (trust: X.X)"}
   {If source_trust is empty: "No source preferences yet -- will develop from feedback"}

   **Content style:** {Interpret form_preference values -- positive means preference, negative means avoidance. Example: "You prefer analysis over opinion pieces. You have low rumor tolerance."}
   **Exploration:** {Interpret exploration_appetite -- 0.0-0.2: "Low", 0.2-0.35: "Moderate", 0.35+: "High"} (appetite: {value}) -- {explain effect}

   **Depth preference:** {depth_preference value} -- {explain: brief=headlines only, moderate=balanced summaries, detailed=in-depth with context, technical=includes implementation details}
   **Judgment angles:** {If empty: "Not yet learned -- will develop over time". If set: list angles with explanations}

   **Feedback history:** {total_feedback_count} feedback signals processed
   **Last preference update:** {last_updated or "Never"}
   **Last decay applied:** {last_decay_at or "Never"}
   ```

   3. Use LLM to polish the structured data into natural-sounding narrative text (not just template fill). The LLM should make it conversational while preserving all data points.
   4. Cap response to reasonable length -- all key preference dimensions but no verbose explanations.

2. In `SKILL.md`, make these changes:

   a. **Processing Phase**: Add a new step 0 at the very beginning (before "Load prompts"):
      `0. **Preference decay**: Check and apply preference decay per {baseDir}/references/processing-instructions.md Section 0. Runs once per 30-day period.`

   b. **User Commands**: Update the existing step 3 ("Query") to be more specific about history and preference queries:
      Replace the current step 3 with:
      ```
      3. **Preference query**: If intent is asking about preferences or what the system has learned, follow `{baseDir}/references/feedback-rules.md` "Preference Visualization" section.
      4. **History query**: If intent is a data query (recent news, topic review, event tracking, hotspot scan, source analysis), follow `{baseDir}/references/prompts/history-query.md`.
      5. **General**: Otherwise, treat as a general query and respond helpfully.
      ```

   Keep SKILL.md within word budget. The decay step adds ~20 words. The user commands restructure replaces ~15 words with ~45 words. Net addition ~50 words. If needed, compact existing text slightly.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "Preference Visualization" references/feedback-rules.md && grep -q "depth_preference" references/feedback-rules.md && grep -q "decay" SKILL.md && grep -q "Preference query" SKILL.md && grep -q "History query" SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Preference visualization procedure in feedback-rules.md generates human-readable profile text covering all 7 layers. SKILL.md Processing Phase starts with decay check (step 0). User Commands routes preference queries to feedback-rules.md visualization, history queries to history-query.md prompt. Word count within budget.</done>
</task>

</tasks>

<verification>
NOTE: All verify commands assume CWD is the project root (skill base directory). Each automated verify block uses `cd "$(git rev-parse --show-toplevel)"` to ensure correct working directory.

- Preference decay formula documented with 5% regression per 30-day period
- No catch-up rule explicitly stated
- Decay order: topic_weights toward 0.5, source_trust toward 0, form_preference toward 0
- Decay runs BEFORE feedback processing (documented and wired in SKILL.md)
- depth_preference and judgment_angles in data-models.md with defaults for schema migration
- config/preferences.json includes new fields
- Preference visualization procedure generates text covering all 7 layers
- SKILL.md routes preference queries and history queries separately
</verification>

<success_criteria>
- Decay mechanism fully specified with formula, timing, and no-catch-up constraint
- 7-layer model extension adds depth_preference and judgment_angles without modifying scoring formula
- Preference visualization produces human-readable text for all preference dimensions
- SKILL.md wiring ensures decay runs at pipeline start and queries are routed correctly
- All files pass grep checks
</success_criteria>

<output>
After completion, create `.planning/phases/03-closed-loop/03-01-SUMMARY.md`
</output>

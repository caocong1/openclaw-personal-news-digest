---
phase: 14-source-discovery-automation
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - references/processing-instructions.md
  - scripts/source-status.sh
  - data/fixtures/source-discovery-audit-sample.md
  - data/fixtures/discovered-sources-rejected-sample.json
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04]

must_haves:
  truths:
    - "Discovery outcomes remain human-auditable through explicit lifecycle records, not only through the current contents of config/sources.json"
    - "Pattern-library expansion is tied to high-confidence discovery decisions and preserves the representative URL evidence needed for path-sensitive rules"
    - "Operator-facing source inspection can surface discovery metadata without breaking the existing Phase 12 source-status contract"
  artifacts:
    - path: "references/processing-instructions.md"
      provides: "Audit and rule-library expansion rules for discovery lifecycle decisions"
      contains: "### Pattern-Library Expansion"
    - path: "scripts/source-status.sh"
      provides: "Operator-visible discovery metadata when present"
      contains: "Auto discovered"
    - path: "data/fixtures/source-discovery-audit-sample.md"
      provides: "Lifecycle audit artifact showing observed, enabled, and disabled states"
      contains: "observed -> enabled -> disabled"
    - path: "data/fixtures/discovered-sources-rejected-sample.json"
      provides: "Machine-readable rejected and disabled examples used by verification"
      contains: "\"reason\": \"tier_ratio_below_disable_threshold\""
  key_links:
    - from: "references/processing-instructions.md"
      to: "scripts/source-status.sh"
      via: "The operator surface prints the same discovery metadata fields documented in the lifecycle contract"
      pattern: "discovery_decision"
    - from: "data/fixtures/source-discovery-audit-sample.md"
      to: "data/fixtures/discovered-sources-rejected-sample.json"
      via: "The human-readable audit artifact and machine-readable fixture describe the same lifecycle outcomes"
      pattern: "example-direct-source.com"
---

<objective>
Finish Phase 14 by making discovery decisions operator-auditable, defining the exact rule-library expansion policy for newly trusted direct sources, and wiring verification coverage that proves the full discovery lifecycle is explainable.

Purpose: ensure Phase 14 does not stop at internal thresholds and fixtures, but also produces a clear audit trail and a safe path for expanding T1/T2 rule libraries.
Output: updated `references/processing-instructions.md`, updated `scripts/source-status.sh`, `data/fixtures/source-discovery-audit-sample.md`, and updated `data/fixtures/discovered-sources-rejected-sample.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-source-discovery-automation/14-CONTEXT.md
@.planning/phases/14-source-discovery-automation/14-RESEARCH.md
@.planning/phases/14-source-discovery-automation/14-VALIDATION.md
@.planning/phases/14-source-discovery-automation/14-01-PLAN.md
@.planning/phases/14-source-discovery-automation/14-02-PLAN.md
@references/processing-instructions.md
@references/data-models.md
@scripts/source-status.sh
@config/t1-sources.json
@config/t2-sources.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add discovery audit artifacts and operator-visible lifecycle metadata</name>
  <files>references/processing-instructions.md, scripts/source-status.sh, data/fixtures/source-discovery-audit-sample.md, data/fixtures/discovered-sources-rejected-sample.json</files>
  <read_first>references/processing-instructions.md, scripts/source-status.sh, references/data-models.md, data/fixtures/discovered-sources-rejected-sample.json, .planning/phases/14-source-discovery-automation/14-RESEARCH.md</read_first>
  <action>
1. In `references/processing-instructions.md`, add a subsection titled `### Discovery Audit Artifacts`.

2. In `### Discovery Audit Artifacts`, define a required human-readable audit artifact for the current state of discovery decisions. Document these exact required fields for each audited domain:
   - `domain`
   - `tier`
   - `first_seen`
   - `last_seen`
   - `hit_count_7d`
   - `t1_ratio`
   - `decision`
   - `reason`
   - `representative_urls`
   - `representative_titles`

3. Update `scripts/source-status.sh` so that when a source has discovery metadata fields it prints additional detail lines in single-source detail mode:
   - `Auto discovered: true|false`
   - `Discovery domain: {domain}`
   - `Discovery tier: {tier}`
   - `Discovery decision: {decision}`
   - `Discovery decided at: {timestamp}`
   Do not remove or rename any existing Phase 12 output lines.

4. In all-source summary mode, append `Auto discovered: true` to the summary line only when the source record includes `auto_discovered: true`. Keep sources without that flag unchanged.

5. Create `data/fixtures/source-discovery-audit-sample.md` with a lifecycle example for `example-direct-source.com` that shows:
   - initial observation
   - enable decision once thresholds pass
   - later disable decision after `t1_ratio` collapses
   Use the exact inline lifecycle phrase `observed -> enabled -> disabled`.

6. Update `data/fixtures/discovered-sources-rejected-sample.json` so the `example-direct-source.com` record includes three ordered decision-history entries:
   - `observed`
   - `enabled`
   - `disabled`
   with concrete timestamps and reasons.
  </action>
  <verify>
    <automated>rg -n "### Discovery Audit Artifacts|domain|decision|reason|representative_urls|representative_titles" references/processing-instructions.md data/fixtures/source-discovery-audit-sample.md && rg -n "Auto discovered|Discovery domain|Discovery tier|Discovery decision|Discovery decided at" scripts/source-status.sh</automated>
  </verify>
  <acceptance_criteria>
    - `references/processing-instructions.md` contains `### Discovery Audit Artifacts`
    - `scripts/source-status.sh` contains `Auto discovered`
    - `scripts/source-status.sh` contains `Discovery decision`
    - `data/fixtures/source-discovery-audit-sample.md` exists
    - `data/fixtures/source-discovery-audit-sample.md` contains `observed -> enabled -> disabled`
    - `data/fixtures/discovered-sources-rejected-sample.json` contains ordered lifecycle decisions for `example-direct-source.com`
  </acceptance_criteria>
  <done>Discovery decisions are now operator-auditable in both machine-readable fixtures and the existing source-status surface.</done>
</task>

<task type="auto">
  <name>Task 2: Define rule-library expansion and final verification coverage for discovery</name>
  <files>references/processing-instructions.md, data/fixtures/source-discovery-audit-sample.md</files>
  <read_first>references/processing-instructions.md, config/t1-sources.json, config/t2-sources.json, .planning/phases/14-source-discovery-automation/14-VALIDATION.md, .planning/phases/14-source-discovery-automation/14-RESEARCH.md</read_first>
  <action>
1. In `references/processing-instructions.md`, add a subsection titled `### Pattern-Library Expansion`.

2. In `### Pattern-Library Expansion`, document these exact promotion rules:
   - add a new pattern to `config/t1-sources.json` or `config/t2-sources.json` only after the source has a discovery decision of `enabled`
   - use `representative_urls` to preserve path-specific evidence when the source is path-scoped, for example `/blog`, `/ai`, or `/releases`
   - append to `config/t1-sources.json` when `discovery_tier` is `T1`
   - append to `config/t2-sources.json` when `discovery_tier` is `T2`
   - record the rule-library promotion inside discovery `decision_history.details.rule_library_target`

3. In the same subsection, explicitly forbid these shallow behaviors:
   - do not add every observed root domain immediately to the rule libraries
   - do not collapse path-scoped evidence into an over-broad bare-domain pattern when the representative URL contains a required path segment
   - do not delete the discovery-history entry after promotion

4. In `data/fixtures/source-discovery-audit-sample.md`, add a short section titled `Rule-library promotion evidence` that includes two concrete lines:
   - `T1 promotion target: config/t1-sources.json -> openai.com/blog`
   - `T2 promotion target: config/t2-sources.json -> venturebeat.com/ai`

5. In `references/processing-instructions.md`, append a final verification checklist for Phase 14 that names these exact proof points:
   - all DISC thresholds are present in docs
   - generated source entries include discovery metadata
   - audit artifacts preserve observed, enabled, deferred, rejected, and disabled outcomes
   - pattern-library promotions preserve path evidence
  </action>
  <verify>
    <automated>rg -n "### Pattern-Library Expansion|config/t1-sources.json|config/t2-sources.json|rule_library_target|do not add every observed root domain|pattern-library promotions preserve path evidence" references/processing-instructions.md data/fixtures/source-discovery-audit-sample.md</automated>
  </verify>
  <acceptance_criteria>
    - `references/processing-instructions.md` contains `### Pattern-Library Expansion`
    - `references/processing-instructions.md` says promotions only happen after `enabled`
    - `references/processing-instructions.md` says path-scoped evidence must be preserved
    - `data/fixtures/source-discovery-audit-sample.md` contains `config/t1-sources.json -> openai.com/blog`
    - `data/fixtures/source-discovery-audit-sample.md` contains `config/t2-sources.json -> venturebeat.com/ai`
    - `references/processing-instructions.md` contains a final Phase 14 verification checklist
  </acceptance_criteria>
  <done>The discovery loop now defines a safe, auditable rule-library promotion path and concrete end-to-end verification targets.</done>
</task>

</tasks>

<verification>
- `rg -n "### Discovery Audit Artifacts|### Pattern-Library Expansion" references/processing-instructions.md` returns both sections
- `rg -n "Auto discovered|Discovery decision" scripts/source-status.sh` returns matches
- `rg -n "observed -> enabled -> disabled|config/t1-sources.json -> openai.com/blog|config/t2-sources.json -> venturebeat.com/ai" data/fixtures/source-discovery-audit-sample.md` returns all audit markers
</verification>

<success_criteria>
- Discovery decisions remain auditable after enable and disable actions
- Operator-visible source inspection can explain discovery-origin metadata
- Rule-library expansion is constrained by enabled decisions and representative URL evidence
- Verification coverage proves the whole discovery lifecycle instead of isolated fields
</success_criteria>

---
phase: 05-daily-depth-control-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/processing-instructions.md
  - SKILL.md
  - references/output-templates.md
autonomous: true
requirements: [PREF-07]

must_haves:
  truths:
    - "Daily summarization batch reads depth_preference and judgment_angles from config/preferences.json before filling the summarize prompt"
    - "SKILL.md Processing Phase step 4 describes variable-depth summaries, not fixed 2-3 sentence output"
    - "output-templates.md Output Control Parameters table reflects depth_preference-dependent summary length"
  artifacts:
    - path: "references/processing-instructions.md"
      provides: "Daily summarization preference injection step"
      contains: "depth_preference"
    - path: "SKILL.md"
      provides: "Updated Processing Phase step 4 description"
      contains: "depth_preference"
    - path: "references/output-templates.md"
      provides: "Variable summary length parameter"
      contains: "depth_preference-dependent"
  key_links:
    - from: "references/processing-instructions.md Section 1"
      to: "config/preferences.json"
      via: "preference read step 1.5"
      pattern: "Read.*config/preferences\\.json.*depth_preference"
    - from: "references/processing-instructions.md Section 1"
      to: "references/prompts/summarize.md"
      via: "placeholder injection"
      pattern: "depth_preference.*judgment_angles.*summarize"
---

<objective>
Wire depth_preference and judgment_angles through the daily summarization path so saved preference fields actually shape daily output end-to-end. Close PREF-07, MISSING-01, and BROKEN-01 from the v1.0 audit.

Purpose: The summarize.md prompt already has {depth_preference} and {judgment_angles} placeholders, and preferences.json already stores these fields. The weekly report path already reads and injects them. But the daily summarization batch in processing-instructions.md Section 1 never reads preferences -- so daily summaries are always default depth. This plan adds the missing consumer wiring and aligns all documentation.

Output: Updated processing-instructions.md, SKILL.md, and output-templates.md with consistent variable-depth daily summarization behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-daily-depth-control-wiring/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preference injection step to daily Summarization Batch</name>
  <files>references/processing-instructions.md</files>
  <read_first>
    - references/processing-instructions.md (current Summarization Batch section, lines 182-205)
    - references/prompts/summarize.md (verify placeholders {depth_preference} and {judgment_angles} exist)
    - config/preferences.json (verify depth_preference and judgment_angles fields exist)
  </read_first>
  <action>
Edit references/processing-instructions.md Section 1 "Summarization Batch" (currently lines 182-197). Insert a new step 1.5 between step 1 (Load prompt) and step 2 (Fill batch data), and update step 2 numbering:

Current steps 1-5 become:

```
1. **Load prompt**: Read `references/prompts/summarize.md`
1.5. **Load depth preferences**: Read `config/preferences.json`. Extract `depth_preference` (default `"moderate"` if absent) and `judgment_angles` (default `[]` if absent). Fill the prompt's User Preferences Context section: replace `{depth_preference}` and `{judgment_angles}` placeholders.
2. **Fill batch data**: For each item, format into the input template:
   ```
   ID: {item.id}
   Title: {item.title}
   Source: {item.source_id}
   Content: {item.content_snippet}
   ---
   ```
3. **Process**: Use the filled prompt template to generate summaries
4. **Parse response**: Expect a JSON array. Match each result to its item by `id`
5. **Update items**: Set `content_summary` from the summarization result
```

This mirrors the established pattern from Section 7 (Weekly Report) step 5, which already reads these same preference fields from preferences.json for the weekly path.

IMPORTANT: Do NOT modify any other section of processing-instructions.md. Only the Summarization Batch subsection of Section 1.
  </action>
  <verify>
    <automated>grep -n "depth_preference" references/processing-instructions.md | grep -i "summariz"</automated>
  </verify>
  <acceptance_criteria>
    - references/processing-instructions.md contains the string "Load depth preferences" within the Summarization Batch section
    - references/processing-instructions.md contains `Read \`config/preferences.json\`` in the new step 1.5
    - references/processing-instructions.md contains `default \`"moderate"\` if absent` in step 1.5
    - references/processing-instructions.md contains `default \`[]\` if absent` in step 1.5
    - references/processing-instructions.md contains `{depth_preference}` and `{judgment_angles}` placeholder references in step 1.5
    - The existing steps (Load prompt, Fill batch data, Process, Parse response, Update items) are all still present
    - No other sections of processing-instructions.md are modified
  </acceptance_criteria>
  <done>Daily summarization batch has a preference-read step that extracts depth_preference and judgment_angles from preferences.json and injects them into the summarize.md prompt placeholders, with backward-compatible defaults for missing fields.</done>
</task>

<task type="auto">
  <name>Task 2: Align SKILL.md and output-templates.md with variable-depth behavior</name>
  <files>SKILL.md, references/output-templates.md</files>
  <read_first>
    - SKILL.md (full file -- verify Processing Phase step 4 current wording and overall word budget)
    - references/output-templates.md (Output Control Parameters table, around lines 80-88)
  </read_first>
  <action>
Two file edits:

**SKILL.md** -- Processing Phase step 4 (currently line 31):

Change FROM:
```
4. **Summarize batch**: Group 5-10 items per LLM call. Generate 2-3 sentence Chinese summary.
```

Change TO:
```
4. **Summarize batch**: Group 5-10 items per LLM call. Read `depth_preference` and `judgment_angles` from `config/preferences.json`, inject into `references/prompts/summarize.md`. Generate Chinese summary at configured depth.
```

This keeps the description compact (avoids SKILL.md word budget overflow) while accurately reflecting variable-depth behavior. Do NOT change any other line in SKILL.md.

**references/output-templates.md** -- Output Control Parameters table (currently line 85):

Change FROM:
```
| Summary length | 2-3 sentences | Per-item summary length |
```

Change TO:
```
| Summary length | depth_preference-dependent | brief=1 sentence, moderate=2-3, detailed=3-5, technical=3-5+specs |
```

Do NOT change any other row in the table or any other section of output-templates.md.
  </action>
  <verify>
    <automated>grep "depth_preference" SKILL.md && grep "depth_preference-dependent" references/output-templates.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md line containing "Summarize batch" includes the string "depth_preference"
    - SKILL.md line containing "Summarize batch" includes the string "judgment_angles"
    - SKILL.md line containing "Summarize batch" does NOT contain the string "2-3 sentence"
    - SKILL.md line containing "Summarize batch" includes "config/preferences.json"
    - references/output-templates.md Output Control Parameters table row for Summary length contains "depth_preference-dependent"
    - references/output-templates.md contains "brief=1 sentence, moderate=2-3, detailed=3-5, technical=3-5+specs"
    - No other lines in SKILL.md are changed
    - No other rows in output-templates.md table are changed
  </acceptance_criteria>
  <done>SKILL.md describes daily summaries as preference-driven variable depth (not fixed 2-3 sentences), and output-templates.md reflects depth_preference-dependent summary length with all four depth levels documented.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full daily depth control E2E path:

1. `config/preferences.json` has `depth_preference` and `judgment_angles` fields (already exists from Phase 3)
2. `references/processing-instructions.md` Section 1 Summarization Batch reads these fields and injects into summarize.md (Task 1)
3. `references/prompts/summarize.md` has `{depth_preference}` and `{judgment_angles}` placeholders with depth-adjusted rules (already exists from Phase 3)
4. `SKILL.md` Processing Phase step 4 describes variable-depth behavior (Task 2)
5. `references/output-templates.md` reflects variable summary length (Task 2)

Automated checks:
```bash
# Verify preference injection in daily summarization batch
grep -A3 "Load depth preferences" references/processing-instructions.md

# Verify SKILL.md no longer says fixed "2-3 sentence"
grep "Summarize batch" SKILL.md | grep -v "2-3 sentence"

# Verify output-templates.md updated
grep "depth_preference-dependent" references/output-templates.md

# Verify weekly path still intact (no regression)
grep "depth_preference" references/processing-instructions.md | grep -c "preferences.json"
# Should return 2 (one for daily Section 1, one for weekly Section 7)
```
</verification>

<success_criteria>
- PREF-07 gap closed: daily summarization consumer wires depth_preference and judgment_angles end-to-end
- MISSING-01 resolved: daily summarize path reads and injects preference depth and angles
- BROKEN-01 resolved: full E2E flow works -- preferences.json -> processing-instructions.md -> summarize.md -> variable-depth output
- Backward compatibility preserved: "moderate" depth produces identical 2-3 sentence output (no behavior change for default users)
- No SKILL.md word budget overflow
</success_criteria>

<output>
After completion, create `.planning/phases/05-daily-depth-control-wiring/05-01-SUMMARY.md`
</output>

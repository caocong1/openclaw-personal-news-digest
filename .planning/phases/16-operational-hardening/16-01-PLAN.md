---
phase: 16-operational-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dedup-index-rebuild.sh
  - scripts/diagnostics.sh
  - scripts/health-check.sh
  - scripts/data-archive.sh
  - scripts/lib/dedup_tools.py
  - scripts/lib/archive_tools.py
  - scripts/lib/diag_tools.py
  - scripts/lib/health_tools.py
  - scripts/lib/jsonl_tools.py
  - references/data-models.md
  - SKILL.md
autonomous: true
requirements: [HARD-01, HARD-03]

must_haves:
  truths:
    - "All inline python3 -c snippets in operational scripts are replaced with heredoc Python calling scripts/lib/ modules"
    - "scripts/source-status.sh remains the reference model for the heredoc pattern"
    - "pipeline_state enum in DailyMetrics distinguishes success-empty, failed-no-scan, and partial-degraded"
    - "HARD-03 distinguishes three silent-failure modes that all previously produced item_count: 0"
  artifacts:
    - path: "scripts/lib/dedup_tools.py"
      provides: "Auditable dedup index rebuild operations extracted from dedup-index-rebuild.sh"
      contains: "def rebuild_index"
    - path: "scripts/lib/archive_tools.py"
      provides: "TTL-based cleanup operations extracted from data-archive.sh"
      contains: "def cleanup_dedup_index"
    - path: "scripts/lib/diag_tools.py"
      provides: "Diagnostics data gathering extracted from diagnostics.sh"
      contains: "def latest_metrics"
    - path: "scripts/lib/health_tools.py"
      provides: "Health check logic extracted from health-check.sh, including pipeline_state determination"
      contains: "def determine_pipeline_state"
    - path: "references/data-models.md"
      provides: "DailyMetrics schema updated with pipeline_state enum"
      contains: "pipeline_state"
  key_links:
    - from: "scripts/dedup-index-rebuild.sh"
      to: "scripts/lib/dedup_tools.py"
      via: "Replaces inline python3 -c with heredoc python3 - calling dedup_tools module"
      pattern: "python3 - \"$BASE_DIR\""
    - from: "scripts/health-check.sh"
      to: "scripts/lib/health_tools.py"
      via: "Replaces inline health-check Python with heredoc calling health_tools.determine_pipeline_state"
      pattern: "determine_pipeline_state"
    - from: "references/data-models.md"
      to: "SKILL.md Output Phase"
      via: "pipeline_state field written by Output Phase step 7 before metrics flush"
      pattern: "pipeline_state"
---

<objective>
Extract all brittle inline `python3 -c` snippets from operational scripts into auditable `scripts/lib/` Python modules using the heredoc pattern pioneered by `scripts/source-status.sh`. Also add an explicit `pipeline_state` enum to DailyMetrics so operators can distinguish success-empty, failed-no-scan, and partial-degraded outcomes that all previously produced `item_count: 0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-operational-hardening/16-RESEARCH.md
@scripts/source-status.sh
@scripts/dedup-index-rebuild.sh
@scripts/diagnostics.sh
@scripts/health-check.sh
@scripts/data-archive.sh
@references/data-models.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/lib/ directory with shared jsonl_tools.py foundation</name>
  <files>scripts/lib/jsonl_tools.py</files>
  <read_first>scripts/source-status.sh, .planning/phases/16-operational-hardening/16-RESEARCH.md</read_first>
  <action>
1. Create directory `scripts/lib/` if it does not exist.

2. Create `scripts/lib/jsonl_tools.py` as the shared JSONL processing foundation. This module is imported by all other lib modules. Define these functions:
   - `def atomic_write_jsonl(path: str, entries: list[dict]) -> None`: Write a JSONL file atomically using `.tmp` rename pattern.
   - `def read_jsonl(path: str) -> list[dict]`: Read a JSONL file and return a list of dicts.
   - `def append_jsonl(path: str, entry: dict) -> None`: Append a single JSON dict as a newline to a JSONL file atomically (write .tmp, then rename).
   - `def latest_jsonl_dir(dir_path: str, pattern: str) -> tuple[str, list[dict]] | None`: Find the most recent file matching `daily-*.json` in `dir_path`, return (path, parsed entries) or None. Searches up to 7 days back.

3. Use `from __future__ import annotations` and `import json, os, glob` at module top. All functions include type hints. Entry point guard: `if __name__ == "__main__": pass` (no execution on direct run).

4. Verify the module is importable: `python3 -c "from scripts.lib.jsonl_tools import atomic_write_jsonl, read_jsonl, append_jsonl, latest_jsonl_dir; print('PASS')"`
</action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0, '.'); from scripts.lib.jsonl_tools import atomic_write_jsonl, read_jsonl, append_jsonl, latest_jsonl_dir; print('PASS')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/jsonl_tools.py` exists and is valid Python 3
    - Module defines `atomic_write_jsonl`, `read_jsonl`, `append_jsonl`, `latest_jsonl_dir` with type hints
    - `python3 -c "from scripts.lib.jsonl_tools import ..."` succeeds with no errors
  </acceptance_criteria>
  <done>Shared JSONL foundation module exists under scripts/lib/.</done>
</task>

<task type="auto">
  <name>Task 2: Extract inline Python from dedup-index-rebuild.sh into scripts/lib/dedup_tools.py</name>
  <files>scripts/dedup-index-rebuild.sh, scripts/lib/dedup_tools.py</files>
  <read_first>scripts/dedup-index-rebuild.sh, scripts/source-status.sh, .planning/phases/16-operational-hardening/16-RESEARCH.md</read_first>
  <action>
1. Create `scripts/lib/dedup_tools.py` with:
   - `def rebuild_index(base_dir: str, days: int = 7) -> dict`: Replaces lines 37-49 of dedup-index-rebuild.sh. Scans `data/news/YYYY-MM-DD.jsonl` files from last `days` days. For each line, reads `id`, `source_id`, `fetched_at` fields. Writes entries to `data/news/dedup-index.json` atomically.
   - `def count_index_entries(index_path: str) -> int`: Reads dedup-index.json and returns entry count.
   - Both functions use `sys.argv` access pattern compatible with heredoc invocation.

2. Modify `scripts/dedup-index-rebuild.sh`:
   - Replace the inline `python3 -c` on lines 37-49 with a heredoc call: `python3 - "$BASE_DIR" "$DAYS" <<'PY'` importing and calling `dedup_tools.rebuild_index(sys.argv[1], int(sys.argv[2]))`.
   - Replace the inline `python3 -c` on line 57 (count entries) with a call to `dedup_tools.count_index_entries("$INDEX_FILE")`.
   - Remove the `TEMP_FILE` variable from bash -- the temp file handling moves entirely into the Python module.
   - Keep all bash control flow (for loop over files, counter updates) -- only the Python logic is extracted.
   - The `set -euo pipefail` and all bash variable declarations remain.

3. The heredoc call must use the pattern: `python3 - "$BASE_DIR" "$DAYS" <<'PY'` then `import sys, os, json` inside the PY block. Do NOT use backtick subshells or `$(...)`.

4. After modification, `bash scripts/dedup-index-rebuild.sh` must produce the same output as before (same echo lines for file/entry counts).
</action>
  <verify>
    <automated>rg -n "python3 -c" scripts/dedup-index-rebuild.sh | grep -v "^#" && echo "FAIL: inline python3 -c still present" || echo "PASS: no inline python3 -c"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dedup-index-rebuild.sh` has zero inline `python3 -c` snippets
    - `scripts/lib/dedup_tools.py` exists with `rebuild_index` and `count_index_entries` functions
    - `scripts/dedup-index-rebuild.sh` uses heredoc `<<'PY'` pattern for Python invocation
    - Running `bash scripts/dedup-index-rebuild.sh` completes without error (or with the same error as before if data is absent)
  </acceptance_criteria>
  <done>dedup-index-rebuild.sh uses auditable scripts/lib/ modules instead of inline Python.</done>
</task>

<task type="auto">
  <name>Task 3: Extract inline Python from health-check.sh into scripts/lib/health_tools.py</name>
  <files>scripts/health-check.sh, scripts/lib/health_tools.py</files>
  <read_first>scripts/health-check.sh, scripts/source-status.sh, .planning/phases/16-operational-hardening/16-RESEARCH.md</read_first>
  <action>
1. Create `scripts/lib/health_tools.py` with these functions:
   - `def determine_pipeline_state(sources_attempted: int, sources_success: int, sources_failed: int, items_fetched: int, items_qualifying: int, circuit_breaker: bool) -> str`: Implements the three-state enum:
     - `"failed-no-scan"` if `sources_attempted > 0 and sources_success == 0`
     - `"partial-degraded"` if `sources_failed > 0 or circuit_breaker`
     - `"success-empty"` if `items_fetched > 0 and items_qualifying == 0`
     - `"success"` otherwise
   - `def check_dedup_consistency(base_dir: str) -> dict`: Replaces the orphaned dedup-index check in health-check.sh. Returns `{"orphaned": int, "total": int, "pct": float}`.
   - `def check_source_concentration(base_dir: str) -> list[tuple[str, float]]`: Replaces inline concentration check. Returns list of (source_id, pct) where pct > 50.
   - `def check_long_stable_events(base_dir: str, threshold_days: int = 14) -> list[tuple[str, str, int]]`: Replaces long-stable events check. Returns list of (event_id, title, days).
   - `def check_source_success_rates(base_dir: str, days: int = 7) -> list[tuple[str, float, int, int]]`: Replaces weekly source success rate check. Returns list of (source_id, rate_pct, successes, totals).

2. Modify `scripts/health-check.sh`:
   - Replace each inline `python3 -c` block (health-checks 7-17) with heredoc Python calls to `health_tools` module functions.
   - Keep the bash `if [ "$MODE" = "weekly" ]; then` conditional wrapper for checks 12-17.
   - Keep the ALERT/WARN/INFO/OK prefix output format exactly as-is.
   - Keep the ERRORS/ALERTS/WARNINGS bash counters -- Python functions return structured data, bash handles formatting and counting.

3. Do NOT change the output format or prefix strings (ALERT:, WARN:, INFO:, OK:). Do NOT change the --mode flag handling. Do NOT change the Summary section.

4. After modification, `bash scripts/health-check.sh` (daily mode) must output the same prefix lines as before.
</action>
  <verify>
    <automated>rg -n "python3 -c" scripts/health-check.sh | grep -v "^#" | grep -v "python3 -c .import json. json.load" && echo "FAIL" || echo "PASS: no inline python3 -c snippets"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/health-check.sh` has zero inline `python3 -c` snippets (the `python3 -c "import json; json.load(...)"` pattern on lines 53-54 and 68 is a one-liner read check -- replace it with a heredoc call to a health_tools function)
    - `scripts/lib/health_tools.py` exists with `determine_pipeline_state`, `check_dedup_consistency`, `check_source_concentration`, `check_long_stable_events`, `check_source_success_rates`
    - `determine_pipeline_state` returns exactly "success", "success-empty", "failed-no-scan", or "partial-degraded"
    - `bash scripts/health-check.sh` completes without syntax error
  </acceptance_criteria>
  <done>health-check.sh uses auditable scripts/lib/ modules instead of inline Python.</done>
</task>

<task type="auto">
  <name>Task 4: Extract inline Python from diagnostics.sh and data-archive.sh into scripts/lib/</name>
  <files>scripts/diagnostics.sh, scripts/data-archive.sh, scripts/lib/diag_tools.py, scripts/lib/archive_tools.py</files>
  <read_first>scripts/diagnostics.sh, scripts/data-archive.sh, scripts/source-status.sh</read_first>
  <action>
1. Create `scripts/lib/diag_tools.py`:
   - `def latest_metrics(base_dir: str) -> dict | None`: Find and return the most recent `daily-*.json` as a parsed dict. Searches up to 7 days back.
   - `def latest_metrics_date(base_dir: str) -> str | None`: Return the date string (YYYY-MM-DD) of the latest metrics file.
   - `def per_source_status(base_dir: str) -> dict[str, dict]`: Return per-source status from today's or most recent metrics.
   - `def digest_history_summary(base_dir: str) -> list[dict]`: Return the last 5 runs from `data/digest-history.json`.
   - `def active_events_summary(base_dir: str) -> dict`: Return counts of active/stable/total events from `data/events/active.json`.
   - `def budget_status(base_dir: str) -> dict`: Return budget counters and effective usage from `config/budget.json`.

2. Create `scripts/lib/archive_tools.py`:
   - `def cleanup_dedup_index(path: str, ttl_days: int = 7) -> int`: Implements the 7-day TTL dedup-index cleanup from data-archive.sh section 2. Returns number of entries removed. Uses atomic write via .tmp rename.
   - `def cleanup_feedback(path: str, ttl_days: int = 90) -> int`: Implements the 90-day TTL feedback cleanup from section 3.
   - `def cleanup_cache_entry(cache_path: str, ttl_days: int = 7) -> int`: Implements the 7-day TTL cache cleanup from section 4. Works on classify-cache.json and summary-cache.json.
   - All functions use `datetime.now(timezone.utc)` for UTC timestamps.

3. Modify `scripts/diagnostics.sh`:
   - Replace all inline `python3 -c` blocks (sections 1-6) with heredoc Python calls to `diag_tools` module functions.
   - Keep the section headers (--- 1. Pipeline Status --- etc.) and `echo` commands.
   - Bash handles section headers and descriptive labels; Python returns structured data.

4. Modify `scripts/data-archive.sh`:
   - Replace inline `python3 -c` blocks in sections 2, 3, 4 with heredoc calls to `archive_tools` functions.
   - Keep bash counters (ARCHIVED_JSONL, DEDUP_REMOVED, etc.) and the file-based operations (find ... -mtime +30, rm, etc.).
   - Keep the echo summary lines at the end.

5. Both scripts must still use `set -euo pipefail` at the top.
</action>
  <verify>
    <automated>(rg -n "python3 -c" scripts/diagnostics.sh | grep -v "^#" | grep -v "python3 -c .import json.") && echo "FAIL" || echo "PASS diagnostics.sh"; (rg -n "python3 -c" scripts/data-archive.sh | grep -v "^#" | grep -v "python3 -c .import json.") && echo "FAIL" || echo "PASS data-archive.sh"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/diagnostics.sh` has zero inline `python3 -c` snippets
    - `scripts/data-archive.sh` has zero inline `python3 -c` snippets
    - `scripts/lib/diag_tools.py` exists with `latest_metrics`, `latest_metrics_date`, `per_source_status`, `digest_history_summary`, `active_events_summary`, `budget_status`
    - `scripts/lib/archive_tools.py` exists with `cleanup_dedup_index`, `cleanup_feedback`, `cleanup_cache_entry`
    - Both scripts complete without syntax error when run against an empty or populated data directory
  </acceptance_criteria>
  <done>diagnostics.sh and data-archive.sh use auditable scripts/lib/ modules instead of inline Python.</done>
</task>

<task type="auto">
  <name>Task 5: Add pipeline_state enum to DailyMetrics schema in data-models.md</name>
  <files>references/data-models.md</files>
  <read_first>references/data-models.md, .planning/phases/16-operational-hardening/16-RESEARCH.md</read_first>
  <action>
1. In `references/data-models.md`, find the `## DailyMetrics` section.

2. Add a new top-level field to the DailyMetrics schema:
   ```
   pipeline_state: "success" | "success-empty" | "failed-no-scan" | "partial-degraded"
   ```
   Document it with this description:
   - `"success"`: Normal run with qualifying items selected for output.
   - `"success-empty"`: Collection ran (items fetched > 0) but no items qualified after scoring/filtering.
   - `"failed-no-scan"`: Sources were attempted but all failed; no collection occurred.
   - `"partial-degraded"`: Some sources succeeded and some failed, or the circuit breaker triggered.

3. Update the Schema Version Registry if a version field exists on DailyMetrics, or add a note that this field was added in Phase 16.

4. In `SKILL.md`, update the Output Phase step 7 (Write metrics):
   - Before writing `{baseDir}/data/metrics/daily-YYYY-MM-DD.json`, compute `pipeline_state` using the same enum logic defined in `scripts/lib/health_tools.py::determine_pipeline_state`.
   - Add the computed `pipeline_state` field to the metrics object before serialization.
   - The field is added to the existing metrics write -- no new write call needed.

5. Do NOT change any other fields in DailyMetrics. Do NOT change the metrics file path or naming convention.
</action>
  <verify>
    <automated>rg -n "pipeline_state" references/data-models.md && rg -n "pipeline_state" SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `references/data-models.md` documents `pipeline_state` with all four enum values
    - `SKILL.md` Output Phase step 7 mentions `pipeline_state` computation before metrics flush
    - The four-state enum is consistent between data-models.md and scripts/lib/health_tools.py
  </acceptance_criteria>
  <done>DailyMetrics schema includes pipeline_state so operators can distinguish the three silent-failure modes from a genuine success-empty run.</done>
</task>

</tasks>

<verification>
- `rg -n "python3 -c" scripts/dedup-index-rebuild.sh scripts/diagnostics.sh scripts/health-check.sh scripts/data-archive.sh | grep -v "^#" | grep -v "python3 -c .import json. json.load" | head -5` returns empty (no inline Python remaining)
- `python3 -c "import sys; sys.path.insert(0, '.'); from scripts.lib.dedup_tools import rebuild_index; from scripts.lib.health_tools import determine_pipeline_state; from scripts.lib.diag_tools import latest_metrics; from scripts.lib.archive_tools import cleanup_dedup_index; print('PASS')"` succeeds
- `rg -n "pipeline_state" references/data-models.md SKILL.md | head -10` returns matches
</verification>

<success_criteria>
- All four operational scripts (dedup-index-rebuild.sh, diagnostics.sh, health-check.sh, data-archive.sh) use heredoc Python calling scripts/lib/ modules -- zero inline `python3 -c` snippets remain
- `scripts/lib/` contains jsonl_tools.py, dedup_tools.py, archive_tools.py, diag_tools.py, health_tools.py
- `scripts/source-status.sh` remains the reference model and is not modified
- `pipeline_state` enum is documented in data-models.md and wired into SKILL.md Output Phase step 7
- Operators can distinguish `success-empty`, `failed-no-scan`, and `partial-degraded` from `item_count: 0` alone
</success_criteria>

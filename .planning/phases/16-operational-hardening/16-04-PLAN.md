---
phase: 16-operational-hardening
plan: "04"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - SKILL.md
  - scripts/health-check.sh
  - references/recovery-matrix.md
  - references/platform-verification.md
  - scripts/smoke-test.sh
autonomous: true
requirements: [OPER-02, OPER-05, OPER-06]

must_haves:
  truths:
    - "SKILL.md frontmatter declares _skill_version and minimum_openclaw_version"
    - "health-check.sh checks version drift and surfaces recovery hints for blocked runs"
    - "recovery-matrix.md documents cross-channel recovery actions (Web UI, terminal, Discord)"
    - "smoke-test.sh automates OPER-06 platform smoke tests"
  artifacts:
    - path: "SKILL.md"
      provides: "Skill version metadata in frontmatter"
      contains: "minimum_openclaw_version"
    - path: "scripts/health-check.sh"
      provides: "Version drift check integrated into health-check.sh"
      contains: "minimum_openclaw_version"
    - path: "references/recovery-matrix.md"
      provides: "Cross-channel recovery documentation for OPER-05"
      contains: "Web UI"
    - path: "references/platform-verification.md"
      provides: "OPER-06 smoke test refresh with automated test cases"
      contains: "smoke-test.sh"
    - path: "scripts/smoke-test.sh"
      provides: "Automated platform smoke tests for OPER-06"
      contains: "cron delivery"
  key_links:
    - from: "SKILL.md"
      to: "scripts/health-check.sh"
      via: "Health check reads minimum_openclaw_version from SKILL.md frontmatter"
      pattern: "minimum_openclaw_version"
    - from: "references/recovery-matrix.md"
      to: "scripts/health-check.sh"
      via: "Recovery hints in health check link to recovery-matrix.md"
      pattern: "recovery-matrix"
    - from: "references/platform-verification.md"
      to: "scripts/smoke-test.sh"
      via: "Automated smoke test script replaces manual platform-verification steps"
      pattern: "smoke-test"
---

<objective>
Add version metadata to SKILL.md (OPER-02), create cross-channel recovery documentation (OPER-05), and refresh and automate the platform smoke tests (OPER-06) so operators can detect CLI/docs drift, version mismatches, and live-run regressions before they silently erode operational confidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-operational-hardening/16-RESEARCH.md
@SKILL.md
@scripts/health-check.sh
@references/platform-verification.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version metadata to SKILL.md frontmatter and integrate into health-check.sh</name>
  <files>SKILL.md, scripts/health-check.sh</files>
  <read_first>SKILL.md, scripts/health-check.sh, .planning/phases/16-operational-hardening/16-RESEARCH.md</read_first>
  <action>
1. In SKILL.md, update the frontmatter to add:
   ```yaml
   _skill_version: "16.0.0"
   minimum_openclaw_version: "1.4.0"
   ```
   Keep all existing frontmatter fields (name, description, user-invocable, metadata). The new fields go after metadata.

2. In SKILL.md Operational Rules section, add a note: "Version metadata: SKILL.md frontmatter declares _skill_version (skill's own semver) and minimum_openclaw_version (minimum platform version). health-check.sh reads these fields and alerts on drift."

3. Modify `scripts/health-check.sh`:
   - Add a new check at the top of the Basic Checks section (after check 1, before check 2): **Version drift check**.
   - The check reads `_skill_version` and `minimum_openclaw_version` from SKILL.md frontmatter using a heredoc Python call.
   - It compares `minimum_openclaw_version` against a known baseline (1.4.0) or an env var `OPENCLAW_VERSION` if set.
   - Output: `OK: Skill version $SKILL_VERSION, OpenClaw version $OPENCLAW_VERSION` if compatible. `WARN: Skill version mismatch -- expected $EXPECTED, found $ACTUAL` if incompatible.
   - This check runs in both daily and weekly modes.

4. Also update the health check to surface recovery hints for blocked runs. When a blocked run is detected (e.g., all sources failed 2+ days), append a hint line: `HINT: See references/recovery-matrix.md for recovery actions.`

5. Do NOT change the existing ALERT/WARN/INFO/OK prefix format. Do NOT change the --mode flag handling. Do NOT change the summary exit code logic.

6. Verify: `rg -n "_skill_version|minimum_openclaw_version" SKILL.md scripts/health-check.sh | head -10`
</action>
  <verify>
    <automated>rg -n "_skill_version|minimum_openclaw_version" SKILL.md scripts/health-check.sh | head -10</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md frontmatter contains `_skill_version` and `minimum_openclaw_version`
    - scripts/health-check.sh has a version drift check that reads SKILL.md frontmatter
    - health-check.sh output includes recovery hints linking to recovery-matrix.md
    - health-check.sh --help or error output mentions the version check
  </acceptance_criteria>
  <done>SKILL.md declares version metadata and health-check.sh checks for version drift on every run.</done>
</task>

<task type="auto">
  <name>Task 2: Create references/recovery-matrix.md with cross-channel recovery documentation</name>
  <files>references/recovery-matrix.md</files>
  <read_first>.planning/phases/16-operational-hardening/16-RESEARCH.md, references/cron-configs.md</read_first>
  <action>
1. Create `references/recovery-matrix.md` with these sections:

   ## Recovery Matrix Overview
   - This document maps failure types to recovery actions across all three operator channels: Web UI, Terminal, and Discord.
   - Each row covers a distinct failure mode. Recovery actions are ordered by operator effort (easiest first).

   ## Recovery Matrix Table
   Include this exact table:

   | Failure Type | Web UI Action | Terminal Action | Discord Action |
   |---|---|---|---|
   | Lock stuck (pipeline hung) | Restart session via UI refresh | `rm data/.lock && bash scripts/health-check.sh` | Same as terminal |
   | All sources failed | Inspect sources via Sources panel | `bash scripts/source-status.sh && bash scripts/diagnostics.sh` | Same as terminal |
   | Budget exhausted (circuit breaker) | Check usage via UI | `cat config/budget.json` | Same as terminal |
   | Cron not firing | Check schedule via cron UI | `bash scripts/health-check.sh` (checks lock age) | Same as terminal |
   | Empty digest (expected) | N/A | `bash scripts/diagnostics.sh` | Same as terminal |
   | Degraded source | Enable/disable via Sources panel | `bash scripts/source-status.sh <source-id>` | Same as terminal |
   | Version drift detected | Check platform version | `bash scripts/health-check.sh` | Same as terminal |
   | Stale dedup-index | N/A | `bash scripts/dedup-index-rebuild.sh` | Same as terminal |
   | Pipeline state: failed-no-scan | Check sources online | Re-enable failed sources via sources.json | Same as terminal |
   | Pipeline state: partial-degraded | Check degraded sources | `bash scripts/source-status.sh` for degraded list | Same as terminal |
   | Provenance classification gap | N/A | `bash scripts/diagnostics.sh` (check provenance stats) | Same as terminal |
   | Run journal shows security events | N/A | `bash scripts/run-journal.sh query --severity security` | Same as terminal |

   ## Source Recovery
   - Auto-disable: Sources auto-disable after 3 consecutive failures or sustained quality drops. See `references/processing-instructions.md` Section 6.
   - Auto-recovery: Sources auto-recover after 3 consecutive successful runs.
   - Manual re-enable: Set `"enabled": true` in `config/sources.json` or use the activate-profile.sh script.

   ## Backlog Follow-up
   - Failure follow-ups are written to `data/backlog/failure-followups.jsonl` and optionally mirrored to the path set in `OPER_BACKLOG_PATH` in `config/preferences.json`.
   - Query the backlog: `bash scripts/run-journal.sh query --severity error`

   ## Version Recovery
   - If `health-check.sh` reports a version mismatch, update OpenClaw to `minimum_openclaw_version` or higher.
   - If the skill version (`_skill_version`) is outdated, pull the latest skill from the skill registry.

   ## Health Check Reference
   - Daily health check: `bash scripts/health-check.sh --mode daily`
   - Weekly health check: `bash scripts/health-check.sh --mode weekly`
   - Diagnostics: `bash scripts/diagnostics.sh`

2. Do NOT invent new failure types not in the table. Do NOT add platform-specific steps that cannot work across all three channels.

3. Verify: `python3 -c "import re; m=open('references/recovery-matrix.md').read(); assert 'Lock stuck' in m; assert 'Web UI' in m; assert 'Terminal Action' in m; print('PASS')"`
</action>
  <verify>
    <automated>python3 -c "m=open('references/recovery-matrix.md').read(); required=['Lock stuck','All sources failed','Budget exhausted','Web UI','Terminal Action','version drift','failed-no-scan','partial-degraded']; [__import__('sys').stdout.write('FAIL: missing: '+r+'\n') or exit(1) for r in required if r not in m]; print('PASS recovery-matrix.md')"</automated>
  </verify>
  <acceptance_criteria>
    - `references/recovery-matrix.md` exists with a table covering all 8+ failure types across Web UI, Terminal, and Discord
    - The table includes: lock stuck, all sources failed, budget exhausted, cron not firing, degraded source, version drift, failed-no-scan, partial-degraded
    - Source recovery section covers auto-disable and auto-recovery behavior
   - Backlog section references run-journal.sh query
   - Health check reference section lists daily and weekly modes
  </acceptance_criteria>
  <done>Cross-channel recovery documentation exists so operators can recover from failures in any channel they use.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh platform-verification.md and create scripts/smoke-test.sh</name>
  <files>references/platform-verification.md, scripts/smoke-test.sh</files>
  <read_first>references/platform-verification.md, .planning/phases/16-operational-hardening/16-RESEARCH.md</read_first>
  <action>
1. Create `scripts/smoke-test.sh`:
   - Usage: `bash scripts/smoke-test.sh [--mode full|quick] [base_dir]`
   - `--mode full` runs all smoke tests (default: full)
   - `--mode quick` runs only the essential 3 tests (file access, exec, timeout)
   - Exit code 0 if all tests pass, non-zero if any fail

   Implement these smoke test functions using heredoc Python calls:
   - `test_file_access()`: Check that SKILL.md exists and is readable, that config/, references/, and scripts/ directories exist.
   - `test_exec_permissions()`: Run `python3 -c "print('smoke-ok')"` via exec and verify output.
   - `test_python3_available()`: Verify `python3 --version` succeeds.
   - `test_cron_delivery()`: Register a one-shot test cron job (if cron tool available), wait, verify output file exists. If cron tool not available, skip this test and output "SKIP: cron tool not available in smoke-test context".
   - `test_timeout_behavior()`: Run a Python script that sleeps 2 seconds and verifies it completes (not a 5-min sleep in smoke test -- use 2s to keep smoke tests fast).
   - `test_empty_input_quality_gate()`: Verify that health-check.sh completes without error even when data directories are empty (run health-check.sh in a dry-run mode if available, or just verify the script syntax is valid).
   - `test_atomic_write()`: Verify that the atomic write pattern (.tmp then rename) works by testing a sample write.
   - `test_version_metadata()`: Verify SKILL.md frontmatter contains `_skill_version` and `minimum_openclaw_version`.
   - `test_jsonl_append()`: Verify run-journal.jsonl and failure-followups.jsonl are writable.

   Each test function returns "PASS", "FAIL: <reason>", or "SKIP: <reason>".
   The script outputs a summary table at the end: test name, result, time.

2. Update `references/platform-verification.md`:
   - Add a header note: "OPER-06 refresh (2026-04-03): This document was written for initial Phase 0 setup. It is now maintained as the authoritative OPER-06 smoke test reference. Prefer `scripts/smoke-test.sh` for automated execution."
   - Add a section "## OPER-06 Smoke Test Coverage" that maps each capability test to OPER-06 requirements:
     - Cron delivery -> OPER-06 "cron delivery"
     - Isolated session file access -> OPER-06 "isolated session loading"
     - Exec permissions -> OPER-06 "exec permissions"
     - Timeout behavior -> OPER-06 "timeout behavior"
     - Empty-input quality gate -> OPER-06 "empty-input quality gates"
   - Keep the existing 5 test procedures intact -- they remain as the authoritative manual test descriptions.
   - Add a "## Automated Smoke Tests" section that refers to `scripts/smoke-test.sh`.

3. Keep the existing PLAT-01 through PLAT-04 section structure intact. Do not remove existing content. The OPER-06 additions are additive.

4. Verify: `bash scripts/smoke-test.sh --mode quick . && echo "PASS smoke-test.sh quick mode"` completes without error.
</action>
  <verify>
    <automated>bash scripts/smoke-test.sh --mode quick . 2>&1 | tail -10 && echo "smoke-test.sh ran"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke-test.sh` exists and runs in --mode quick without error
    - `references/platform-verification.md` contains an OPER-06 section referencing smoke-test.sh
    - smoke-test.sh covers all 5 OPER-06 criteria: cron delivery, isolated session, exec permissions, timeout behavior, empty-input quality gate
    - smoke-test.sh output includes a summary table with PASS/FAIL/SKIP per test
  </acceptance_criteria>
  <done>OPER-06 smoke tests are automated via smoke-test.sh and the platform verification doc is refreshed.</done>
</task>

</tasks>

<verification>
- `rg -n "_skill_version|minimum_openclaw_version" SKILL.md | head -5`
- `rg -n "version drift|minimum_openclaw_version" scripts/health-check.sh | head -5`
- `python3 -c "m=open('references/recovery-matrix.md').read(); assert 'Lock stuck' in m; assert 'Web UI' in m; print('PASS')"`
- `bash scripts/smoke-test.sh --mode quick . 2>&1 | tail -5`
</verification>

<success_criteria>
- OPER-02: SKILL.md frontmatter declares `_skill_version` and `minimum_openclaw_version`
- OPER-02: health-check.sh checks version drift and surfaces recovery hints for blocked runs
- OPER-05: references/recovery-matrix.md documents all failure types across Web UI, terminal, and Discord channels
- OPER-05: CLI/docs parity is implicitly checked via recovery-matrix.md table coverage
- OPER-06: scripts/smoke-test.sh automates all OPER-06 criteria
- OPER-06: references/platform-verification.md is refreshed with OPER-06 section
</success_criteria>

---
phase: 12-interaction-surface-deployment-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/schedule-profiles.json
  - references/data-models.md
  - references/cron-configs.md
  - references/feedback-rules.md
  - SKILL.md
autonomous: true
requirements: [INTERACT-01, INTERACT-04]

must_haves:
  truths:
    - "Scheduling profiles live in repo state, not only in prose examples"
    - "At least one weekday-only profile and one custom-hours profile exist with concrete cron values"
    - "Natural-language intent recognition is centralized in references/feedback-rules.md"
    - "SKILL.md references the canonical intent table instead of duplicating trigger examples"
  artifacts:
    - path: "config/schedule-profiles.json"
      provides: "Named schedule profiles with active_profile and concrete cron expressions"
      contains: "\"weekday-only\""
    - path: "references/data-models.md"
      provides: "Schedule profile schema and field notes"
      contains: "ScheduleProfileConfig"
    - path: "references/cron-configs.md"
      provides: "Profile-aware schedule management commands"
      contains: "Schedule Profiles"
    - path: "references/feedback-rules.md"
      provides: "Canonical NL intent recognition table"
      contains: "Intent Recognition Table"
    - path: "SKILL.md"
      provides: "Thin routing layer pointing to canonical intent table"
      contains: "Intent Recognition Table"
  key_links:
    - from: "SKILL.md"
      to: "references/feedback-rules.md"
      via: "SKILL routes all user messages through the canonical intent table"
      pattern: "Intent Recognition Table"
    - from: "references/cron-configs.md"
      to: "config/schedule-profiles.json"
      via: "Schedule profile docs map directly to concrete profile names"
      pattern: "active_profile"
---

<objective>
Introduce named scheduling profiles and move natural-language intent recognition into one canonical table so users can configure deployment timing through SKILL commands without scattering routing logic across the repo.

Purpose: Make scheduling operator-friendly and keep intent routing maintainable as Phase 12 adds new commands.
Output: Schedule profile config, schedule profile schema/docs, canonical intent table, slimmer SKILL routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-interaction-surface-deployment-ux/12-RESEARCH.md
@.planning/phases/12-interaction-surface-deployment-ux/12-VALIDATION.md
@references/cron-configs.md
@references/feedback-rules.md
@references/data-models.md
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schedule profile source of truth and document its schema</name>
  <files>config/schedule-profiles.json, references/data-models.md, references/cron-configs.md</files>
  <read_first>references/cron-configs.md, references/data-models.md, SKILL.md</read_first>
  <action>
1. Create `config/schedule-profiles.json` with `_schema_v: 1`, `active_profile`, and a `profiles` object. Add exactly these three named profiles with concrete values:

```json
{
  "_schema_v": 1,
  "active_profile": "daily-default",
  "profiles": {
    "daily-default": {
      "description": "Default all-days schedule",
      "timezone": "Asia/Shanghai",
      "daily_digest": { "enabled": true, "job_name": "news-daily-digest", "expr": "0 8 * * *" },
      "quick_check": { "enabled": true, "job_name": "news-quick-check", "expr": "0 */2 * * *" },
      "weekly_health": { "enabled": true, "job_name": "weekly-health-inspection", "expr": "0 3 * * 1" },
      "weekly_report": { "enabled": true, "job_name": "news-weekly-report", "expr": "0 20 * * 0" }
    },
    "weekday-only": {
      "description": "Weekdays only with business-hours quick checks",
      "timezone": "Asia/Shanghai",
      "daily_digest": { "enabled": true, "job_name": "news-daily-digest", "expr": "0 8 * * 1-5" },
      "quick_check": { "enabled": true, "job_name": "news-quick-check", "expr": "0 8-20/2 * * 1-5" },
      "weekly_health": { "enabled": true, "job_name": "weekly-health-inspection", "expr": "0 3 * * 1" },
      "weekly_report": { "enabled": true, "job_name": "news-weekly-report", "expr": "0 20 * * 0" }
    },
    "custom-hours": {
      "description": "Editable example profile for user-defined hours",
      "timezone": "Asia/Shanghai",
      "daily_digest": { "enabled": true, "job_name": "news-daily-digest", "expr": "30 9 * * 1-5" },
      "quick_check": { "enabled": true, "job_name": "news-quick-check", "expr": "0 9,13,17 * * 1-5" },
      "weekly_health": { "enabled": true, "job_name": "weekly-health-inspection", "expr": "0 3 * * 1" },
      "weekly_report": { "enabled": true, "job_name": "news-weekly-report", "expr": "0 20 * * 0" }
    }
  }
}
```

2. In `references/data-models.md`, add a new section titled `## ScheduleProfileConfig` documenting the schema above. Include field notes for:
   - `_schema_v`
   - `active_profile`
   - `profiles.<name>.timezone`
   - each job object field: `enabled`, `job_name`, `expr`
   - requirement that profile names are stable IDs used by SKILL commands

3. In `references/cron-configs.md`, add a new `## Schedule Profiles` section near the top that:
   - declares `config/schedule-profiles.json` as the source of truth
   - lists `daily-default`, `weekday-only`, and `custom-hours`
   - explains that each profile maps onto the existing four cron jobs by `job_name`
   - gives an explicit apply flow:
     - read the active profile
     - use `cron get <job_name>` to inspect current state
     - use `cron create` or the platform's update path with the profile's `expr`
     - use `cron disable <job_name>` when `enabled` is `false`

4. In `references/cron-configs.md`, add a compact management table for scheduling commands:
   - "list schedule profiles"
   - "show active schedule profile"
   - "activate weekday-only profile"
   - "set custom-hours digest to 09:30"
   - "set custom-hours quick check to 09,13,17"

Do not invent extra profile names or alternate field shapes. Use the exact field names and cron expressions above.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('config/schedule-profiles.json')); assert d['active_profile']=='daily-default'; assert 'weekday-only' in d['profiles']; assert d['profiles']['custom-hours']['daily_digest']['expr']=='30 9 * * 1-5'; print('PASS')" 2>&1 && rg -n "ScheduleProfileConfig|Schedule Profiles|active_profile" references/data-models.md references/cron-configs.md</automated>
  </verify>
  <acceptance_criteria>
    - config/schedule-profiles.json exists and is valid JSON
    - config/schedule-profiles.json contains `daily-default`, `weekday-only`, and `custom-hours`
    - config/schedule-profiles.json contains `news-daily-digest`, `news-quick-check`, `weekly-health-inspection`, and `news-weekly-report`
    - references/data-models.md contains `## ScheduleProfileConfig`
    - references/data-models.md contains `active_profile`
    - references/cron-configs.md contains `## Schedule Profiles`
    - references/cron-configs.md contains `weekday-only`
    - references/cron-configs.md contains `set custom-hours digest to 09:30`
  </acceptance_criteria>
  <done>Schedule profiles are stored in repo config with concrete cron expressions and documented as a first-class contract.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize intent recognition and slim SKILL routing</name>
  <files>references/feedback-rules.md, SKILL.md, references/cron-configs.md</files>
  <read_first>references/feedback-rules.md, SKILL.md, references/cron-configs.md, references/collection-instructions.md, references/processing-instructions.md</read_first>
  <action>
1. In `references/feedback-rules.md`, add a new top-level section titled `## Intent Recognition Table` near the top of the file, before feedback-specific procedures. Add a markdown table with exactly these route categories and guidance:

| Intent | Example phrases | Route |
|--------|-----------------|-------|
| `schedule_management` | `set digest to weekdays`, `move daily digest to 9:30`, `change quick check hours`, `activate custom-hours profile` | `references/cron-configs.md` Schedule Profiles section |
| `source_status` | `source status`, `how is 36Kr doing`, `which sources are failing`, `show source health` | Phase 12 source-status command path |
| `source_management` | `add source`, `disable Hacker News`, `increase 36Kr weight` | `references/collection-instructions.md` Source Management Commands |
| `feedback` | `more AI news`, `less gaming`, `trust this source`, `like #3` | Feedback Type Mapping in this file |
| `preference_query` | `what have you learned about me`, `show my preferences`, `my interests` | Preference Visualization in this file |
| `history_query` | `latest news`, `AI news this week`, `what happened with X`, `what did I miss` | `references/prompts/history-query.md` plus `references/processing-instructions.md` Section 8 |
| `diagnostics` | `system status`, `health check`, `diagnostics` | `bash scripts/diagnostics.sh {baseDir}` |
| `general` | anything else | General helpful response |

2. Add a short note under the table stating: `This table is the canonical routing source for SKILL.md. Do not duplicate trigger examples elsewhere.`

3. In `SKILL.md`, replace the current `## User Commands` numbered list with a thinner version that:
   - starts by saying: `Use {baseDir}/references/feedback-rules.md "Intent Recognition Table" as the canonical routing layer for user messages.`
   - keeps one flat list of route destinations only, without repeating example phrases
   - includes a new first route for schedule management pointing to `references/cron-configs.md`
   - includes a distinct route for source status commands
   - preserves existing routes for source management, feedback, preference query, history query, diagnostics, and general

4. In `SKILL.md`, make the source-status route concrete:
   - if the command is broad (`source status`, `show source health`), route to the source-status command path defined in Phase 12
   - if the command names a specific source, still route to the source-status path rather than the generic history-query path

5. In `references/cron-configs.md`, add a short note under the new Schedule Profiles section saying the schedule-management intent in `references/feedback-rules.md` is the canonical entry point for profile selection and updates.

Keep `SKILL.md` readable. The goal is to remove duplicated trigger logic, not to hide routes.
  </action>
  <verify>
    <automated>rg -n "Intent Recognition Table|schedule_management|source_status|canonical routing source" references/feedback-rules.md SKILL.md references/cron-configs.md</automated>
  </verify>
  <acceptance_criteria>
    - references/feedback-rules.md contains `## Intent Recognition Table`
    - references/feedback-rules.md contains `schedule_management` and `source_status`
    - references/feedback-rules.md contains `Do not duplicate trigger examples elsewhere`
    - SKILL.md contains `Intent Recognition Table`
    - SKILL.md contains a route for schedule management
    - SKILL.md contains a distinct route for source status
    - references/cron-configs.md references the intent table as the scheduling entry point
  </acceptance_criteria>
  <done>Intent routing is centralized in one table, and SKILL.md becomes a thin dispatcher instead of a duplicate source of truth.</done>
</task>

</tasks>

<verification>
- `python3 -c "import json; json.load(open('config/schedule-profiles.json'))"` succeeds
- `rg -n "Intent Recognition Table" references/feedback-rules.md SKILL.md` returns matches in both files
- `rg -n "weekday-only|custom-hours" config/schedule-profiles.json references/cron-configs.md` returns matches
- `rg -n "source_status|schedule_management" references/feedback-rules.md` returns both rows
</verification>

<success_criteria>
- Scheduling profiles are repo-backed, not prose-only
- Users can choose between daily-default, weekday-only, and custom-hours profiles
- Canonical intent recognition lives in references/feedback-rules.md
- SKILL.md no longer duplicates trigger examples for all command categories
- INTERACT-01 and INTERACT-04 are both satisfied by concrete docs/config artifacts
</success_criteria>

---
phase: 12-interaction-surface-deployment-ux
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - references/processing-instructions.md
  - references/output-templates.md
  - SKILL.md
  - data/fixtures/events-active-dense-day.json
autonomous: true
requirements: [INTERACT-05]

must_haves:
  truths:
    - "Dense event days collapse based on same-day entry count, not total event length"
    - "Collapse is presentation-only and does not mutate raw event.timeline storage"
    - "Collapsed rolling coverage still surfaces the newest/highest-signal updates"
    - "The threshold is strictly greater than 5 items in a single day"
  artifacts:
    - path: "references/processing-instructions.md"
      provides: "Same-day collapse algorithm for rolling coverage"
      contains: ">5 timeline entries"
    - path: "references/output-templates.md"
      provides: "Collapsed day block render contract"
      contains: "collapsed_day_count"
    - path: "SKILL.md"
      provides: "Event Tracking step references dense-day collapse"
      contains: "collapsed timeline view"
    - path: "data/fixtures/events-active-dense-day.json"
      provides: "Fixture event with more than 5 entries on one day"
      contains: "\"timeline\""
  key_links:
    - from: "SKILL.md"
      to: "references/processing-instructions.md"
      via: "Event Tracking step points to same-day collapse logic"
      pattern: "collapsed timeline view"
    - from: "references/output-templates.md"
      to: "data/fixtures/events-active-dense-day.json"
      via: "Fixture demonstrates exact collapsed-day render contract"
      pattern: "collapsed_day_count"
---

<objective>
Collapse bursty same-day event timelines into a cleaner rolling coverage view so high-volume events stay readable without hiding the newest developments.

Purpose: Prevent event tracking from flooding the digest when many updates land on one day.
Output: Same-day collapse algorithm, updated Event Tracking render contract, SKILL wiring, dense-day fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-interaction-surface-deployment-ux/12-RESEARCH.md
@.planning/phases/12-interaction-surface-deployment-ux/12-VALIDATION.md
@references/processing-instructions.md
@references/output-templates.md
@references/data-models.md
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add same-day collapse algorithm to rolling coverage instructions</name>
  <files>references/processing-instructions.md, SKILL.md</files>
  <read_first>references/processing-instructions.md, references/output-templates.md, SKILL.md, references/data-models.md</read_first>
  <action>
1. In `references/processing-instructions.md`, update the Output Phase / digest assembly area so Event Tracking is no longer described as an MVP omission.

2. Add a new subsection titled `### Dense-Day Timeline Collapse (INTERACT-05)` near Event Tracking rendering rules with this exact algorithm:
   - Build day buckets from `event.timeline` using the entry date in `YYYY-MM-DD`
   - Sort day buckets newest first
   - If a day bucket has `<= 5` timeline entries, render all entries for that day in chronological order
   - If a day bucket has `> 5` timeline entries:
     - render one summary line first:
       `- [YYYY-MM-DD] collapsed_day_count={count}; latest={latest_brief}`
     - then render the newest 2 timeline entries for that day
     - then render one omission line:
       `... earlier_same_day_updates={count_minus_two}`
   - Keep raw `event.timeline` unchanged; this is presentation-only

3. Add one explicit note that the threshold is **strictly greater than 5 entries in a single day**, not "5 or more" and not "5 total entries across the event".

4. In `SKILL.md` Output Phase step 5, change the Event Tracking instruction so it explicitly says:
   - build a timeline view for events with new items merged today
   - collapse same-day bursts when a day has more than 5 items
   - follow `references/output-templates.md` and `references/processing-instructions.md` rules

5. Do not introduce any storage mutation, archival, or digest-history changes in this plan. The only behavior change is how rolling coverage is rendered.
  </action>
  <verify>
    <automated>rg -n "Dense-Day Timeline Collapse|> 5|presentation-only|collapsed timeline view|earlier_same_day_updates" references/processing-instructions.md SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - references/processing-instructions.md contains `### Dense-Day Timeline Collapse (INTERACT-05)`
    - references/processing-instructions.md contains `> 5` and `single day`
    - references/processing-instructions.md contains `presentation-only`
    - SKILL.md step 5 contains `collapsed timeline view`
    - SKILL.md step 5 contains `more than 5 items`
  </acceptance_criteria>
  <done>Rolling coverage instructions now define a strict same-day collapse rule without touching stored event history.</done>
</task>

<task type="auto">
  <name>Task 2: Update Event Tracking template and add dense-day fixture</name>
  <files>references/output-templates.md, data/fixtures/events-active-dense-day.json</files>
  <read_first>references/output-templates.md, references/processing-instructions.md, references/data-models.md</read_first>
  <action>
1. In `references/output-templates.md`, update the Event Tracking section example so it includes a collapsed same-day block. Add this exact example beneath the existing timeline bullets:

```markdown
- [2026-01-01] collapsed_day_count=7; latest=Regulator issued a new response
- [2026-01-01 18:00] Regulator issued a new response (update) -- Source: Example Source
- [2026-01-01 12:00] Company disclosed key details (update) -- Source: Example Source
... earlier_same_day_updates=5
```

2. The surrounding user-facing template should remain Chinese. The example above is structural and may be translated, but it must preserve the same four data points:
   - bucket date
   - collapsed count
   - latest update summary
   - omitted same-day update count

3. In the Quality Rules / Section Omission area of `references/output-templates.md`, replace the old "most recent 5 timeline entries per event" note with a day-aware rule:
   - same-day buckets with `>5` entries collapse
   - collapsed day blocks still show the newest 2 entries
   - older non-rendered entries are summarized with omission text

4. Create `data/fixtures/events-active-dense-day.json` as a deterministic fixture containing one active event with:
   - at least 7 timeline entries on `2026-01-01`
   - at least 2 older entries on a previous day
   - realistic `status`, `importance`, `keywords`, and `item_ids`

5. Use exact timestamps in the fixture so the newest entry on `2026-01-01` is clearly identifiable. For example:
   - `2026-01-01T08:00:00Z`
   - `2026-01-01T10:00:00Z`
   - `2026-01-01T12:00:00Z`
   - `2026-01-01T14:00:00Z`
   - `2026-01-01T16:00:00Z`
   - `2026-01-01T18:00:00Z`
   - `2026-01-01T20:00:00Z`

6. Ensure the fixture remains compatible with the Event schema already documented in `references/data-models.md`.
  </action>
  <verify>
    <automated>rg -n "collapsed_day_count=7|earlier_same_day_updates=5" references/output-templates.md && python3 -c "import json; d=json.load(open('data/fixtures/events-active-dense-day.json')); e=d[0] if isinstance(d,list) else d['events'][0]; same_day=[t for t in e['timeline'] if t['timestamp'].startswith('2026-01-01')]; assert len(same_day)>=7; print('PASS')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - references/output-templates.md contains `collapsed_day_count=7`
    - references/output-templates.md contains `earlier_same_day_updates=5`
    - references/output-templates.md no longer says `most recent 5 timeline entries per event`
    - data/fixtures/events-active-dense-day.json exists and is valid JSON
    - the fixture contains at least 7 entries on `2026-01-01`
  </acceptance_criteria>
  <done>The Event Tracking template includes a concrete collapsed-day example and the dense-day fixture proves the thresholded render contract.</done>
</task>

</tasks>

<verification>
- `rg -n "Dense-Day Timeline Collapse|collapsed_day_count|collapsed timeline view" references/processing-instructions.md references/output-templates.md SKILL.md` returns matches
- `python3 -c "import json; json.load(open('data/fixtures/events-active-dense-day.json')); print('PASS')"` succeeds
- `rg -n "more than 5 items" SKILL.md` returns the updated Event Tracking instruction
</verification>

<success_criteria>
- INTERACT-05 is implemented as a rendering rule, not a storage mutation
- Same-day bursts over 5 entries collapse into a compact block
- The newest updates remain visible after collapse
- The dense-day fixture demonstrates the exact threshold and shape of the collapsed output
</success_criteria>

---
phase: 01-multi-source-preferences
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - references/output-templates.md
  - SKILL.md
  - references/processing-instructions.md
autonomous: true
requirements: [OUT-02, OUT-06]

must_haves:
  truths:
    - "When any item in today's batch has importance_score >= 0.85, a breaking news alert is generated and delivered"
    - "When no item meets the breaking news threshold, no alert is sent (no false positives)"
    - "Daily digest footer shows: source count, items processed, LLM calls made, cache hits"
    - "Breaking news alert includes item title, source, importance score, and 1-sentence summary"
  artifacts:
    - path: "references/output-templates.md"
      provides: "Activated breaking news template and transparency stats footer wiring"
      contains: "## Breaking News Alert"
    - path: "SKILL.md"
      provides: "Breaking news check flow in quick-check cron path"
      contains: "breaking news"
    - path: "references/processing-instructions.md"
      provides: "Metrics collection wiring for transparency footer (source_count, items_processed, llm_calls, cache_hits)"
      contains: "transparency"
  key_links:
    - from: "SKILL.md"
      to: "references/output-templates.md"
      via: "breaking news flow references output template for alert format"
      pattern: "output-templates\\.md"
    - from: "references/output-templates.md"
      to: "data/metrics/daily-YYYY-MM-DD.json"
      via: "transparency footer reads run metrics for stats"
      pattern: "daily-.*\\.json"
    - from: "SKILL.md"
      to: "references/processing-instructions.md"
      via: "quick-check flow references processing for breaking news threshold"
      pattern: "processing-instructions\\.md"
---

<objective>
Activate breaking news alerts and add transparency stats to the daily digest footer. Breaking news fires only when items hit importance >= 0.85 threshold, with zero false positives when nothing qualifies. The digest footer shows operational transparency: source count, items processed, LLM calls, and cache hits.

Purpose: Deliver time-sensitive high-importance news immediately (not waiting for daily digest), and give the user visibility into how the system operates.

Output: Extended output-templates.md with active breaking news template and transparency footer. Extended SKILL.md with breaking news check flow. Extended processing-instructions.md with metrics collection for footer stats.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-source-preferences/01-RESEARCH.md

@references/output-templates.md
@references/cron-configs.md
@SKILL.md

<!-- Plan 01 will have extended SKILL.md with type routing and User Commands. Plan 04 adds on top of that. -->
<!-- Plan 02 will have created feedback-rules.md. Plan 04 does not directly depend on its content but SKILL.md will reference it. -->
<!-- Plan 03 will have added cache_hits to processing metrics. Plan 04 wires those into the footer. -->

<interfaces>
<!-- Existing output-templates.md has: daily digest template with sections (Core/Adjacent/Hot/Explore), quality rules, and a kuaixun (breaking news) template that is documented but not wired into SKILL.md flow. -->

<!-- Existing cron-configs.md has quick-check cron job (every 2h) already configured. The quick-check currently does collection + processing but does NOT check for breaking news. Plan 04 adds the breaking news check to the quick-check flow. -->

<!-- Metrics fields from processing-instructions.md (after Plan 03): -->
<!-- daily-YYYY-MM-DD.json will contain: sources_checked, items_fetched, items_new, items_processed, llm_calls, llm_tokens, cache_hits, errors -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate breaking news template and add transparency footer in output-templates.md</name>
  <files>references/output-templates.md</files>
  <action>
Read the current `references/output-templates.md`. It already has a kuaixun (breaking news) template section from Phase 0 but it is inactive/placeholder. Activate and refine it.

**1. Update the Breaking News Alert section** (or create if only a stub exists):

```markdown
## Breaking News Alert (Kuaixun)

**Trigger condition:** Any item with `importance_score >= 0.85` during a quick-check run.

**Conservative threshold policy:** Prefer to MISS a breaking story rather than send a false alarm. The threshold of 0.85 is deliberately high. Only events of genuine breaking significance (major policy changes, critical security incidents, landmark product launches by tier-1 companies, significant market events) should reach 0.85.

**Additional safeguards:**
- Only items classified with `form_type: "news"` or `form_type: "announcement"` qualify (no opinion/analysis alerts)
- Maximum 3 alerts per day. If 3 alerts have already been sent today (tracked in daily metrics), skip further alerts.
- Same-URL dedup: Do not alert for an item whose URL was already alerted (track in metrics `alerted_urls` array)

**Alert format:**
```
[Breaking] {title}

{1-sentence summary}

Source: {source_name} | Importance: {importance_score}
Time: {published_at or fetch time}
```

**No-alert behavior:** If no items meet the threshold after a quick-check run, produce NO output. Do not send "no breaking news" messages. Silence means no breaking news.
```

**2. Add/update the Transparency Footer section** for the daily digest:

```markdown
## Transparency Footer

Appended at the bottom of every daily digest output:

```
---
Stats: {source_count} sources checked | {items_processed} items processed | {llm_calls} LLM calls | {cache_hits} cache hits
```

Where:
- `source_count`: Number of enabled sources in sources.json at time of run
- `items_processed`: Number of items that reached processing_status "complete" today
- `llm_calls`: Value of budget.json calls_today at end of run
- `cache_hits`: Sum of classify + summarize cache hits for this run (from daily metrics)

If circuit-breaker was triggered, append: ` | Budget: EXHAUSTED`
If budget warning was triggered, append: ` | Budget: {percentage}% used`
```

**3. Update the daily digest template** to reference the transparency footer:
- At the end of the digest template, add a note: "Append Transparency Footer (see section above) after all content sections."
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/openclaw-personal-news-digest && grep -q "importance_score >= 0.85" references/output-templates.md && grep -q "Transparency Footer" references/output-templates.md && grep -q "source_count" references/output-templates.md && grep -q "cache_hits" references/output-templates.md && grep -q "Maximum 3 alerts" references/output-templates.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Breaking news alert template activated with importance >= 0.85 threshold
    - Conservative safeguards documented: form_type filter, 3 alerts/day max, URL dedup
    - No-alert behavior explicitly stated (silence when nothing qualifies)
    - Transparency footer template shows source_count, items_processed, llm_calls, cache_hits
    - Budget status appended to footer when warning/exhausted
    - Daily digest template references the transparency footer
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire breaking news check into SKILL.md and add metrics collection to processing-instructions.md</name>
  <files>SKILL.md, references/processing-instructions.md</files>
  <action>
**1. Extend SKILL.md** with a breaking news check flow. Add a new section after "Output Phase" and before "Standing Orders":

```markdown
## Quick-Check Flow (Breaking News)

When triggered by the quick-check cron job (every 2 hours):
1. Execute Collection Phase (same as daily -- fetch all enabled sources by type)
2. Execute Processing Phase (classify and summarize new items, with cache and circuit-breaker)
3. **Breaking news scan**: After processing, scan today's items for `importance_score >= 0.85`:
   a. Filter: only `form_type: "news"` or `form_type: "announcement"`
   b. Check daily metrics `alerts_sent_today` -- if >= 3, skip
   c. Check daily metrics `alerted_urls` -- skip already-alerted URLs
   d. If qualifying items exist: Generate alert per `{baseDir}/references/output-templates.md` "Breaking News Alert" format. Deliver via announce channel. Record URL in `alerted_urls` and increment `alerts_sent_today`.
   e. If no qualifying items: Produce NO output. End run silently.
4. Release lock.
```

Keep this section concise (the detail is in output-templates.md). Count words after edit to ensure SKILL.md stays under 700 words total (it will be ~620-650 after Plan 01 + Plan 04 additions).

**2. Extend `references/processing-instructions.md`** -- Add a "## Metrics Collection for Transparency" section at the end (or near the existing budget tracking section):

```markdown
## Metrics Collection for Transparency

During each pipeline run, track these counters for the transparency footer:

| Metric | Source | When Updated |
|--------|--------|--------------|
| source_count | Count of enabled:true in sources.json | At run start |
| items_fetched | Items returned by collection phase | After collection |
| items_new | Items that passed dedup | After dedup |
| items_processed | Items reaching processing_status: "complete" | After processing |
| llm_calls | calls_today from budget.json | After each LLM batch |
| cache_hits | Classify + summarize cache hits | After each cache lookup |
| alerts_sent_today | Breaking news alerts sent | After alert delivery |
| alerted_urls | URLs already alerted today | After alert delivery |

All metrics are written to `data/metrics/daily-YYYY-MM-DD.json` at the end of each run.

The daily digest output phase reads these metrics to populate the Transparency Footer.
```

Also add `alerts_sent_today` (integer, default 0) and `alerted_urls` (array, default []) to the daily metrics schema in this file.

**3. Update SKILL.md Output Phase** -- Add a final step to the existing Output Phase (daily digest path):
```
Last step: Append Transparency Footer per `{baseDir}/references/output-templates.md` "Transparency Footer" section, reading stats from `data/metrics/daily-YYYY-MM-DD.json`.
```
This ensures the daily digest output flow actually includes the footer, not just the quick-check flow.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/openclaw-personal-news-digest && grep -q "Quick-Check Flow" SKILL.md && grep -q "breaking news" SKILL.md && grep -q "importance_score >= 0.85" SKILL.md && grep -q "alerted_urls" SKILL.md && grep -q "Metrics Collection for Transparency" references/processing-instructions.md && grep -q "alerts_sent_today" references/processing-instructions.md && wc -w SKILL.md | awk '{if ($1 > 750) print "WARN: SKILL.md is " $1 " words"; else print "PASS: " $1 " words"}'</automated>
  </verify>
  <done>
    - SKILL.md has "Quick-Check Flow" section with breaking news scan after processing
    - Breaking news scan filters by importance >= 0.85 + form_type + daily cap + URL dedup
    - No-output behavior when nothing qualifies is explicit in SKILL.md
    - processing-instructions.md has "Metrics Collection for Transparency" section
    - Daily metrics schema includes alerts_sent_today and alerted_urls fields
    - SKILL.md stays under 750 words total
  </done>
</task>

</tasks>

<verification>
- Breaking news fires ONLY for importance >= 0.85 items with news/announcement form_type
- Maximum 3 alerts per day prevents alert fatigue
- No output when nothing qualifies (silence, not "no news" message)
- Transparency footer shows all 4 stats: source_count, items_processed, llm_calls, cache_hits
- Budget warning/exhaustion status appears in footer when applicable
- SKILL.md quick-check flow references output-templates.md for alert format
- Metrics collection documents all fields needed for transparency footer
</verification>

<success_criteria>
- Breaking news alert template is active with >= 0.85 threshold and conservative safeguards (OUT-02)
- No false positives: silence when nothing qualifies, form_type filter, daily cap
- Transparency footer shows source count, items processed, LLM calls, cache hits (OUT-06)
- SKILL.md has complete quick-check flow for breaking news
- Daily metrics schema tracks alert-specific fields (alerts_sent_today, alerted_urls)
- All changes integrate cleanly with Plans 01-03 outputs
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-source-preferences/01-04-SUMMARY.md`
</output>

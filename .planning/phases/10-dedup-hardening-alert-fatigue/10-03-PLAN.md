---
phase: 10-dedup-hardening-alert-fatigue
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - references/data-models.md
  - references/processing-instructions.md
  - references/output-templates.md
  - SKILL.md
  - data/fixtures/digest-history-sample.json
  - data/fixtures/metrics-sample.json
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03]

must_haves:
  truths:
    - "DigestHistory tracks the last 5 digest runs with per-event timeline snapshots"
    - "Events with no new timeline progress since last digest receive 0.7x score penalty"
    - "The 0.7x penalty is a single multiplier comparing only against the last digest, not compounding"
    - "Digest footer shows count of suppressed repeat items"
    - "DigestHistory is written at end of Output Phase after digest assembly"
  artifacts:
    - path: "references/data-models.md"
      provides: "DigestHistory schema definition"
      contains: "DigestHistory"
    - path: "references/processing-instructions.md"
      provides: "Repetition penalty logic and digest-history write step"
      contains: "repetition penalty"
    - path: "references/output-templates.md"
      provides: "Suppression footer line"
      contains: "已抑制重复"
    - path: "SKILL.md"
      provides: "Output Phase wiring for penalty and history write"
      contains: "digest-history"
    - path: "data/fixtures/digest-history-sample.json"
      provides: "Fixture for 5-run digest history with snapshots"
      contains: "event_timeline_snapshot"
  key_links:
    - from: "SKILL.md Output Phase step 1"
      to: "references/processing-instructions.md repetition penalty"
      via: "Apply penalty after scoring, before quota"
      pattern: "repetition penalty"
    - from: "SKILL.md Output Phase"
      to: "data/digest-history.json"
      via: "Write digest history after output"
      pattern: "digest-history"
    - from: "references/output-templates.md footer"
      to: "DailyMetrics"
      via: "repeat_suppressed count in footer"
      pattern: "已抑制重复"
---

<objective>
Create the DigestHistory data model, implement cross-digest repetition penalty (0.7x), add suppression footer, and wire everything into the pipeline.

Purpose: Prevent stale events from dominating consecutive digests by penalizing events with no new timeline progress. Users see fresher content and know how many repeat items were suppressed.

Output: DigestHistory schema in data-models.md, repetition penalty and history write in processing-instructions.md, suppression footer in output-templates.md, updated SKILL.md Output Phase, fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dedup-hardening-alert-fatigue/10-01-SUMMARY.md
@.planning/phases/10-dedup-hardening-alert-fatigue/10-02-SUMMARY.md
@references/data-models.md
@references/processing-instructions.md
@references/output-templates.md
@references/scoring-formula.md
@SKILL.md
@data/fixtures/metrics-sample.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: DigestHistory data model + repetition penalty logic + suppression footer</name>
  <files>references/data-models.md, references/processing-instructions.md, references/output-templates.md, data/fixtures/digest-history-sample.json</files>
  <read_first>references/data-models.md, references/processing-instructions.md, references/output-templates.md, references/scoring-formula.md, data/fixtures/metrics-sample.json</read_first>
  <action>
**1. Add DigestHistory model to references/data-models.md:**

After the AlertState section (added in Plan 10-01), add a new section "## DigestHistory":

```json
{
  "_schema_v": 1,
  "runs": [
    {
      "run_id": "run-20260402-080000-abcd",
      "date": "2026-04-02",
      "event_timeline_snapshot": {
        "evt-12345678": {
          "timeline_count": 5,
          "last_news_id": "abc123",
          "last_timestamp": "ISO8601"
        }
      },
      "selected_event_ids": ["evt-12345678", "evt-87654321"]
    }
  ]
}
```

Field notes:
- Stored at `data/digest-history.json`
- Rolling window: keep only last 5 runs. On write, if `runs.length > 5`, remove oldest entry (index 0).
- `event_timeline_snapshot`: Captures `timeline.length` and last entry per event at digest time. Used to detect whether an event has new progress since last digest.
- `selected_event_ids`: Event IDs of items selected for this digest (for tracking which events appeared).
- If file not found, initialize with `{ _schema_v: 1, runs: [] }`.

Add to New Fields Registry table:
| `runs` | DigestHistory | Phase 10 | v1 | `[]` | Rolling 5-run digest history with event snapshots |

**2. Add Section 4A: Cross-Digest Repetition Penalty to references/processing-instructions.md:**

Insert after Section 4 (Output Generation), before Section 5. Title: "## Section 4A: Cross-Digest Repetition Penalty (DEDUP-02)"

Content:

```
Applied during Output Phase, AFTER computing final_score (Section 4 scoring) and BEFORE quota allocation (Section 4 Step 1 through Step 3). This penalizes events that appear in consecutive digests without new developments.

### When to Run

After all items are scored (final_score computed), before quota group assignment.

### Penalty Procedure

1. Read `data/digest-history.json`
   - If file not found or runs array is empty: skip penalty entirely (no history to compare against)
2. Get `last_run = runs[runs.length - 1]` (most recent run)
3. Get `last_snapshot = last_run.event_timeline_snapshot`

4. For each scored item where `item.event_id` is not null:
   a. Look up `item.event_id` in `last_snapshot`
   b. If NOT found in snapshot: no penalty (first appearance in digest)
   c. If found in snapshot:
      - Read current event from `data/events/active.json`
      - Compare `current_event.timeline.length` vs `snapshot[event_id].timeline_count`
      - If `current_event.timeline.length == snapshot.timeline_count`:
        -> No new progress. Apply penalty: `item.final_score *= 0.7`
        -> Increment `repeat_suppressed_count` (track for footer)
      - If `current_event.timeline.length > snapshot.timeline_count`:
        -> New progress exists. No penalty.

### Key Design Rules

- The 0.7 multiplier is applied ONCE (not compounding). It only compares against the LAST digest run, not all previous runs.
- Items with genuinely high importance (0.85+) still score competitively even with penalty: 0.85 * 0.25 weight = 0.2125 for importance dimension alone, plus other dimensions.
- The penalty applies to `final_score` (the already-computed weighted sum), not to any individual dimension.
- Items penalized may still be selected if their penalized score remains above the selection threshold.
- `repeat_suppressed_count` tracks items that received the 0.7x penalty, whether or not they were subsequently excluded from the digest.

### Interaction with Other Steps

- Runs AFTER: Section 4 scoring (final_score computed)
- Runs BEFORE: Section 4 Step 1-3 quota allocation
- Does NOT interact with noise filter (Section 0E) -- items already filtered by noise never reach scoring
- Does NOT interact with stale event filter (ANTI-03 Step 5) -- that filter checks 3-day consecutive push, this checks timeline progress
```

**3. Add Section 4B: DigestHistory Write to references/processing-instructions.md:**

Insert after Section 4A. Title: "## Section 4B: DigestHistory Write (DEDUP-01)"

Content:

```
At the END of Output Phase, after digest is written to output/latest-digest.md and before lock release.

### Write Procedure

1. Read `data/digest-history.json` (or initialize `{ _schema_v: 1, runs: [] }` if not found)
2. Build `event_timeline_snapshot` from the current digest's selected items:
   - For each selected item with a non-null `event_id`:
     - Read the event from `data/events/active.json`
     - Record: `event_id -> { timeline_count: event.timeline.length, last_news_id: event.timeline[last].news_id, last_timestamp: event.timeline[last].timestamp }`
3. Build `selected_event_ids`: deduplicated list of event_ids from selected items
4. Append new run entry:
   ```json
   {
     "run_id": "{current run_id}",
     "date": "{today YYYY-MM-DD}",
     "event_timeline_snapshot": { ... },
     "selected_event_ids": [ ... ]
   }
   ```
5. If `runs.length > 5`: remove `runs[0]` (oldest)
6. Atomic write `data/digest-history.json` (tmp + rename)
```

**4. Add suppression footer line to references/output-templates.md:**

In the "Transparency Footer" section, after the existing footer template line:

```
统计: {source_count} 个来源已检查 | {items_processed} 条已处理 | {llm_calls} 次 LLM 调用 | {cache_hits} 次缓存命中
```

Add a conditional line:

```
If repeat_suppressed_count > 0, append on a new line:
已抑制重复: {repeat_suppressed_count} 条 (无新进展的事件)
```

Where `repeat_suppressed_count` is the count of items that received the 0.7x repetition penalty (from Section 4A).

**5. Create data/fixtures/digest-history-sample.json:**

```json
{
  "_schema_v": 1,
  "runs": [
    {
      "run_id": "run-20260410-080000-ab01",
      "date": "2026-04-10",
      "event_timeline_snapshot": {
        "evt-fix00001": {
          "timeline_count": 2,
          "last_news_id": "fix01a2b3c4d5e6",
          "last_timestamp": "2026-01-01T00:00:00Z"
        }
      },
      "selected_event_ids": ["evt-fix00001"]
    },
    {
      "run_id": "run-20260411-080000-cd02",
      "date": "2026-04-11",
      "event_timeline_snapshot": {
        "evt-fix00001": {
          "timeline_count": 2,
          "last_news_id": "fix01a2b3c4d5e6",
          "last_timestamp": "2026-01-01T00:00:00Z"
        },
        "evt-fix00002": {
          "timeline_count": 1,
          "last_news_id": "fix02newevt0001",
          "last_timestamp": "2026-01-10T00:00:00Z"
        }
      },
      "selected_event_ids": ["evt-fix00001", "evt-fix00002"]
    },
    {
      "run_id": "run-20260412-080000-ef03",
      "date": "2026-04-12",
      "event_timeline_snapshot": {
        "evt-fix00001": {
          "timeline_count": 3,
          "last_news_id": "fix01newupdate1",
          "last_timestamp": "2026-04-12T06:00:00Z"
        },
        "evt-fix00002": {
          "timeline_count": 1,
          "last_news_id": "fix02newevt0001",
          "last_timestamp": "2026-01-10T00:00:00Z"
        }
      },
      "selected_event_ids": ["evt-fix00001", "evt-fix00002"]
    },
    {
      "run_id": "run-20260413-080000-gh04",
      "date": "2026-04-13",
      "event_timeline_snapshot": {
        "evt-fix00001": {
          "timeline_count": 3,
          "last_news_id": "fix01newupdate1",
          "last_timestamp": "2026-04-12T06:00:00Z"
        }
      },
      "selected_event_ids": ["evt-fix00001"]
    },
    {
      "run_id": "run-20260414-080000-ij05",
      "date": "2026-04-14",
      "event_timeline_snapshot": {
        "evt-fix00001": {
          "timeline_count": 3,
          "last_news_id": "fix01newupdate1",
          "last_timestamp": "2026-04-12T06:00:00Z"
        },
        "evt-fix00003": {
          "timeline_count": 2,
          "last_news_id": "fix03brandnew01",
          "last_timestamp": "2026-04-14T07:00:00Z"
        }
      },
      "selected_event_ids": ["evt-fix00001", "evt-fix00003"]
    }
  ]
}
```

This fixture demonstrates:
- 5 runs (max rolling window)
- evt-fix00001 appears in all 5 runs, with timeline_count changing from 2 to 3 between runs 2 and 3 (new progress), then staying at 3 for runs 3-5 (would trigger penalty on runs 4 and 5)
- evt-fix00002 appears in runs 2-3 with static timeline_count (would trigger penalty on run 3)
- evt-fix00003 appears only in run 5 (first appearance, no penalty)
  </action>
  <verify>
    <automated>grep "DigestHistory" references/data-models.md && grep "Section 4A" references/processing-instructions.md && grep "已抑制重复" references/output-templates.md && cat data/fixtures/digest-history-sample.json | python3 -c "import json,sys; d=json.load(sys.stdin); assert len(d['runs'])==5; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - references/data-models.md contains "## DigestHistory" section with `event_timeline_snapshot` and `selected_event_ids` in schema
    - references/data-models.md New Fields Registry contains row with `runs | DigestHistory | Phase 10`
    - references/processing-instructions.md contains "## Section 4A: Cross-Digest Repetition Penalty (DEDUP-02)"
    - Section 4A contains `item.final_score *= 0.7` penalty formula
    - Section 4A contains "AFTER computing final_score" and "BEFORE quota allocation" timing
    - Section 4A explicitly states "applied ONCE (not compounding)"
    - references/processing-instructions.md contains "## Section 4B: DigestHistory Write (DEDUP-01)"
    - Section 4B contains "runs.length > 5" rolling window cleanup
    - references/output-templates.md Transparency Footer contains "已抑制重复: {repeat_suppressed_count} 条 (无新进展的事件)"
    - data/fixtures/digest-history-sample.json has exactly 5 runs with event_timeline_snapshot data
  </acceptance_criteria>
  <done>DigestHistory schema defined, repetition penalty logic documented with 0.7x non-compounding multiplier, digest-history write step documented, suppression footer added, fixture covers 5-run window with progress and no-progress scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SKILL.md Output Phase and update metrics fixture</name>
  <files>SKILL.md, data/fixtures/metrics-sample.json</files>
  <read_first>SKILL.md, references/processing-instructions.md, data/fixtures/metrics-sample.json</read_first>
  <action>
**1. Update SKILL.md Output Phase to include repetition penalty and digest history write:**

In the Output Phase section, modify step 1 and add new steps:

**Step 1 update:** Change the existing step 1 from:
```
1. **Score items**: Read `{baseDir}/references/scoring-formula.md`. Score all completed items (all 7 dimensions active, including event_boost from `data/events/active.json`), sort by `final_score` descending. Exclude items with `dedup_status: "title_dup"` or `"url_dup"` from the scoring pool. Exclude items with `digest_eligible: false` from the scoring pool.
```
To:
```
1. **Score items**: Read `{baseDir}/references/scoring-formula.md`. Score all completed items (all 7 dimensions active, including event_boost from `data/events/active.json`), sort by `final_score` descending. Exclude items with `dedup_status: "title_dup"` or `"url_dup"` from the scoring pool. Exclude items with `digest_eligible: false` from the scoring pool.
1b. **Cross-digest repetition penalty**: Read `{baseDir}/data/digest-history.json`. For events with no new timeline progress since last digest, apply 0.7x penalty to final_score. Track repeat_suppressed_count. See `{baseDir}/references/processing-instructions.md` Section 4A.
```

**Add step 6b after step 6 (Write output) and before step 7 (Write metrics):**
```
6b. **Write digest history**: Snapshot event timelines for selected items, append to `{baseDir}/data/digest-history.json` (rolling 5-run window). See `{baseDir}/references/processing-instructions.md` Section 4B.
```

**Update step 8 (Append transparency footer):**
Change the existing step 8 to add the suppression line:
```
8. **Append transparency footer**: Read stats from `data/metrics/daily-YYYY-MM-DD.json`, format per `{baseDir}/references/output-templates.md` "Transparency Footer" section. Include repeat_suppressed_count if > 0 (suppression footer line). Append to digest output.
```

**2. Update data/fixtures/metrics-sample.json to include repeat_suppressed field:**

Read the current metrics-sample.json. Add a new field `repeat_suppressed` to the `items` object, with value `2` (sample value indicating 2 items were penalized). The field should be at the same level as `noise_filter_suppressed`.

Add this field:
```json
"repeat_suppressed": 2
```

inside the `"items"` object, after the existing `"noise_filter_suppressed"` field.
  </action>
  <verify>
    <automated>grep "repetition penalty" SKILL.md && grep "digest-history" SKILL.md && grep "repeat_suppressed" SKILL.md && grep "repeat_suppressed" data/fixtures/metrics-sample.json</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md Output Phase contains step "1b" with "Cross-digest repetition penalty" referencing Section 4A
    - SKILL.md Output Phase contains step "6b" with "Write digest history" referencing Section 4B
    - SKILL.md Output Phase step 8 mentions "repeat_suppressed_count" in footer
    - data/fixtures/metrics-sample.json items object contains `"repeat_suppressed"` field with integer value
  </acceptance_criteria>
  <done>SKILL.md Output Phase wires repetition penalty between scoring and quota, writes digest history after output, includes suppression count in footer. Metrics fixture updated with repeat_suppressed field.</done>
</task>

</tasks>

<verification>
1. grep "DigestHistory" references/data-models.md -- schema defined
2. grep "Section 4A" references/processing-instructions.md -- penalty logic exists
3. grep "Section 4B" references/processing-instructions.md -- history write exists
4. grep "已抑制重复" references/output-templates.md -- footer line exists
5. grep "digest-history" SKILL.md -- Output Phase wired
6. grep "repeat_suppressed" data/fixtures/metrics-sample.json -- metrics fixture updated
7. cat data/fixtures/digest-history-sample.json | python3 -c "import json,sys; d=json.load(sys.stdin); assert len(d['runs'])==5; assert 'event_timeline_snapshot' in d['runs'][0]; print('PASS')"
</verification>

<success_criteria>
- DigestHistory data model defined with rolling 5-run window and event_timeline_snapshot
- Repetition penalty applies 0.7x to final_score for events with no new timeline progress
- Penalty compares only against last digest run (not compounding)
- DigestHistory written at end of Output Phase with atomic write
- Suppression footer shows count of penalized items
- SKILL.md Output Phase includes steps for penalty, history write, and suppression footer
- Metrics fixture includes repeat_suppressed count
</success_criteria>

<output>
After completion, create `.planning/phases/10-dedup-hardening-alert-fatigue/10-03-SUMMARY.md`
</output>

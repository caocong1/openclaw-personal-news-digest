---
phase: 10-dedup-hardening-alert-fatigue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/data-models.md
  - references/processing-instructions.md
  - references/output-templates.md
  - SKILL.md
  - data/fixtures/alert-state-sample.json
autonomous: true
requirements: [ALERT-01, ALERT-03, ALERT-06]

must_haves:
  truths:
    - "Alert state is persisted in a dedicated daily file, not in DailyMetrics"
    - "At most 3 alerts fire per day, enforced by reading alert-state file"
    - "Same URL is never alerted twice in one day"
    - "Items without event_id fall through to standard alert path"
    - "Quick-Check reads/writes alert-state file as authoritative source"
  artifacts:
    - path: "references/data-models.md"
      provides: "AlertState schema definition"
      contains: "AlertState"
    - path: "references/processing-instructions.md"
      provides: "Unified alert decision tree"
      contains: "Alert Decision Tree"
    - path: "SKILL.md"
      provides: "Bootstrap for data/alerts/ and alert-state read/write in Quick-Check"
      contains: "data/alerts/"
    - path: "data/fixtures/alert-state-sample.json"
      provides: "Fixture for alert state verification"
      contains: "max_alerts"
  key_links:
    - from: "SKILL.md Quick-Check flow"
      to: "references/processing-instructions.md alert decision tree"
      via: "Quick-Check step 2 references decision tree"
      pattern: "Alert Decision Tree"
    - from: "references/processing-instructions.md alert decision tree"
      to: "data/alerts/alert-state-YYYY-MM-DD.json"
      via: "Read/write alert state file"
      pattern: "alert-state"
---

<objective>
Create the AlertState data model, persistent daily alert-state file, unified alert decision tree, and standard alert fallback path.

Purpose: Replace scattered alert tracking (alerts_sent_today/alerted_urls in DailyMetrics) with a dedicated alert-state file that persists across multiple quick-check runs per day, enforcing a 3-alert daily cap with URL dedup. Consolidate all alert eligibility logic into a single decision tree.

Output: AlertState schema in data-models.md, unified decision tree in processing-instructions.md, updated SKILL.md bootstrap and Quick-Check flow, alert-state fixture file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@references/data-models.md
@references/processing-instructions.md
@references/output-templates.md
@SKILL.md
@data/fixtures/events-active.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: AlertState data model + alert decision tree + output template update</name>
  <files>references/data-models.md, references/processing-instructions.md, references/output-templates.md, data/fixtures/alert-state-sample.json</files>
  <read_first>references/data-models.md, references/processing-instructions.md, references/output-templates.md, data/fixtures/events-active.json</read_first>
  <action>
**1. Add AlertState model to references/data-models.md:**

After the AlertCondition section, add a new section "## AlertState" with this schema:

```json
{
  "_schema_v": 1,
  "date": "YYYY-MM-DD",
  "alerts_sent": 0,
  "max_alerts": 3,
  "alerted_urls": [],
  "alert_log": [
    {
      "news_id": "string",
      "event_id": "string or null",
      "url": "string",
      "title": "string",
      "importance_score": 0.0,
      "alert_type": "standard|delta",
      "sent_at": "ISO8601"
    }
  ]
}
```

Field notes:
- Stored at `data/alerts/alert-state-YYYY-MM-DD.json`
- Separate from DailyMetrics -- authoritative source for alert tracking across multiple quick-check runs per day
- `max_alerts`: Fixed at 3 (daily cap)
- `alerted_urls`: Array of URLs already alerted today for same-URL dedup
- `alert_log`: Audit trail of all alerts sent today
- If file not found for today, initialize with `alerts_sent: 0, max_alerts: 3, alerted_urls: [], alert_log: []`

Add to New Fields Registry table:
| `alert_log` | AlertState | Phase 10 | v1 | `[]` | Audit trail of alerts sent today |

**2. Add Section 5A: Unified Alert Decision Tree to references/processing-instructions.md:**

Add after existing Section 5 (Metrics Collection). Title: "## Section 5A: Unified Alert Decision Tree (ALERT-01, ALERT-03, ALERT-06)"

Content:

```
Single decision tree for all alert eligibility checks during Quick-Check flow. This replaces scattered alert logic.

### Decision Tree

1. Item has importance_score >= 0.85?
   NO  -> skip (not breaking news)
   YES -> continue

2. Item form_type is "news" or "announcement"?
   NO  -> skip (only factual content triggers alerts)
   YES -> continue

3. Read data/alerts/alert-state-{today YYYY-MM-DD}.json
   If file not found: initialize { _schema_v: 1, date: today, alerts_sent: 0, max_alerts: 3, alerted_urls: [], alert_log: [] }

4. alerts_sent >= max_alerts (3)?
   YES -> skip (daily cap reached)
   NO  -> continue

5. Item URL already in alerted_urls?
   YES -> skip (URL dedup)
   NO  -> continue

6. Item has event_id AND event has last_alerted_at (not null)?
   YES -> DELTA ALERT path (see Section 5B in Plan 10-02)
   NO  -> STANDARD ALERT path (step 7)

STANDARD ALERT (ALERT-06 fallback):
7. Generate alert using Breaking News Alert template from output-templates.md
8. Update alert-state file:
   - Increment alerts_sent
   - Append URL to alerted_urls
   - Append entry to alert_log: { news_id, event_id: item.event_id or null, url, title, importance_score, alert_type: "standard", sent_at: now ISO8601 }
9. Atomic write alert-state file (tmp + rename)

### DailyMetrics Derivation

DailyMetrics fields `alerts_sent_today` and `alerted_urls` become DERIVED from the alert-state file:
- At metrics write time (Output Phase step 7), read alert-state file and copy values
- Quick-Check flow reads/writes alert-state file directly, NOT DailyMetrics
- This prevents race conditions between quick-check runs and daily pipeline runs

### Backward Compatibility

- Existing DailyMetrics `alerts_sent_today` and `alerted_urls` fields are preserved for backward compat
- Old consumers reading DailyMetrics still get correct values (derived from alert-state)
- If alert-state file does not exist (first run or pre-Phase-10 state), fall back to DailyMetrics values
```

**3. Update output-templates.md Breaking News Alert section:**

In the "Breaking News Alert (Kuaixun)" section, update the "Additional safeguards" paragraph to reference the alert-state file:

Replace:
```
- Maximum 3 alerts per day. If 3 alerts have already been sent today (tracked in daily metrics `alerts_sent_today`), skip further alerts.
- Same-URL dedup: Do not alert for an item whose URL was already alerted (track in metrics `alerted_urls` array)
```

With:
```
- Maximum 3 alerts per day. Read `data/alerts/alert-state-{today}.json` to check `alerts_sent >= max_alerts`. Alert-state file is authoritative (not DailyMetrics).
- Same-URL dedup: Do not alert for an item whose URL is in `alerted_urls` array of the alert-state file.
- See `references/processing-instructions.md` Section 5A for the full unified alert decision tree.
```

**4. Create fixture file data/fixtures/alert-state-sample.json:**

```json
{
  "_schema_v": 1,
  "date": "2026-01-15",
  "alerts_sent": 3,
  "max_alerts": 3,
  "alerted_urls": [
    "https://example.com/breaking-ai-regulation",
    "https://example.com/major-security-incident",
    "https://example.com/landmark-product-launch"
  ],
  "alert_log": [
    {
      "news_id": "fix0alert00001",
      "event_id": "evt-fix00001",
      "url": "https://example.com/breaking-ai-regulation",
      "title": "AI 安全法规正式实施",
      "importance_score": 0.92,
      "alert_type": "standard",
      "sent_at": "2026-01-15T08:15:00Z"
    },
    {
      "news_id": "fix0alert00002",
      "event_id": null,
      "url": "https://example.com/major-security-incident",
      "title": "大规模安全漏洞被发现",
      "importance_score": 0.88,
      "alert_type": "standard",
      "sent_at": "2026-01-15T10:30:00Z"
    },
    {
      "news_id": "fix0alert00003",
      "event_id": null,
      "url": "https://example.com/landmark-product-launch",
      "title": "重大产品发布震动市场",
      "importance_score": 0.86,
      "alert_type": "delta",
      "sent_at": "2026-01-15T14:45:00Z"
    }
  ]
}
```
  </action>
  <verify>
    <automated>grep -c "AlertState" references/data-models.md && grep -c "Alert Decision Tree" references/processing-instructions.md && grep -c "alert-state" references/output-templates.md && cat data/fixtures/alert-state-sample.json | head -5</automated>
  </verify>
  <acceptance_criteria>
    - references/data-models.md contains "## AlertState" section with `"max_alerts": 3` and `"alert_log"` in schema
    - references/data-models.md New Fields Registry contains row with `alert_log | AlertState | Phase 10`
    - references/processing-instructions.md contains "## Section 5A: Unified Alert Decision Tree"
    - references/processing-instructions.md Section 5A contains "importance_score >= 0.85" and "alerts_sent >= max_alerts"
    - references/processing-instructions.md Section 5A contains "STANDARD ALERT" and "DELTA ALERT" paths
    - references/processing-instructions.md Section 5A contains "DailyMetrics Derivation" subsection
    - references/output-templates.md Breaking News Alert section contains "alert-state-{today}" and "Section 5A"
    - data/fixtures/alert-state-sample.json exists with `"alerts_sent": 3` and 3 entries in `alert_log`
  </acceptance_criteria>
  <done>AlertState schema defined in data-models.md, unified alert decision tree in processing-instructions.md Section 5A with standard and delta paths, output-templates.md updated to reference alert-state file, fixture file created with cap-reached scenario.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md bootstrap and Quick-Check flow for alert-state</name>
  <files>SKILL.md</files>
  <read_first>SKILL.md, references/processing-instructions.md</read_first>
  <action>
**1. Add data/alerts/ to SKILL.md bootstrap directory list:**

In Collection Phase step 0 (Bootstrap), add `{baseDir}/data/alerts/` to the list of required directories. Insert it after `{baseDir}/data/events/archived/` in the existing list. The updated list should include:

```
`{baseDir}/data/`, `{baseDir}/data/news/`, `{baseDir}/data/cache/`, `{baseDir}/data/events/`, `{baseDir}/data/events/archived/`, `{baseDir}/data/alerts/`, `{baseDir}/data/feedback/`, `{baseDir}/data/metrics/`, `{baseDir}/output/`, `{baseDir}/config/`
```

**2. Update Quick-Check flow to use alert-state file:**

In the "Quick-Check Flow (breaking news)" section, replace step 2 with:

```
2. **Breaking news scan**: For each item with `importance_score >= 0.85`, run the unified alert decision tree from `{baseDir}/references/processing-instructions.md` Section 5A. The decision tree reads/writes `{baseDir}/data/alerts/alert-state-{today YYYY-MM-DD}.json` as authoritative source (initializes file if absent). Enforces 3-alert daily cap, URL dedup, form_type filter (news/announcement only), and routes to standard or delta alert path.
```

**3. Update Output Phase step 7 to derive alert metrics:**

In Output Phase step 7 (Write metrics), add this sentence at the end:

```
Derive `alerts_sent_today` and `alerted_urls` from `{baseDir}/data/alerts/alert-state-{today}.json` (read file, copy `alerts_sent` and `alerted_urls` values). If alert-state file does not exist, use defaults (0 and []).
```
  </action>
  <verify>
    <automated>grep "data/alerts/" SKILL.md && grep "Section 5A" SKILL.md && grep "alert-state" SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md Collection Phase step 0 Bootstrap list contains `data/alerts/`
    - SKILL.md Quick-Check Flow step 2 contains "Section 5A" and "alert-state"
    - SKILL.md Quick-Check Flow step 2 contains "3-alert daily cap" or "unified alert decision tree"
    - SKILL.md Output Phase step 7 contains "alert-state" and "alerts_sent_today" derivation instruction
  </acceptance_criteria>
  <done>SKILL.md bootstrap creates data/alerts/ directory, Quick-Check references unified decision tree from Section 5A, Output Phase derives DailyMetrics alert fields from alert-state file.</done>
</task>

</tasks>

<verification>
1. grep "AlertState" references/data-models.md -- schema section exists
2. grep "Section 5A" references/processing-instructions.md -- decision tree section exists
3. grep "data/alerts/" SKILL.md -- bootstrap includes alerts directory
4. grep "alert-state" SKILL.md -- Quick-Check references alert-state file
5. cat data/fixtures/alert-state-sample.json | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['alerts_sent']==3; assert len(d['alert_log'])==3; print('PASS')" -- fixture valid
</verification>

<success_criteria>
- AlertState data model is defined with _schema_v: 1, max_alerts: 3, alert_log array
- Unified decision tree consolidates all alert checks into single flow with numbered steps
- Standard alert path (ALERT-06) handles items without event context
- Delta alert path is referenced but deferred to Plan 10-02
- SKILL.md bootstrap creates data/alerts/ directory
- Quick-Check flow reads/writes alert-state file, not DailyMetrics directly
- DailyMetrics alert fields are derived from alert-state at metrics write time
- Fixture file validates cap-reached scenario with 3 logged alerts
</success_criteria>

<output>
After completion, create `.planning/phases/10-dedup-hardening-alert-fatigue/10-01-SUMMARY.md`
</output>

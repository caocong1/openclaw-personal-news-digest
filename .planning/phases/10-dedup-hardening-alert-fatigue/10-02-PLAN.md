---
phase: 10-dedup-hardening-alert-fatigue
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - references/data-models.md
  - references/processing-instructions.md
  - references/output-templates.md
  - references/prompts/delta-alert.md
  - data/fixtures/events-active-v3.json
autonomous: true
requirements: [ALERT-02, ALERT-04, ALERT-05]

must_haves:
  truths:
    - "Event objects track when they were last alerted and what the alert said"
    - "Delta alerts fire when an event has new timeline entries with update/correction/reversal/escalation relations since last alert"
    - "Delta alert output shows what changed vs previous alert"
    - "Event alert memory is updated after each alert is sent"
  artifacts:
    - path: "references/data-models.md"
      provides: "Event v3 schema with alert memory fields and escalation relation"
      contains: "last_alerted_at"
    - path: "references/prompts/delta-alert.md"
      provides: "LLM prompt for delta alert generation"
      contains: "delta"
    - path: "references/output-templates.md"
      provides: "Delta alert rendering template"
      contains: "快讯更新"
    - path: "references/processing-instructions.md"
      provides: "Delta alert flow in Section 5B"
      contains: "Section 5B"
    - path: "data/fixtures/events-active-v3.json"
      provides: "Fixture for Event v3 with alert memory"
      contains: "last_alerted_at"
  key_links:
    - from: "references/processing-instructions.md Section 5A step 6"
      to: "references/processing-instructions.md Section 5B"
      via: "Delta alert path branch"
      pattern: "Section 5B"
    - from: "references/processing-instructions.md Section 5B"
      to: "references/prompts/delta-alert.md"
      via: "LLM prompt for delta summary"
      pattern: "delta-alert.md"
    - from: "references/processing-instructions.md Section 5B"
      to: "data/events/active.json"
      via: "Write last_alerted_at, last_alert_news_id, last_alert_brief after alert"
      pattern: "last_alerted_at"
---

<objective>
Add per-event alert memory fields to Event schema v3, create the delta alert prompt and template, and wire the delta alert flow into the unified decision tree.

Purpose: Enable delta alerts that show what changed since the last alert for an event, rather than repeating the full alert. This reduces alert fatigue by giving users incremental information about events they already know about.

Output: Event v3 schema with alert memory, delta-alert.md prompt, delta alert template in output-templates.md, Section 5B in processing-instructions.md, events-active-v3 fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dedup-hardening-alert-fatigue/10-01-SUMMARY.md
@references/data-models.md
@references/processing-instructions.md
@references/output-templates.md
@references/prompts/classify.md
@data/fixtures/events-active.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Event v3 schema + escalation relation + delta alert prompt + delta alert template</name>
  <files>references/data-models.md, references/prompts/delta-alert.md, references/output-templates.md, data/fixtures/events-active-v3.json</files>
  <read_first>references/data-models.md, references/output-templates.md, data/fixtures/events-active.json, references/prompts/classify.md</read_first>
  <action>
**1. Add "escalation" to Event timeline relation enum in references/data-models.md:**

In the Event section, update the timeline entry `relation` field from:
```
"relation": "initial|update|correction|analysis|reversal"
```
To:
```
"relation": "initial|update|correction|analysis|reversal|escalation"
```

Update the timeline field notes line to include escalation:
```
- `timeline`: Array of timeline entries, one per merged news item. Relation types: `initial` (first report), `update` (new developments), `correction` (fact corrections), `analysis` (commentary/interpretation), `reversal` (situation reversal), `escalation` (situation escalation/intensification)
```

This satisfies ALERT-04 which explicitly names "escalation" as a delta trigger relation.

**2. Update Event schema to v3 in references/data-models.md:**

In the Event section, add three new fields to the JSON schema (after the `timeline` array, before `_schema_v`):

```json
"last_alerted_at": "ISO8601 or null",
"last_alert_news_id": "string or null",
"last_alert_brief": "string or null",
"_schema_v": 3
```

Update `_schema_v` from 2 to 3 in the schema example.

Add field notes:
- `last_alerted_at`: ISO8601 timestamp of when this event was last used in an alert. Null if never alerted.
- `last_alert_news_id`: The news_id of the item that triggered the last alert for this event. Null if never alerted.
- `last_alert_brief`: One-sentence summary of what the last alert communicated. Used by delta alerts to describe what changed. Null if never alerted.

Add to "Defaults for missing fields" section:
- `last_alerted_at`: `null`
- `last_alert_news_id`: `null`
- `last_alert_brief`: `null`

Add three rows to New Fields Registry table:
| `last_alerted_at` | Event | Phase 10 | v3 | `null` | Timestamp of last alert for this event |
| `last_alert_news_id` | Event | Phase 10 | v3 | `null` | News ID that triggered last alert |
| `last_alert_brief` | Event | Phase 10 | v3 | `null` | Summary of last alert content |

**3. Create references/prompts/delta-alert.md:**

```markdown
<!-- prompt_version: delta-alert-v1 -->
# Delta Alert Generation

You are generating an alert UPDATE for a breaking news event. The user has already been alerted about this event before. Your job is to describe WHAT CHANGED since the last alert.

## Previous Alert

Event: {event_title}
Previous alert summary: {last_alert_brief}
Previous alert time: {last_alerted_at}

## New Developments

{For each new timeline entry since last_alerted_at:}
- [{timestamp}] {brief} (relation: {relation})

## Current Event State

{event_summary}

## Instructions

1. Write a concise delta summary (1-2 sentences) describing what changed since the previous alert
2. Focus on the DIFFERENCE, not the full story
3. Write in Chinese
4. Output JSON:

```json
{
  "delta_summary": "string (1-2 sentences describing what changed)",
  "current_status": "string (1 sentence current state)"
}
```

## Examples

Good delta_summary: "此前报道的AI安全法规草案已正式通过审议，将于下月实施。"
Bad delta_summary: "AI安全法规是一项重要的法律..." (this repeats background, not the change)
```

**4. Add delta alert template to references/output-templates.md:**

After the Breaking News Alert section and before the Transparency Footer section, add:

```markdown
## Delta Alert (Event Update)

**Trigger condition:** Unified alert decision tree (Section 5A of processing-instructions.md) routes to delta path when item has event_id AND event has last_alerted_at.

**Delta alert format:**
```
【快讯更新】{event_title}

变化: {delta_summary}
当前状态: {current_status}

新进展:
- [{timestamp}] {brief} ({relation display label})

上次快讯: {last_alert_brief} ({last_alerted_at formatted})
来源: {source_name} | 重要性: {importance_score}
```

**Rendering rules:**
- Use Timeline Relation Display Mapping for relation labels (首报, 更新, 更正, 分析, 反转, 升级)
- Show only timeline entries added since last_alerted_at (not full timeline)
- `last_alerted_at` formatted as YYYY-MM-DD HH:MM
- If delta-alert prompt fails (LLM error), fall back to standard alert format
```

**5. Create data/fixtures/events-active-v3.json:**

```json
[
  {
    "id": "evt-fix00001",
    "title": "测试事件：AI 安全法规推进",
    "summary": "多国推进 AI 安全法规立法，预计年内出台新规。",
    "first_seen": "2025-12-25T00:00:00Z",
    "last_updated": "2026-01-01T00:00:00Z",
    "status": "active",
    "topic": "ai-models",
    "importance": 0.8,
    "keywords": ["AI", "安全", "法规", "监管"],
    "item_ids": ["lang01chinese00", "fix01a2b3c4d5e6"],
    "timeline": [
      {
        "news_id": "lang01chinese00",
        "relation": "initial",
        "timestamp": "2025-12-25T00:00:00Z",
        "brief": "首次报道 AI 安全法规讨论"
      },
      {
        "news_id": "fix01a2b3c4d5e6",
        "relation": "update",
        "timestamp": "2026-01-01T00:00:00Z",
        "brief": "法规草案公开征求意见"
      }
    ],
    "last_alerted_at": "2025-12-25T08:00:00Z",
    "last_alert_news_id": "lang01chinese00",
    "last_alert_brief": "多国开始讨论AI安全法规立法",
    "_schema_v": 3
  },
  {
    "id": "evt-fix00002",
    "title": "测试事件：未曾报警的新事件",
    "summary": "一个从未触发过快讯的事件，用于测试标准快讯路径。",
    "first_seen": "2026-01-10T00:00:00Z",
    "last_updated": "2026-01-12T00:00:00Z",
    "status": "active",
    "topic": "dev-tools",
    "importance": 0.9,
    "keywords": ["开源", "工具", "发布"],
    "item_ids": ["fix02newevt0001"],
    "timeline": [
      {
        "news_id": "fix02newevt0001",
        "relation": "initial",
        "timestamp": "2026-01-10T00:00:00Z",
        "brief": "重大开源工具发布"
      }
    ],
    "last_alerted_at": null,
    "last_alert_news_id": null,
    "last_alert_brief": null,
    "_schema_v": 3
  }
]
```
  </action>
  <verify>
    <automated>grep "last_alerted_at" references/data-models.md && grep "escalation" references/data-models.md && test -f references/prompts/delta-alert.md && grep "快讯更新" references/output-templates.md && grep "last_alerted_at" data/fixtures/events-active-v3.json | head -2</automated>
  </verify>
  <acceptance_criteria>
    - references/data-models.md Event timeline relation field includes `escalation` in the enum
    - references/data-models.md Event section has `_schema_v: 3` and contains `last_alerted_at`, `last_alert_news_id`, `last_alert_brief` fields
    - references/data-models.md Event defaults section lists null defaults for all three new fields
    - references/data-models.md New Fields Registry has 3 new rows for Phase 10 Event fields
    - references/prompts/delta-alert.md exists with `prompt_version: delta-alert-v1` comment
    - references/prompts/delta-alert.md contains `{last_alert_brief}` and `{event_title}` placeholders
    - references/output-templates.md contains "## Delta Alert (Event Update)" section
    - references/output-templates.md delta alert format contains "【快讯更新】"
    - references/output-templates.md rendering rules include "升级" display label for escalation
    - data/fixtures/events-active-v3.json has 2 events, first with non-null last_alerted_at, second with null last_alerted_at
    - data/fixtures/events-active-v3.json events have `_schema_v: 3`
  </acceptance_criteria>
  <done>Event v3 schema with escalation relation and 3 alert memory fields defined, delta-alert prompt created, delta alert template added to output-templates.md, fixture file covers both alerted and never-alerted event scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Wire delta alert flow into processing-instructions.md Section 5B</name>
  <files>references/processing-instructions.md</files>
  <read_first>references/processing-instructions.md, references/prompts/delta-alert.md, references/data-models.md</read_first>
  <action>
**Add Section 5B: Delta Alert Flow to references/processing-instructions.md:**

Insert after Section 5A (Unified Alert Decision Tree). Title: "## Section 5B: Delta Alert Flow (ALERT-04, ALERT-05)"

Content:

```
When the unified decision tree (Section 5A, step 6) routes to the DELTA ALERT path, execute this flow.

### Trigger Condition

Item has `event_id` that is not null, AND the linked event in `data/events/active.json` has `last_alerted_at` that is not null. This means the user has already been alerted about this event.

### Delta Detection

1. Read the event from `data/events/active.json` by `item.event_id`
2. Find timeline entries with `timestamp > event.last_alerted_at`
3. Filter those entries to relations: `update`, `correction`, `reversal`, `escalation` (exclude `initial` and `analysis`)
   - These four relation types represent substantive developments per ALERT-04
4. If NO qualifying new timeline entries exist:
   -> Skip alert (no new developments worth alerting about)
5. If qualifying new timeline entries exist:
   -> Continue to delta alert generation

### Delta Alert Generation

1. Load prompt: Read `references/prompts/delta-alert.md`
2. Fill placeholders:
   - `{event_title}`: event.title
   - `{last_alert_brief}`: event.last_alert_brief
   - `{last_alerted_at}`: event.last_alerted_at (ISO8601)
   - `{event_summary}`: event.summary
   - New timeline entries: format each as `- [{timestamp}] {brief} (relation: {relation})`
3. Process with LLM (fast model tier -- structured JSON output)
4. Parse response JSON: `{ delta_summary, current_status }`
5. If LLM call fails: fall back to standard alert format (ALERT-06 path from Section 5A step 7)

### Rendering

Use the Delta Alert template from `references/output-templates.md` "Delta Alert (Event Update)" section:
- Fill `{delta_summary}` and `{current_status}` from LLM response
- List new timeline entries since last_alerted_at
- Show `{last_alert_brief}` and formatted `{last_alerted_at}` for context

### Event Memory Update

After successfully sending a delta alert:

1. Update the event in `data/events/active.json`:
   - Set `last_alerted_at` to current ISO8601 timestamp
   - Set `last_alert_news_id` to the triggering item's id
   - Set `last_alert_brief` to the `current_status` from LLM response (NOT the delta_summary -- brief should reflect the current state for future delta comparison)
2. Atomic write `data/events/active.json` (tmp + rename)

### Alert State Update

Same as standard alert (Section 5A step 8):
- Increment alerts_sent in alert-state file
- Append URL to alerted_urls
- Append to alert_log with `alert_type: "delta"`
- Atomic write alert-state file

### Standard Alert Memory Update

When a STANDARD alert (Section 5A step 7) fires for an item WITH an event_id (but event has no last_alerted_at -- first alert for this event):
1. Update the event in `data/events/active.json`:
   - Set `last_alerted_at` to current ISO8601 timestamp
   - Set `last_alert_news_id` to the item's id
   - Set `last_alert_brief` to the item's content_summary (or title if summary is null)
2. Atomic write `data/events/active.json`

This ensures the first alert for an event seeds the memory for future delta alerts.
```
  </action>
  <verify>
    <automated>grep -c "Section 5B" references/processing-instructions.md && grep "last_alerted_at" references/processing-instructions.md | head -3 && grep "delta-alert.md" references/processing-instructions.md && grep "escalation" references/processing-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - references/processing-instructions.md contains "## Section 5B: Delta Alert Flow (ALERT-04, ALERT-05)"
    - Section 5B contains "Delta Detection" subsection checking for timeline entries with `timestamp > event.last_alerted_at`
    - Section 5B filters relations to `update`, `correction`, `reversal`, `escalation` (all four per ALERT-04)
    - Section 5B contains "Delta Alert Generation" subsection referencing `references/prompts/delta-alert.md`
    - Section 5B contains "Event Memory Update" subsection writing `last_alerted_at`, `last_alert_news_id`, `last_alert_brief`
    - Section 5B contains "Standard Alert Memory Update" subsection for first-alert event memory seeding
    - Section 5B contains fallback instruction: "fall back to standard alert format" on LLM failure
    - Section 5B contains "alert_type: \"delta\"" in alert state update
  </acceptance_criteria>
  <done>Delta alert flow fully documented in Section 5B with detection (including escalation relation per ALERT-04), generation, rendering, event memory update, and fallback to standard alert. Standard alert also seeds event memory on first alert for future delta comparison.</done>
</task>

</tasks>

<verification>
1. grep "last_alerted_at" references/data-models.md -- Event v3 fields present
2. grep "_schema_v.*3" references/data-models.md -- schema version bumped
3. grep "escalation" references/data-models.md -- escalation relation in enum
4. cat references/prompts/delta-alert.md | head -1 -- prompt exists with version comment
5. grep "快讯更新" references/output-templates.md -- delta template exists
6. grep "Section 5B" references/processing-instructions.md -- delta flow documented
7. grep "escalation" references/processing-instructions.md -- escalation in delta trigger set
8. cat data/fixtures/events-active-v3.json | python3 -c "import json,sys; d=json.load(sys.stdin); assert d[0]['last_alerted_at'] is not None; assert d[1]['last_alerted_at'] is None; assert d[0]['_schema_v']==3; print('PASS')"
</verification>

<success_criteria>
- Event timeline relation enum includes "escalation" (per ALERT-04 requirement)
- Event objects have 3 alert memory fields (last_alerted_at, last_alert_news_id, last_alert_brief) with null defaults
- Delta alert prompt exists and instructs LLM to describe what changed vs previous alert
- Delta alert template renders in Chinese with 【快讯更新】header
- Delta detection considers update/correction/reversal/escalation relations (per ALERT-04)
- Event memory is updated after both standard and delta alerts
- LLM failure falls back gracefully to standard alert
- Fixture covers both alerted and never-alerted event scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/10-dedup-hardening-alert-fatigue/10-02-SUMMARY.md`
</output>

---
phase: 09-noise-floor-classification-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/data-models.md
  - config/sources.json
  - references/processing-instructions.md
  - SKILL.md
autonomous: true
requirements: [NOISE-01, NOISE-03, NOISE-04, NOISE-05]

must_haves:
  truths:
    - "Items matching source noise_patterns are skipped before any LLM call"
    - "Filtered items remain in JSONL with processing_status noise_filtered and digest_eligible false"
    - "Sources have configurable noise_patterns and title_discard_patterns in fetch_config"
    - "DailyMetrics tracks noise_filter_suppressed count"
    - "SKILL.md pipeline includes noise filter step between collect-unprocessed and classify-batch"
  artifacts:
    - path: "references/data-models.md"
      provides: "NewsItem v4 with digest_eligible field, DailyMetrics noise_filter_suppressed, Source noise_patterns"
      contains: "digest_eligible"
    - path: "config/sources.json"
      provides: "All sources have noise_patterns and title_discard_patterns in fetch_config"
      contains: "noise_patterns"
    - path: "references/processing-instructions.md"
      provides: "Section 0E pre-classify noise filter procedure"
      contains: "Section 0E"
    - path: "SKILL.md"
      provides: "Noise filter step wired into Processing Phase"
      contains: "noise filter"
  key_links:
    - from: "SKILL.md"
      to: "references/processing-instructions.md"
      via: "Section 0E cross-reference in Processing Phase"
      pattern: "Section 0E"
    - from: "references/processing-instructions.md"
      to: "config/sources.json"
      via: "noise_patterns lookup by source_id"
      pattern: "noise_patterns"
    - from: "references/data-models.md"
      to: "references/processing-instructions.md"
      via: "digest_eligible field used by filter logic"
      pattern: "digest_eligible"
---

<objective>
Add pre-classify noise filtering infrastructure: extend data models with digest_eligible field and noise_filter_suppressed metric, add noise_patterns/title_discard_patterns to source schema and config, create the Section 0E pre-classify noise filter procedure in processing-instructions.md, and wire the noise filter step into SKILL.md's Processing Phase pipeline.

Purpose: Items matching known noise patterns (ads, promotional content, aggregation roundups) are skipped before consuming any LLM budget, saving cost and improving output quality.
Output: Updated data models, source config, processing instructions, and SKILL.md with complete pre-classify noise filter pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-noise-floor-classification-quality/09-RESEARCH.md

<interfaces>
<!-- Key schemas and pipeline structure the executor needs -->

From references/data-models.md - NewsItem current schema (v3):
```json
{
  "processing_status": "raw|partial|complete",
  "duplicate_of": null,
  "_schema_v": 3
}
```
Must add: `"digest_eligible": true` (new field), bump to `_schema_v: 4`
Must add new processing_status value: `"noise_filtered"`

From references/data-models.md - DailyMetrics.items:
```json
{
  "items": {
    "fetched": 0,
    "url_deduped": 0,
    "title_deduped": 0,
    "classified": 0,
    "partial": 0,
    "selected_for_output": 0
  }
}
```
Must add: `"noise_filter_suppressed": 0` inside `items`

From references/data-models.md - Source.fetch_config variants:
```
| Type | fetch_config Fields |
| rss  | {}                  |
```
Must add `noise_patterns` and `title_discard_patterns` (arrays, default `[]`) applicable to ALL source types.

From SKILL.md Processing Phase steps (current):
```
2. **Collect unprocessed**: Find items with processing_status: "raw" from today's JSONL.
3. **Classify batch**: Group 5-10 items per LLM call.
```
Must insert noise filter step between steps 2 and 3.

From references/processing-instructions.md section numbering:
```
Section 0D: Pre-Write Quality Contract
Section 1: Batch LLM Processing
```
Must add Section 0E between 0D and 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend data models and source config for noise filtering</name>
  <files>references/data-models.md, config/sources.json</files>
  <read_first>references/data-models.md, config/sources.json</read_first>
  <action>
**In references/data-models.md:**

1. **NewsItem schema (bump v3 -> v4):** Add `digest_eligible` field to the NewsItem JSON schema block, positioned after `duplicate_of` and before `_schema_v`:
```json
"digest_eligible": true,
"_schema_v": 4
```

2. **NewsItem processing_status field notes:** Update the `processing_status` field note to include the new value:
```
- `processing_status`: `raw` = just fetched, `partial` = classification or summary failed, `complete` = fully processed, `noise_filtered` = matched noise pattern, skipped LLM
```

3. **NewsItem defaults section:** Add these two entries to the "Defaults for missing fields (older schema versions)" list:
```
- `digest_eligible`: `true` (items from v3 and earlier are eligible since they passed the old pipeline)
```

4. **DailyMetrics schema:** Add `noise_filter_suppressed` to the `items` object in the DailyMetrics JSON schema block, after `selected_for_output`:
```json
"noise_filter_suppressed": 0
```

5. **Source fetch_config table:** Add a new row to the `fetch_config variants by type` table:
```
| (all types) | `{ ..., noise_patterns?, title_discard_patterns? }` | `noise_patterns`: array of regex strings to match against item titles (case-insensitive). `title_discard_patterns`: array of regex strings for exact title matches to discard. Both default to `[]` if absent. Applied by pre-classify noise filter (processing-instructions.md Section 0E). |
```
Actually, add a note BELOW the fetch_config variants table instead (since these fields apply to ALL types, not a specific type):
```
**Common fetch_config fields (all source types):**
- `noise_patterns`: Array of regex strings. Items whose title matches any pattern (case-insensitive) are noise-filtered before LLM classification. Default: `[]` (no filtering).
- `title_discard_patterns`: Array of regex strings. Items whose title exactly matches any pattern (case-insensitive full-match) are discarded. Default: `[]` (no filtering).
```

6. **New Fields Registry:** Add these entries to the New Fields Registry table:
```
| `digest_eligible` | NewsItem | Phase 9 | v4 | `true` | Noise filter eligibility flag |
| `noise_filter_suppressed` | DailyMetrics.items | Phase 9 | - | `0` | Count of items filtered by noise/importance |
| `noise_patterns` | Source.fetch_config | Phase 9 | - | `[]` | Per-source noise regex patterns |
| `title_discard_patterns` | Source.fetch_config | Phase 9 | - | `[]` | Per-source title discard patterns |
```

**In config/sources.json:**

Add `noise_patterns` and `title_discard_patterns` arrays to the `fetch_config` object of every source entry. All arrays start empty (conservative default per research recommendation):

- src-36kr: `"noise_patterns": [], "title_discard_patterns": []`
- src-github-langchain: Add to existing fetch_config: `"noise_patterns": [], "title_discard_patterns": []`
- src-search-ai-regulation: Add to existing fetch_config: `"noise_patterns": [], "title_discard_patterns": []`
- src-official-openai-blog: Add to existing fetch_config: `"noise_patterns": [], "title_discard_patterns": []`
- src-community-hackernews: Add to existing fetch_config: `"noise_patterns": [], "title_discard_patterns": []`
- src-ranking-github-trending: Add to existing fetch_config: `"noise_patterns": [], "title_discard_patterns": []`
  </action>
  <verify>
    <automated>grep -c "digest_eligible" references/data-models.md && grep -c "noise_filter_suppressed" references/data-models.md && grep -c "noise_patterns" config/sources.json && grep -c "_schema_v.*4" references/data-models.md</automated>
  </verify>
  <acceptance_criteria>
    - references/data-models.md contains `"digest_eligible": true`
    - references/data-models.md contains `_schema_v": 4` in NewsItem schema
    - references/data-models.md contains `"noise_filter_suppressed": 0` in DailyMetrics items
    - references/data-models.md contains `noise_patterns` in fetch_config documentation
    - references/data-models.md contains `processing_status` field note listing `noise_filtered` as a valid value
    - references/data-models.md New Fields Registry has rows for `digest_eligible`, `noise_filter_suppressed`, `noise_patterns`, `title_discard_patterns`
    - config/sources.json: all 6 source entries contain `"noise_patterns": []` in fetch_config
    - config/sources.json: all 6 source entries contain `"title_discard_patterns": []` in fetch_config
  </acceptance_criteria>
  <done>NewsItem schema bumped to v4 with digest_eligible field, DailyMetrics has noise_filter_suppressed, Source schema documents noise_patterns and title_discard_patterns, all 6 sources in config have empty noise pattern arrays</done>
</task>

<task type="auto">
  <name>Task 2: Create Section 0E pre-classify noise filter and wire into SKILL.md</name>
  <files>references/processing-instructions.md, SKILL.md</files>
  <read_first>references/processing-instructions.md, SKILL.md</read_first>
  <action>
**In references/processing-instructions.md:**

Add a new Section 0E between the existing Section 0D (Pre-Write Quality Contract) and Section 1 (Batch LLM Processing). Insert the following content:

```markdown
---

## Section 0E: Pre-Classify Noise Filter (NOISE-01, NOISE-04)

Before LLM classification, apply pattern-based filtering to skip obvious noise items. This saves LLM budget by preventing promotional, aggregation, and low-signal content from reaching the classify call.

### When to Run

During Processing Phase, after collecting unprocessed items (step 2) and before classification batch (step 3). This filter operates on items with `processing_status: "raw"` only.

### Filter Procedure

1. Read `config/sources.json` to load `fetch_config.noise_patterns` and `fetch_config.title_discard_patterns` for each source
2. For each item with `processing_status: "raw"`:
   a. Look up the item's `source_id` in sources.json
   b. Get `noise_patterns` array (default `[]` if absent or missing from fetch_config)
   c. Get `title_discard_patterns` array (default `[]` if absent or missing from fetch_config)
   d. Test `item.title` against each pattern in `noise_patterns` (case-insensitive regex partial match)
   e. Test `item.title` against each pattern in `title_discard_patterns` (case-insensitive regex full match)
   f. If ANY pattern matches:
      - Set `processing_status: "noise_filtered"`
      - Set `digest_eligible: false`
      - Remove item from the LLM classification batch (do NOT send to classify or summarize)
      - Increment `noise_filter_suppressed` counter in run metrics
      - Log: `"Noise filtered: {item.title} (source: {source_id}, matched: {pattern})"`
   g. If NO pattern matches: keep item in classification batch, proceed normally

### Backward Compatibility

- Sources without `noise_patterns` or `title_discard_patterns` in `fetch_config` default to empty arrays (no filtering applied)
- Existing items without `digest_eligible` field default to `true` (eligible)
- Items with `processing_status: "noise_filtered"` are NOT picked up by breakpoint resume (Section 3) -- they are permanently filtered for this run

### Interaction with Other Pipeline Steps

- Noise-filtered items remain in JSONL (written during Collection Phase step 7) -- they are NOT deleted
- Noise-filtered items are excluded from classification (step 3), summarization (step 4), title dedup (step 8), and event merge (step 10)
- Noise-filtered items are excluded from the scoring pool in Output Phase step 1
- History queries (Section 8) that look for "all items from a source" should use `processing_status != "raw"` rather than `== "complete"` to include noise-filtered items in counts
```

**In SKILL.md:**

In the Processing Phase section, insert a new step 2.5 (numbered as part of the flow, between existing step 2 and step 3). Update as follows:

After the current step 2:
```
2. **Collect unprocessed**: Find items with `processing_status: "raw"` from today's JSONL.
```

Insert:
```
2.5. **Pre-classify noise filter**: For each raw item, check `config/sources.json` `fetch_config.noise_patterns` and `title_discard_patterns`. Items matching any pattern: set `processing_status: "noise_filtered"`, `digest_eligible: false`, remove from batch, increment `noise_filter_suppressed`. See `{baseDir}/references/processing-instructions.md` Section 0E.
```

Also update Output Phase step 1 to exclude noise-filtered and digest-ineligible items from scoring. Change:
```
1. **Score items**: Read `{baseDir}/references/scoring-formula.md`. Score all completed items (all 7 dimensions active, including event_boost from `data/events/active.json`), sort by `final_score` descending. Exclude items with `dedup_status: "title_dup"` or `"url_dup"` from the scoring pool.
```
To:
```
1. **Score items**: Read `{baseDir}/references/scoring-formula.md`. Score all completed items (all 7 dimensions active, including event_boost from `data/events/active.json`), sort by `final_score` descending. Exclude items with `dedup_status: "title_dup"` or `"url_dup"` from the scoring pool. Exclude items with `digest_eligible: false` from the scoring pool.
```
  </action>
  <verify>
    <automated>grep -c "Section 0E" references/processing-instructions.md && grep -c "noise filter" SKILL.md && grep -c "digest_eligible: false" SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - references/processing-instructions.md contains `## Section 0E: Pre-Classify Noise Filter`
    - references/processing-instructions.md Section 0E contains `processing_status: "noise_filtered"`
    - references/processing-instructions.md Section 0E contains `digest_eligible: false`
    - references/processing-instructions.md Section 0E contains `noise_filter_suppressed`
    - references/processing-instructions.md Section 0E appears between Section 0D and Section 1
    - SKILL.md Processing Phase contains step `2.5` referencing `noise filter` and `Section 0E`
    - SKILL.md Output Phase step 1 contains `digest_eligible: false` exclusion
  </acceptance_criteria>
  <done>Section 0E pre-classify noise filter procedure exists in processing-instructions.md with complete filter logic, SKILL.md has noise filter step 2.5 in Processing Phase and digest_eligible exclusion in Output Phase scoring</done>
</task>

</tasks>

<verification>
1. `grep "Section 0E" references/processing-instructions.md` returns the section header
2. `grep "noise_filtered" references/data-models.md` returns the processing_status value
3. `grep "digest_eligible" references/data-models.md` returns the new field definition
4. `grep "noise_filter_suppressed" references/data-models.md` returns the metrics field
5. `grep "noise_patterns" config/sources.json` returns entries for all 6 sources
6. `grep "noise filter" SKILL.md` returns the new pipeline step
7. `grep "digest_eligible" SKILL.md` returns the scoring pool exclusion
</verification>

<success_criteria>
- Pre-classify noise filter pipeline is fully specified: source patterns -> filter logic -> status marking -> metrics tracking
- NewsItem schema v4 with digest_eligible field, backward-compatible default of true
- All 6 sources have noise_patterns and title_discard_patterns in fetch_config (starting empty)
- DailyMetrics tracks noise_filter_suppressed count
- SKILL.md Processing Phase includes noise filter step 2.5
- SKILL.md Output Phase excludes digest_eligible: false from scoring
- Filtered items remain in JSONL (not deleted) for history queryability
</success_criteria>

<output>
After completion, create `.planning/phases/09-noise-floor-classification-quality/09-01-SUMMARY.md`
</output>

---
phase: 09-noise-floor-classification-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - references/prompts/classify.md
  - config/categories.json
  - references/processing-instructions.md
autonomous: true
requirements: [CLASS-01, CLASS-02, CLASS-03]

must_haves:
  truths:
    - "Classification prompt has concrete 0.0-0.2 tier examples showing what low-importance items look like"
    - "Classification prompt has disambiguation rules for commonly confused category pairs"
    - "Each category has 2-3 negative examples showing what does NOT belong in that category"
    - "Cache version bumped to classify-v2 forcing re-classification of all cached items"
    - "Prompt assembly step formats negative_examples into the {categories_list} placeholder so the LLM actually sees them"
  artifacts:
    - path: "references/prompts/classify.md"
      provides: "Hardened classification prompt with low-end calibration and disambiguation"
      contains: "classify-v2"
    - path: "config/categories.json"
      provides: "Category definitions with negative_examples field"
      contains: "negative_examples"
    - path: "references/processing-instructions.md"
      provides: "Updated Fill categories step including negative_examples in formatted output"
      contains: "negative_examples"
  key_links:
    - from: "references/prompts/classify.md"
      to: "config/categories.json"
      via: "Prompt assembly reads negative_examples from categories and injects into prompt"
      pattern: "negative_examples"
    - from: "references/prompts/classify.md"
      to: "data/cache/classify-cache.json"
      via: "prompt_version classify-v2 forces cache miss on v1 entries"
      pattern: "classify-v2"
    - from: "references/processing-instructions.md"
      to: "config/categories.json"
      via: "Section 1 Step 2 Fill categories formats negative_examples into {categories_list}"
      pattern: "negative_examples"
---

<objective>
Harden the classification prompt with better low-end calibration (0.0-0.2 tier examples), disambiguation rules for commonly confused category pairs, and per-category negative examples. Bump cache version to classify-v2 to force re-classification with the improved prompt. Wire negative_examples into the prompt assembly step so values actually reach the LLM.

Purpose: Improve classification accuracy at the low end of the importance spectrum and reduce category confusion for ambiguous items, leading to better noise filtering and more accurate digest curation.
Output: Updated classify.md prompt, categories.json with negative examples, and processing-instructions.md with negative_examples wiring in prompt assembly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-noise-floor-classification-quality/09-RESEARCH.md

<interfaces>
<!-- Current classify.md prompt structure -->

From references/prompts/classify.md:
```
<!-- prompt_version: classify-v1 -->
```
Must change to: `<!-- prompt_version: classify-v2 -->`

Current Importance Score Reference section:
```
- **0.9-1.0**: Major events with broad impact
- **0.7-0.8**: Significant industry/community events
- **0.5-0.6**: Noteworthy developments
- **0.3-0.4**: General information
- **0.0-0.2**: Low information density (repetitive coverage, clickbait, pure marketing)
```
The 0.0-0.2 tier needs concrete examples added.

From config/categories.json - current category structure:
```json
{
  "id": "ai-models",
  "name_zh": "AI 模型",
  "name_en": "AI Models",
  "description": "...",
  "adjacent": ["dev-tools", "science", "open-source"]
}
```
Must add: `"negative_examples": [...]` array to each category.

From references/processing-instructions.md Section 1 - Classification Batch:
```
2. **Fill categories**: Read config/categories.json, format the 12 categories into {categories_list} placeholder
```
The prompt assembly already reads categories.json. The negative_examples field must be included when filling the {categories_list} placeholder. This step must be updated to explicitly format negative_examples values into the output so the LLM sees them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden classify.md prompt and wire negative_examples into prompt assembly</name>
  <files>references/prompts/classify.md, references/processing-instructions.md</files>
  <read_first>references/prompts/classify.md, config/categories.json, references/processing-instructions.md</read_first>
  <action>
**In references/prompts/classify.md:**

**Update prompt_version:** Change line 1 from:
```
<!-- prompt_version: classify-v1 -->
```
To:
```
<!-- prompt_version: classify-v2 -->
```

**Expand the 0.0-0.2 tier** in the Importance Score Reference section. Replace the current 0.0-0.2 line:
```
- **0.0-0.2**: Low information density (repetitive coverage, clickbait, pure marketing)
```
With an expanded version containing concrete examples:
```
- **0.0-0.2**: Low information density. Assign this range to items that add little unique information value:
  - Repetitive coverage of already-well-reported events with no new facts
  - Clickbait titles without substantive content behind them
  - Pure marketing or promotional content (product ads, sponsored posts)
  - Routine minor version bumps with no notable changes (e.g., "v1.2.3 patch release")
  - Aggregated round-up posts that compile links without original analysis (e.g., "每日精选", "Weekly Roundup")
  - Social media reaction compilations without expert analysis
  - Press releases restating previously announced information
```

**Add Borderline Examples section** after the Importance Score Reference section and before the Output Format section. Insert:
```markdown
## Borderline Examples

Items near score boundaries -- use these as calibration anchors:

| Title Example | Score | Rationale |
|---------------|-------|-----------|
| "v2.1.3 bug fix release" | 0.1 | Routine patch, no notable changes |
| "本周 AI 领域十大热门文章" | 0.1 | Aggregation roundup, no original content |
| "某某公司获得 A 轮融资" (small unknown startup) | 0.3 | General business news, limited industry impact |
| "某某公司获得 B 轮 5000 万美元融资" (known company) | 0.5 | Noteworthy funding with market signal |
| "Google 发布新版 Gemini 模型" | 0.8 | Significant model release from major player |
| "重大安全漏洞影响百万用户" | 0.9 | Critical security incident with broad impact |
```

**Add Disambiguation Rules section** after the Borderline Examples section and before the Output Format section. Insert:
```markdown
## Disambiguation Rules

When an item could belong to multiple categories, use these rules to determine the primary category:

- "AI startup raises funding" -> `business` (NOT `ai-models`), unless the funding is specifically tied to a new model release
- "New IDE with AI features" -> `dev-tools` (NOT `ai-models`), unless the AI capability itself is the primary news
- "Open source AI model released" -> `ai-models` (primary), `open-source` as tag. The model is the news, not the license.
- "Security vulnerability in AI system" -> `security` (NOT `ai-models`), unless the vulnerability is inherent to the model architecture itself
- "Government bans AI technology" -> `macro-policy` (NOT `ai-models`). Policy is the action; AI is the subject.
- "Game engine adds AI generation" -> `gaming` or `dev-tools` depending on audience (game devs -> `dev-tools`, gamers -> `gaming`). NOT `ai-models`.
- "Crypto exchange launches new product" -> `finance` (NOT `tech-products`). Financial product, not tech product.
- "Open source project changes license" -> `open-source` (NOT `business`). License is the core community issue.
- "Country restricts tech exports" -> `international` (NOT `macro-policy`), if cross-border. `macro-policy` if domestic-only.

**General principle:** Classify by the PRIMARY ACTION or EVENT, not by the subject domain. "AI" as a subject does not automatically mean `ai-models` -- the action (funding, regulation, vulnerability, tool release) determines the category.
```

**Add Negative Examples instruction** to the Available Categories section. After the existing note:
```
_(Note: The agent reads `config/categories.json` to fill this placeholder. Each category has an `id`, `name_zh`, `name_en`, and `description`.)_
```
Add:
```
_(Each category also includes `negative_examples` -- items that should NOT be classified under that category. Use these to avoid common misclassifications.)_
```

**Update the {categories_list} formatting instruction** by adding to the Classification Guidelines section, after the `primary` guideline:
```
- When `negative_examples` are provided for a category, actively check whether the item matches a negative example before assigning that category. If it does, choose the alternative category suggested in the negative example.
```

**In references/processing-instructions.md:**

In Section 1 "Classification Batch", update Step 2 ("Fill categories"). Change the current line:
```
2. **Fill categories**: Read `config/categories.json`, format the 12 categories into `{categories_list}` placeholder
```
To:
```
2. **Fill categories**: Read `config/categories.json`, format the 12 categories into `{categories_list}` placeholder. For each category, include `id`, `name_zh`, `name_en`, `description`, and `negative_examples` (the list of items that should NOT be classified under that category). The formatted output for each category should look like:
   ```
   - {id} ({name_zh} / {name_en}): {description}
     NOT this category: {negative_examples[0]}; {negative_examples[1]}; {negative_examples[2]}
   ```
   This ensures the LLM sees both what belongs in a category and what does not.
```
  </action>
  <verify>
    <automated>grep -c "classify-v2" references/prompts/classify.md && grep -c "Disambiguation Rules" references/prompts/classify.md && grep -c "Borderline Examples" references/prompts/classify.md && grep -c "negative_examples" references/prompts/classify.md && grep -c "negative_examples" references/processing-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - references/prompts/classify.md line 1 contains `<!-- prompt_version: classify-v2 -->`
    - references/prompts/classify.md contains `## Borderline Examples` section with at least 6 example rows
    - references/prompts/classify.md contains `## Disambiguation Rules` section with at least 9 rules
    - references/prompts/classify.md 0.0-0.2 tier has at least 5 concrete example types (repetitive, clickbait, marketing, minor version, aggregated)
    - references/prompts/classify.md mentions `negative_examples` in Available Categories note and Classification Guidelines
    - references/processing-instructions.md Section 1 Step 2 "Fill categories" explicitly includes `negative_examples` in the formatted `{categories_list}` output with the `NOT this category:` format
  </acceptance_criteria>
  <done>Classify prompt version bumped to v2 with expanded 0.0-0.2 tier examples, borderline calibration anchors, disambiguation rules for 9+ confused pairs, negative_examples integration guidance, AND processing-instructions.md Section 1 Step 2 updated to format negative_examples into the {categories_list} placeholder so values actually reach the LLM</done>
</task>

<task type="auto">
  <name>Task 2: Add negative_examples to all categories in categories.json</name>
  <files>config/categories.json</files>
  <read_first>config/categories.json</read_first>
  <action>
Add a `negative_examples` array to each of the 12 category objects in `config/categories.json`. Each array contains 2-3 strings describing items that should NOT be classified under that category, with the correct alternative category in parentheses. Place the field after `adjacent`.

Here are the exact negative_examples for each category:

**ai-models:**
```json
"negative_examples": [
  "App using AI API without model innovation (-> tech-products)",
  "AI company funding/acquisition (-> business)",
  "Government AI regulation or policy (-> macro-policy)"
]
```

**dev-tools:**
```json
"negative_examples": [
  "Consumer app that happens to use a framework (-> tech-products)",
  "Developer hiring/salary trends (-> business)",
  "Security vulnerability in a dev tool (-> security)"
]
```

**tech-products:**
```json
"negative_examples": [
  "Developer SDK or API release (-> dev-tools)",
  "Product company stock performance (-> finance)",
  "Product company regulatory action (-> macro-policy)"
]
```

**business:**
```json
"negative_examples": [
  "Product feature announcement (-> tech-products)",
  "Stock price movement without business context (-> finance)",
  "Company open-sources a project (-> open-source)"
]
```

**finance:**
```json
"negative_examples": [
  "Startup funding round (-> business)",
  "Financial regulation law passed (-> macro-policy)",
  "Fintech product feature launch (-> tech-products)"
]
```

**macro-policy:**
```json
"negative_examples": [
  "Company voluntary policy change (-> business)",
  "Cross-border trade dispute (-> international)",
  "Industry self-regulation standard (-> business)"
]
```

**international:**
```json
"negative_examples": [
  "Domestic-only policy change (-> macro-policy)",
  "Single-country company news (-> business)",
  "International scientific collaboration (-> science)"
]
```

**security:**
```json
"negative_examples": [
  "Privacy policy update by company (-> macro-policy)",
  "Security company funding round (-> business)",
  "Security tool new feature release (-> dev-tools)"
]
```

**open-source:**
```json
"negative_examples": [
  "Commercial product with open API (-> tech-products)",
  "Open source company IPO (-> business)",
  "Using open source in proprietary product (-> tech-products)"
]
```

**gaming:**
```json
"negative_examples": [
  "Gaming company stock/earnings (-> finance)",
  "Game engine used for non-game purpose (-> dev-tools)",
  "Gaming regulation by government (-> macro-policy)"
]
```

**science:**
```json
"negative_examples": [
  "Applied AI product using research (-> ai-models or tech-products)",
  "Science funding policy (-> macro-policy)",
  "Biotech company IPO (-> business)"
]
```

**breaking:**
```json
"negative_examples": [
  "Routine product update even if from major company (-> appropriate category)",
  "Scheduled event or conference announcement (-> appropriate category)",
  "Follow-up coverage of yesterday's breaking news (-> appropriate category)"
]
```
  </action>
  <verify>
    <automated>grep -c "negative_examples" config/categories.json</automated>
  </verify>
  <acceptance_criteria>
    - config/categories.json contains `"negative_examples"` in all 12 category objects (grep count = 12 or more)
    - Each category's negative_examples array has exactly 3 entries
    - Each negative example includes the correct alternative category in parentheses with `->` arrow format
    - JSON is valid (no trailing commas, proper bracket nesting)
    - Existing fields (id, name_zh, name_en, description, adjacent) are unchanged
  </acceptance_criteria>
  <done>All 12 categories in categories.json have negative_examples arrays with 3 entries each, covering the most commonly confused classification boundary cases</done>
</task>

</tasks>

<verification>
1. `grep "classify-v2" references/prompts/classify.md` returns the version comment
2. `grep "Disambiguation Rules" references/prompts/classify.md` returns the section header
3. `grep "Borderline Examples" references/prompts/classify.md` returns the section header
4. `grep -c "negative_examples" config/categories.json` returns 12 or more
5. `grep "negative_examples" references/processing-instructions.md` returns the updated Fill categories step with NOT this category format
6. Validate JSON: the file can be parsed without errors
</verification>

<success_criteria>
- Classify prompt is version classify-v2 (all classify-v1 cache entries will miss on next run)
- 0.0-0.2 importance tier has 5+ concrete example types for better LLM calibration
- Borderline Examples table provides 6 calibration anchors across the score range
- Disambiguation Rules section covers 9+ commonly confused category pairs
- All 12 categories have 3 negative examples each in categories.json
- Negative examples use consistent format with alternative category suggestion
- processing-instructions.md Section 1 Step 2 explicitly formats negative_examples into {categories_list} so values are injected into the LLM prompt
</success_criteria>

<output>
After completion, create `.planning/phases/09-noise-floor-classification-quality/09-02-SUMMARY.md`
</output>

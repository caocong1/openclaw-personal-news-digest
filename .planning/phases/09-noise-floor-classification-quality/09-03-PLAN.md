---
phase: 09-noise-floor-classification-quality
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - references/processing-instructions.md
  - data/fixtures/news-items-noise-filtered.jsonl
  - data/fixtures/metrics-sample.json
autonomous: true
requirements: [NOISE-02, NOISE-05]

must_haves:
  truths:
    - "Items with importance_score < 0.25 are marked digest_eligible false after classification"
    - "Post-classify filtered items skip summarization to save LLM budget"
    - "noise_filter_suppressed counter includes both pre-classify and post-classify filtered items"
    - "Fixture files demonstrate all noise filtering scenarios with correct field values"
  artifacts:
    - path: "references/processing-instructions.md"
      provides: "Post-classify importance filter procedure"
      contains: "Post-Classify Importance Filter"
    - path: "data/fixtures/news-items-noise-filtered.jsonl"
      provides: "Test fixture with noise-filtered and low-importance items"
      contains: "noise_filtered"
    - path: "data/fixtures/metrics-sample.json"
      provides: "Updated metrics fixture with noise_filter_suppressed field"
      contains: "noise_filter_suppressed"
  key_links:
    - from: "references/processing-instructions.md"
      to: "SKILL.md"
      via: "SKILL.md step 3 references post-classify filter logic"
      pattern: "importance.*0.25"
    - from: "data/fixtures/news-items-noise-filtered.jsonl"
      to: "references/data-models.md"
      via: "Fixture items use NewsItem v4 schema with digest_eligible"
      pattern: "digest_eligible"
---

<objective>
Add post-classify importance filter (items with importance < 0.25 marked digest_eligible: false and skip summarization), wire it into SKILL.md, and create/update test fixtures for all noise filtering scenarios.

Purpose: After LLM classification assigns importance scores, items below the noise floor threshold are excluded from scoring and summarization, saving additional LLM budget and improving digest quality.
Output: Post-classify filter procedure, updated SKILL.md, new noise-filtered fixture file, updated metrics fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-noise-floor-classification-quality/09-RESEARCH.md
@.planning/phases/09-noise-floor-classification-quality/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From references/data-models.md (after Plan 01):
- NewsItem has `digest_eligible` field (default: true), `_schema_v: 4`
- NewsItem `processing_status` includes `"noise_filtered"` as valid value
- DailyMetrics.items has `noise_filter_suppressed: 0`

From references/processing-instructions.md (after Plan 01):
- Section 0E exists: Pre-Classify Noise Filter
- Section 1 exists: Batch LLM Processing (classification then summarization)
The post-classify filter must be inserted AFTER classification (Section 1 "Classification Batch") and BEFORE summarization (Section 1 "Summarization Batch").

From SKILL.md (after Plan 01):
- Processing Phase step 2.5: pre-classify noise filter
- Processing Phase step 3: Classify batch
- Processing Phase step 4: Summarize batch
The post-classify filter must be referenced as step 3.5 (between classify and summarize).

From data/fixtures/news-items-complete.jsonl (existing fixture):
```json
{"id":"fix01a2b3c4d5e6","title":"测试新闻：AI 模型发布",...,"_schema_v":3}
```
Schema v3 -- new fixture must use v4 with digest_eligible field.

From data/fixtures/metrics-sample.json (existing fixture):
```json
{"items":{"fetched":25,"url_deduped":5,"title_deduped":2,"classified":18,"partial":0,"selected_for_output":15}}
```
Must add `"noise_filter_suppressed": 0` to items object.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add post-classify importance filter to processing-instructions.md and SKILL.md</name>
  <files>references/processing-instructions.md, SKILL.md</files>
  <read_first>references/processing-instructions.md, SKILL.md</read_first>
  <action>
**In references/processing-instructions.md:**

Within Section 1 (Batch LLM Processing), add a new subsection between "Classification Batch" and "Summarization Batch". Insert the following content after the Classification Batch subsection ends and before the "### Summarization Batch" heading:

```markdown
### Post-Classify Importance Filter (NOISE-02)

After classification assigns importance scores, filter items below the noise floor threshold. This prevents low-value items from consuming summarization LLM budget.

#### When to Run

Immediately after each classification batch completes (Section 1 "Classification Batch" step 6), before summarization begins.

#### Filter Procedure

For each item that received fresh classification results in this batch:

1. If `importance_score < 0.25`:
   - Set `digest_eligible: false`
   - Keep `processing_status` at its current value (do NOT change to `"noise_filtered"` -- classification DID succeed, this is a threshold filter not a pattern filter)
   - Skip this item from the summarization batch (do NOT send to summarize LLM call)
   - Increment `noise_filter_suppressed` counter in run metrics
   - Log: `"Low-importance filtered: {item.title} (score: {importance_score})"`
2. If `importance_score >= 0.25`:
   - Set `digest_eligible: true`
   - Include item in the summarization batch as normal

#### Processing Status Behavior

Unlike the pre-classify noise filter (Section 0E) which sets `processing_status: "noise_filtered"`, the post-classify filter keeps `processing_status` as-is. After summarization completes for eligible items:
- Items with `importance_score >= 0.25` proceed to `processing_status: "complete"` as normal
- Items with `importance_score < 0.25` and `digest_eligible: false` get `processing_status: "complete"` (classification succeeded) but they skip summarization, so `content_summary` remains `null`

This means `processing_status: "complete"` items may have `digest_eligible: false` -- the scoring pool must check BOTH `processing_status` and `digest_eligible`.

#### Threshold Constant

The importance threshold `0.25` is defined here. To adjust the noise floor sensitivity, change this single value. A lower threshold (e.g., 0.15) lets more items through; a higher threshold (e.g., 0.35) filters more aggressively.

#### Interaction with noise_filter_suppressed

The `noise_filter_suppressed` counter in DailyMetrics is the SUM of:
- Pre-classify filtered items (Section 0E): items matching noise_patterns
- Post-classify filtered items (this section): items with importance_score < 0.25

Both stages increment the same counter. This gives a single metric for "total items removed from the digest pipeline."
```

**In SKILL.md:**

In the Processing Phase section, insert a new step 3.5 between existing step 3 (Classify batch) and step 4 (Summarize batch):

After the current step 3:
```
3. **Classify batch**: Group 5-10 items per LLM call. Assign `categories`, `importance_score`, `form_type`, `tags`.
```

Insert:
```
3.5. **Post-classify importance filter**: For each classified item with `importance_score < 0.25`: set `digest_eligible: false`, skip summarization, increment `noise_filter_suppressed`. See `{baseDir}/references/processing-instructions.md` Section 1 "Post-Classify Importance Filter".
```
  </action>
  <verify>
    <automated>grep -c "Post-Classify Importance Filter" references/processing-instructions.md && grep -c "importance_score < 0.25" references/processing-instructions.md && grep -c "3.5" SKILL.md && grep -c "Post-classify importance filter" SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - references/processing-instructions.md contains `### Post-Classify Importance Filter (NOISE-02)` within Section 1
    - references/processing-instructions.md contains `importance_score < 0.25` as the threshold
    - references/processing-instructions.md post-classify filter specifies setting `digest_eligible: false`
    - references/processing-instructions.md post-classify filter specifies skipping summarization for filtered items
    - references/processing-instructions.md documents that `noise_filter_suppressed` is the sum of pre-classify and post-classify
    - SKILL.md Processing Phase contains step `3.5` mentioning `Post-classify importance filter`
    - SKILL.md step 3.5 contains `importance_score < 0.25` and `digest_eligible: false`
  </acceptance_criteria>
  <done>Post-classify importance filter is fully specified in processing-instructions.md Section 1 with threshold 0.25, summarization skip, and metrics tracking. SKILL.md has step 3.5 wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Create noise-filtered fixture and update metrics fixture</name>
  <files>data/fixtures/news-items-noise-filtered.jsonl, data/fixtures/metrics-sample.json</files>
  <read_first>data/fixtures/metrics-sample.json, data/fixtures/news-items-complete.jsonl, references/data-models.md</read_first>
  <action>
**Create `data/fixtures/news-items-noise-filtered.jsonl`** (new file) with 4 fixture items demonstrating all noise filtering scenarios. Use fixed timestamps `2026-01-01T00:00:00Z` for determinism (per Phase 8 convention). All items use `_schema_v: 4`.

Item 1 -- Pre-classify noise filtered (matched noise_pattern):
```json
{"id":"fix04noise01abc","title":"[推广] 某某产品限时优惠","url":"https://example.com/promo-item","normalized_url":"https://example.com/promo-item","source_id":"src-test-a","content_summary":null,"categories":{"primary":null,"secondary":[],"tags":[]},"importance_score":0.0,"event_id":null,"fetched_at":"2026-01-01T00:00:00Z","published_at":"2026-01-01T00:00:00Z","form_type":"other","language":"zh","dedup_status":"unique","content_hash":"hash04noise01abc","processing_status":"noise_filtered","duplicate_of":null,"digest_eligible":false,"_schema_v":4}
```

Item 2 -- Pre-classify noise filtered (matched title_discard_pattern):
```json
{"id":"fix05noise02def","title":"每日精选","url":"https://example.com/daily-picks","normalized_url":"https://example.com/daily-picks","source_id":"src-test-a","content_summary":null,"categories":{"primary":null,"secondary":[],"tags":[]},"importance_score":0.0,"event_id":null,"fetched_at":"2026-01-01T01:00:00Z","published_at":"2026-01-01T01:00:00Z","form_type":"other","language":"zh","dedup_status":"unique","content_hash":"hash05noise02def","processing_status":"noise_filtered","duplicate_of":null,"digest_eligible":false,"_schema_v":4}
```

Item 3 -- Post-classify low-importance filtered (importance < 0.25):
```json
{"id":"fix06noise03ghi","title":"某软件 v3.2.1 修复小错误","url":"https://example.com/minor-patch","normalized_url":"https://example.com/minor-patch","source_id":"src-test-b","content_summary":null,"categories":{"primary":"dev-tools","secondary":[],"tags":["patch","bug-fix"]},"importance_score":0.15,"event_id":null,"fetched_at":"2026-01-01T02:00:00Z","published_at":"2026-01-01T02:00:00Z","form_type":"announcement","language":"zh","dedup_status":"unique","content_hash":"hash06noise03ghi","processing_status":"complete","duplicate_of":null,"digest_eligible":false,"_schema_v":4}
```

Item 4 -- Normal item passing all filters (importance >= 0.25, no noise match):
```json
{"id":"fix07normal04jkl","title":"重大 AI 模型突破：推理能力大幅提升","url":"https://example.com/ai-breakthrough","normalized_url":"https://example.com/ai-breakthrough","source_id":"src-test-a","content_summary":"研究团队发布新一代推理模型，在多项基准测试中取得突破性成绩。","categories":{"primary":"ai-models","secondary":[],"tags":["llm","reasoning","benchmark"]},"importance_score":0.85,"event_id":null,"fetched_at":"2026-01-01T03:00:00Z","published_at":"2026-01-01T03:00:00Z","form_type":"news","language":"zh","dedup_status":"unique","content_hash":"hash07normal04jkl","processing_status":"complete","duplicate_of":null,"digest_eligible":true,"_schema_v":4}
```

**Update `data/fixtures/metrics-sample.json`:** Add `"noise_filter_suppressed": 3` to the `items` object (after `"selected_for_output": 15`). The value 3 represents the 3 filtered items from the noise fixture (2 pre-classify + 1 post-classify).
  </action>
  <verify>
    <automated>grep -c "noise_filtered" data/fixtures/news-items-noise-filtered.jsonl && grep -c "digest_eligible" data/fixtures/news-items-noise-filtered.jsonl && grep -c "noise_filter_suppressed" data/fixtures/metrics-sample.json</automated>
  </verify>
  <acceptance_criteria>
    - data/fixtures/news-items-noise-filtered.jsonl exists with exactly 4 lines (4 JSONL items)
    - Item 1 has `processing_status: "noise_filtered"` and `digest_eligible: false` and `categories.primary: null`
    - Item 2 has `processing_status: "noise_filtered"` and `digest_eligible: false`
    - Item 3 has `processing_status: "complete"` and `digest_eligible: false` and `importance_score: 0.15`
    - Item 4 has `processing_status: "complete"` and `digest_eligible: true` and `importance_score: 0.85`
    - All 4 items have `_schema_v: 4`
    - data/fixtures/metrics-sample.json contains `"noise_filter_suppressed": 3` in the items object
  </acceptance_criteria>
  <done>Noise filtering fixture file exists with 4 scenarios (pre-classify pattern match, pre-classify title discard, post-classify low importance, normal pass-through), and metrics fixture includes noise_filter_suppressed count</done>
</task>

</tasks>

<verification>
1. `grep "Post-Classify Importance Filter" references/processing-instructions.md` returns the subsection header
2. `grep "0.25" references/processing-instructions.md` returns the threshold
3. `wc -l data/fixtures/news-items-noise-filtered.jsonl` returns 4
4. `grep "noise_filter_suppressed" data/fixtures/metrics-sample.json` returns the field
5. `grep "3.5" SKILL.md` returns the post-classify step
</verification>

<success_criteria>
- Post-classify importance filter is fully specified with 0.25 threshold, digest_eligible marking, summarization skip, and counter increment
- SKILL.md has step 3.5 for post-classify filtering
- Noise fixture has 4 items covering all scenarios: pre-classify noise_pattern match, pre-classify title_discard match, post-classify low importance, and normal pass-through
- Metrics fixture updated with noise_filter_suppressed field
- Documentation clarifies that noise_filter_suppressed is the sum of pre-classify and post-classify counts
</success_criteria>

<output>
After completion, create `.planning/phases/09-noise-floor-classification-quality/09-03-SUMMARY.md`
</output>

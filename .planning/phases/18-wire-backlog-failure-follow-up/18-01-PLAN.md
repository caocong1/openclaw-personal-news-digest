---
phase: 18-wire-backlog-failure-follow-up
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/backlog_tools.py
  - scripts/run-journal.sh
  - SKILL.md
autonomous: true
requirements:
  - OPER-03
gap_closure: true
must_haves:
  truths:
    - "Every error journal entry in SKILL.md triggers a backlog follow-up entry"
    - "Backlog entries use correct failure_type from the VALID_FAILURE_TYPES enum"
    - "version_drift is recognized as a valid failure_type"
  artifacts:
    - path: "scripts/run-journal.sh"
      provides: "backlog subcommand invoking append_failure_followup"
      min_lines: 5
    - path: "scripts/lib/backlog_tools.py"
      provides: "VALID_FAILURE_TYPES includes version_drift"
      contains: "version_drift"
  key_links:
    - from: "SKILL.md Collection Phase step 4"
      to: "scripts/run-journal.sh backlog"
      via: "bash call after SRC_TIMEOUT/SRC_MALFORMED journal entry"
      pattern: "run-journal.sh append.*error.*SRC"
    - from: "SKILL.md Processing Phase step 5"
      to: "scripts/run-journal.sh backlog"
      via: "bash call after LLM_FAILURE journal entry"
      pattern: "run-journal.sh append.*error.*LLM"
    - from: "SKILL.md Output Phase step 4"
      to: "scripts/run-journal.sh backlog"
      via: "bash call after DIGEST_FAILED journal entry"
      pattern: "run-journal.sh append.*error.*DIGEST"
---

<objective>
Wire backlog failure follow-up into SKILL.md so every error journal entry creates a backlog follow-up entry via `backlog_tools.append_failure_followup`. Also add `version_drift` to VALID_FAILURE_TYPES for completeness.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/dongli/Workspace/openclaw-personal-news-digest/SKILL.md
@/Users/dongli/Workspace/openclaw-personal-news-digest/scripts/lib/backlog_tools.py
@/Users/dongli/Workspace/openclaw-personal-news-digest/scripts/run-journal.sh
@/Users/dongli/Workspace/openclaw-personal-news-digest/.planning/v3.0-MILESTONE-AUDIT.md
@/Users/dongli/Workspace/openclaw-personal-news-digest/.planning/phases/16-operational-hardening/16-VERIFICATION.md

<interfaces>
From scripts/lib/backlog_tools.py:
```python
VALID_FAILURE_TYPES = {"source_timeout", "llm_failure", "version_drift", "degraded_sources"}
def append_failure_followup(run_id, failure_type, summary, recovery_hint, source_ids=None, base_dir="."):
    # Appends JSONL entry to configured backlog path
    # Raises ValueError if failure_type not in VALID_FAILURE_TYPES
```

From scripts/run-journal.sh (existing pattern):
```bash
python3 - "$SCRIPT_DIR" "$BASE_DIR" "$CMD" "${@:3}" <<'PY'
# imports journal_tools, routes by cmd
# append command: journal_append(run_id, severity, stage, code, message, hint, source_id)
```
</interfaces>

There are 3 error journal entries in SKILL.md:

1. **Line 22 (Collection Phase step 4)**: `SRC_TIMEOUT` and `SRC_MALFORMED` for source fetch failures
2. **Line 48 (Processing Phase step 5)**: `LLM_FAILURE` for LLM call failures
3. **Line 75 (Output Phase step 4)**: `DIGEST_FAILED` for digest generation failures

The `security` journal entry (SECURITY_BLOCK) at line 48 should NOT trigger a backlog follow-up -- security events are informational/audit only, not failure follow-up.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version_drift to VALID_FAILURE_TYPES</name>
  <files>scripts/lib/backlog_tools.py</files>
  <action>
Add `version_drift` to the `VALID_FAILURE_TYPES` set in backlog_tools.py. The set currently reads:
  `{"source_timeout", "llm_failure", "version_drift", "degraded_sources"}`

If `version_drift` is already present, this task is a no-op -- verify and mark complete.
  </action>
  <verify>
<automated>grep -q 'version_drift' /Users/dongli/Workspace/openclaw-personal-news-digest/scripts/lib/backlog_tools.py && echo "version_drift found in backlog_tools.py"</automated>
  </verify>
  <done>version_drift is in VALID_FAILURE_TYPES set</done>
</task>

<task type="auto">
  <name>Task 2: Add backlog subcommand to run-journal.sh</name>
  <files>scripts/run-journal.sh</files>
  <action>
Add a `backlog` subcommand to `run-journal.sh` that calls `backlog_tools.append_failure_followup`. Follow the existing heredoc Python pattern already used by `append`, `query`, and `summary`.

The `backlog` subcommand takes:
  - `$BASE_DIR` (sys.argv[2]) -- already available
  - `failure_type` (mapped from journal code, e.g. "source_timeout" for SRC_TIMEOUT)
  - `run_id` (sys.argv[3])
  - `summary` (sys.argv[4])
  - `recovery_hint` (sys.argv[5])
  - `source_ids` (sys.argv[6], optional)

Add `backlog` to the usage comment at the top of the script. Add import for `backlog_tools` and `append_failure_followup` alongside the existing `journal_tools` import. The backlog call uses the same `base_dir` pattern.

Example call:
  bash scripts/run-journal.sh backlog "$RUN_ID" source_timeout "Source $SOURCE_ID failed" "Re-enable source manually" "$SOURCE_ID"
</action>
  <verify>
<automated>grep -q 'backlog_tools\|append_failure_followup' /Users/dongli/Workspace/openclaw-personal-news-digest/scripts/run-journal.sh && echo "backlog subcommand found in run-journal.sh"</automated>
  </verify>
  <done>run-journal.sh accepts backlog subcommand and calls append_failure_followup</done>
</task>

<task type="auto">
  <name>Task 3: Wire backlog calls into SKILL.md after each error journal entry</name>
  <files>SKILL.md</files>
  <action>
After each `bash {baseDir}/scripts/run-journal.sh append ... error ...` call in SKILL.md, add a corresponding `bash {baseDir}/scripts/run-journal.sh backlog` call using the failure_type mapping.

**Mapping (from SKILL.md Operational Rules Rule 4):**
  - `SRC_TIMEOUT` -> `source_timeout`
  - `SRC_MALFORMED` -> `degraded_sources`
  - `LLM_FAILURE` -> `llm_failure`
  - `DIGEST_FAILED` -> `llm_failure` (digest generation failure is an LLM-tier issue)

**Do NOT add backlog calls for warning or security journal entries.**

Three locations to update:

1. **Collection Phase step 4 (line ~22)**: After the `run-journal.sh append ... SRC_TIMEOUT ...` call, add:
   ```
   bash {baseDir}/scripts/run-journal.sh backlog "$RUN_ID" source_timeout "Source $SOURCE_ID failed: $ERROR" "Re-enable source manually or check network" "$SOURCE_ID"
   ```
   For `SRC_MALFORMED` record:
   ```
   bash {baseDir}/scripts/run-journal.sh backlog "$RUN_ID" degraded_sources "Source $SOURCE_ID returned malformed data" "Check source URL and content format" "$SOURCE_ID"
   ```

2. **Processing Phase step 5 (line ~48)**: After the `run-journal.sh append ... LLM_FAILURE ...` call, add:
   ```
   bash {baseDir}/scripts/run-journal.sh backlog "$RUN_ID" llm_failure "LLM call failed after 2 attempts for item $ITEM_ID" "Check API key, budget limits, or network connectivity" "$SOURCE_ID"
   ```

3. **Output Phase step 4 (line ~75)**: After the `run-journal.sh append ... DIGEST_FAILED ...` call, add:
   ```
   bash {baseDir}/scripts/run-journal.sh backlog "$RUN_ID" llm_failure "Digest generation failed: $ERROR" "Check output-templates.md and recent schema changes"
   ```

Also update Operational Rules Rule 4 to reflect that the backlog calls are now implemented (currently describes the mapping but says nothing about the actual call mechanism).
</action>
  <verify>
<automated>grep -c 'run-journal.sh backlog' /Users/dongli/Workspace/openclaw-personal-news-digest/SKILL.md</automated>
  </verify>
  <done>SKILL.md has 3 backlog calls wired after error journal entries (SRC_TIMEOUT, LLM_FAILURE, DIGEST_FAILED)</done>
</task>

</tasks>

<verification>
- `grep -c 'run-journal.sh backlog' SKILL.md` returns 3
- `grep -c 'version_drift' scripts/lib/backlog_tools.py` returns at least 1
- `grep -q 'backlog_tools\|append_failure_followup' scripts/run-journal.sh` confirms backlog subcommand exists
- Verify the 3 backlog calls are placed AFTER (not before) their corresponding journal entries
</verification>

<success_criteria>
- `version_drift` in `VALID_FAILURE_TYPES`
- `run-journal.sh backlog` subcommand calls `append_failure_followup`
- SKILL.md has exactly 3 `run-journal.sh backlog` calls at the correct failure points
- SKILL.md Operational Rules Rule 4 reflects the wired implementation
- No security or warning journal entries trigger backlog follow-ups
</success_criteria>

<output>
After completion, create `.planning/phases/18-wire-backlog-failure-follow-up/18-01-SUMMARY.md`
</output>
